---
phase: 05-foundation-auth-ws
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/src/app/globals.css
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/(auth)/login/page.tsx
  - apps/web/src/lib/cn.ts
autonomous: false
requirements:
  - FND-01
user_setup: []

must_haves:
  truths:
    - Developer can run pnpm install without errors
    - Developer can run pnpm build and it completes successfully
    - React Flow CSS loads correctly (node handles and edges visible)
    - Magic UI animated component plays @keyframes animations
    - No CSS cascade conflicts between Tailwind 4 and third-party CSS
  artifacts:
    - path: apps/web/package.json
      provides: Next.js 16, React 19, Tailwind 4, shadcn/ui, Magic UI dependencies
      contains: '"next": "16.x", "react": "19.x"'
    - path: apps/web/src/app/globals.css
      provides: Tailwind 4 @import, React Flow CSS in @layer base
      contains: '@import "tailwindcss"', '@layer base { @import "@xyflow/react/dist/style.css" }'
    - path: apps/web/src/app/(auth)/login/page.tsx
      provides: Login page placeholder
      contains: 'export default function LoginPage'
    - path: apps/web/src/lib/cn.ts
      provides: cn() utility for class merging
      exports: ['cn']
  key_links:
    - from: apps/web/src/app/globals.css
      to: @xyflow/react/dist/style.css
      via: @layer base import
      pattern: '@layer base.*@import.*xyflow'
    - from: apps/web/package.json
      to: node_modules
      via: pnpm install
      pattern: 'pnpm install'
---

<objective>
Scaffold Next.js 16 application with Tailwind 4, shadcn/ui (new-york), and Magic UI — verify React Flow CSS loads correctly without style conflicts and Magic UI @keyframes animations work.

Purpose: This creates the foundational Next.js 16 app that all subsequent frontend plans build on. The CSS layer ordering verification (React Flow in @layer base) prevents the critical Tailwind 4 specificity bug that breaks node handles and edges in production builds.

Output: Working Next.js 16 app with clean build, verified CSS loading, and placeholder login page.
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-foundation-auth-ws/05-CONTEXT.md
@.planning/phases/05-foundation-auth-ws/05-RESEARCH.md
@.planning/REQUIREMENTS.md
@.claude/skills/nextjs-15/SKILL.md
@.claude/skills/react-19/SKILL.md
@.claude/skills/tailwind-4/SKILL.md

## Critical Constraints from CONTEXT.md (Brain-04 Frontend)

- React Flow CSS MUST be in globals.css @layer base — never in tsx imports (Pitfall 1)
- Magic UI MUST be installed via `npx shadcn@latest add` — NOT magicui-cli directly (Pitfall 3)
- Tailwind 4 uses CSS-only config (no tailwind.config.js)
- shadcn/ui uses new-york style with OKLCH colors
- React Compiler: Leave disabled for v2.1 (unvalidated double-memoization conflicts)
- Package manager: pnpm always for Node.js

## Stack from RESEARCH.md

- Next.js 16.x (proxy.ts replaces middleware.ts)
- React 19.2 (named imports only, no React.default)
- Tailwind 4.x (@import "tailwindcss", CSS-only @theme config)
- shadcn/ui (latest, Tailwind v4 mode, new-york style)
- @xyflow/react 12.x (CSS in globals.css @layer base only)
- tw-animate-css (replaces tailwindcss-animate)
- clsx + tailwind-merge (for cn() utility)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 app with create-next-app</name>
  <files>apps/web/package.json, apps/web/next.config.ts, apps/web/tsconfig.json, apps/web/src/app/layout.tsx, apps/web/src/app/page.tsx, apps/web/src/app/globals.css</files>
  <action>
Create Next.js 16 app in apps/web/ using:
```bash
cd apps/web && pnpm create next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-turbopack
```

Verify package.json has Next.js 16.x and React 19.2. Update globals.css to use Tailwind 4 CSS-only syntax:
- Remove `@tailwind base/components/utilities`
- Add `@import "tailwindcss";`
- Add `@import "tw-animate-css";`
- Add @layer base with design tokens (OKLCH from shadcn/ui new-york)

CRITICAL: Do NOT import React Flow CSS yet — Task 2 handles this with the correct @layer base placement.
  </action>
  <verify>
    <automated>cd apps/web && pnpm install && pnpm build exits 0</automated>
  </verify>
  <done>Next.js 16 scaffolded, pnpm install succeeds, pnpm build completes without errors</done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn/ui, Magic UI, and React Flow with correct CSS layering</name>
  <files>apps/web/package.json, apps/web/src/app/globals.css, apps/web/components.json</files>
  <action>
Install dependencies:
```bash
cd apps/web && pnpm add zustand immer jose clsx tailwind-merge @xyflow/react
cd apps/web && pnpm add -D tw-animate-css
```

Initialize shadcn/ui (selects Tailwind v4 mode automatically):
```bash
cd apps/web && npx shadcn@latest init
```
- Select new-york style
- Select OKLCH for colors
- Confirm CSS variables (shadcn/ui handles Tailwind 4 setup)

Add one Magic UI component via shadcn@latest (NOT magicui-cli):
```bash
cd apps/web && npx shadcn@latest add button
```

CRITICAL: Add React Flow CSS to globals.css inside @layer base (Pitfall 1 prevention):
```css
@import "tailwindcss";
@import "tw-animate-css";

@layer base {
  /* React Flow — MUST be in @layer base to survive Tailwind 4 cascade */
  @import "@xyflow/react/dist/style.css";

  /* Design tokens from shadcn/ui new-york */
  :root {
    --background: 0 0% 100%;
    --foreground: 0 0% 3.9%;
    /* ... OKLCH color tokens ... */
  }
}
```

Verify no tailwind.config.js exists (Tailwind 4 uses CSS-only @theme config).
  </action>
  <verify>
    <automated>grep -q '@layer base' apps/web/src/app/globals.css && grep -q '@import.*xyflow' apps/web/src/app/globals.css && cd apps/web && pnpm build exits 0</automated>
  </verify>
  <done>shadcn/ui and Magic UI installed, React Flow CSS in @layer base, pnpm build clean</done>
</task>

<task type="auto">
  <name>Task 3: Create placeholder login page and utility functions</name>
  <files>apps/web/src/app/(auth)/login/page.tsx, apps/web/src/lib/cn.ts</files>
  <action>
Create (auth) route group and login page placeholder:
- apps/web/src/app/(auth)/login/page.tsx — Server Component with basic login form structure (form submission in Plan 05-02)
- Use shadcn/ui button and input components
- Follow React 19 patterns (no React.default import, named imports from 'react')

Create cn() utility in apps/web/src/lib/cn.ts:
```typescript
import { clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

Create basic app structure:
- apps/web/src/app/layout.tsx — RootLayout with metadata, fonts
- apps/web/src/app/(auth)/layout.tsx — Auth layout (minimal, for login page)
- apps/web/src/app/(protected)/layout.tsx — Placeholder (Plan 05-02 implements AuthGuardLayout)

Follow Tailwind 4 skill: no var() in className, use Tailwind semantic classes.
  </action>
  <verify>
    <automated>test -f apps/web/src/lib/cn.ts && test -f apps/web/src/app/\(auth\)/login/page.tsx && cd apps/web && pnpm build exits 0</automated>
  </verify>
  <done>Login page placeholder exists, cn() utility created, build succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Next.js 16 scaffold with Tailwind 4, shadcn/ui, Magic UI, and React Flow CSS</what-built>
  <how-to-verify>
1. Run `cd apps/web && pnpm build` — verify exit code 0
2. Run `cd apps/web && pnpm dev` — visit http://localhost:3000/login
3. Verify login page renders with shadcn/ui button and input components
4. Install one animated Magic UI component (e.g., BentoGrid):
   ```bash
   cd apps/web && npx shadcn@latest add bento-grid
   ```
5. Create a 2-node ReactFlow smoke test in login/page.tsx to verify CSS:
   - Import ReactFlow, Node, Edge from @xyflow/react
   - Render 2 nodes connected by 1 edge
   - Verify node handles (small dots on edges) are visible
   - Verify edge renders as a line between nodes
6. Check DevTools Console — no CSS warnings or errors
7. If all checks pass, type "approved" to continue
  </how-to-verify>
  <resume-signal>Type "approved" or describe CSS/style issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
- [ ] `pnpm build` exits 0 (no build errors)
- [ ] React Flow handles and edges visible in production build
- [ ] Magic UI @keyframes animations play smoothly
- [ ] No CSS cascade conflicts in DevTools
- [ ] Login page renders with shadcn/ui components
</verification>

<success_criteria>
- Developer can scaffold Next.js 16 app with Tailwind 4, shadcn/ui, and Magic UI
- `pnpm build` completes without errors
- React Flow CSS loads correctly (node handles and edges visible)
- Magic UI animated components play @keyframes animations
- No CSS cascade conflicts between Tailwind 4 and third-party CSS
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation-auth-ws/05-01-SUMMARY.md`
</output>
