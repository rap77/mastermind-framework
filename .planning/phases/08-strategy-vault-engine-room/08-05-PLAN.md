---
phase: 08-strategy-vault-engine-room
plan: 05
type: execute
wave: 4
depends_on: [08-04]
files_modified: [
  "apps/web/src/__tests__/FocusMode.e2e.test.tsx",
  "apps/web/src/__tests__/APIKeyManager.test.tsx",
  "apps/web/src/__tests__/phases/Phase08Integration.test.tsx"
]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Focus Mode activates on task start, user can exit with [Esc], no re-trapping when task running"
    - "API key create shows full key once, list shows masked keys, revoke removes from list"
    - "Full Phase 08 workflow tested: brief submit → Focus Mode → DAG render → logs filter → scrubber sync → API key CRUD"
  artifacts:
    - path: "apps/web/src/__tests__/FocusMode.e2e.test.tsx"
      provides: "E2E tests for Focus Mode behavior"
      exports: ["test_focus_mode_*"]
    - path: "apps/web/src/__tests__/APIKeyManager.test.tsx"
      provides: "Component tests for API key CRUD"
      exports: ["test_api_key_*"]
    - path: "apps/web/src/__tests__/phases/Phase08Integration.test.tsx"
      provides: "Full Phase 08 workflow integration test"
      exports: ["test_phase_08_full_workflow"]
  key_links:
    - from: "Phase08Integration.test.tsx"
      to: "all Phase 08 components"
      via: "mocks all endpoints and WS events, simulates user workflow"
      pattern: "mock.*api|mock.*ws"
---

<objective>
Write integration and E2E tests for Phase 08 features. Verify Focus Mode behavior, API key management CRUD, and full Phase 08 workflow from task submission through log filtering and scrubber sync.

Purpose: Ensure all Phase 08 components work together correctly, catch regressions, document expected behavior through tests.

Output: Three test suites (Focus Mode E2E, API Key Manager, Phase 08 Integration), all passing with >80% coverage.
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md

**Key context:** Phase 08-01 through 08-04 are complete and executing. This plan adds integration tests to verify all components work together. Tests should mock APIs and WS events, not depend on running backend. All tests run in isolation with no side effects.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-strategy-vault-engine-room/08-CONTEXT.md
</context>

<task type="auto">
  <name>Task 1: Write Focus Mode E2E tests</name>
  <files>apps/web/src/__tests__/FocusMode.e2e.test.tsx</files>
  <action>
Create E2E test suite for Focus Mode behavior:

**Test file: FocusMode.e2e.test.tsx**

1. **test_focus_mode_activates_on_task_start:**
   - Setup: Mock BriefInputModal, OrchestratorStore
   - Action: Submit brief → POST /api/tasks success → startTask() called
   - Assert: orchestratorStore.isFocusMode === true, state === 'running', userOverride === false

2. **test_focus_mode_sidebar_collapses:**
   - Setup: Focus Mode active, NexusPage rendered
   - Action: Monitor sidebar visibility
   - Assert: Sidebar hidden (AnimatePresence exit), canvas expands to full width

3. **test_focus_mode_escape_hatch:**
   - Setup: Focus Mode active, task running
   - Action: Press [Esc] key
   - Assert: userOverride === true, Focus Mode still displays (state=running + override=true)

4. **test_focus_mode_no_retrapping:**
   - Setup: Task running, [Esc] pressed (userOverride=true)
   - Action: WS 'task:complete' event arrives → completeTask()
   - Assert: Focus Mode exits, userOverride resets to false, sidebar not hidden anymore

5. **test_focus_mode_toggle_with_f_key:**
   - Setup: Focus Mode active, FocusModeBadge rendered
   - Action: Press [F] key
   - Assert: userOverride toggles, Focus Mode state changes

6. **test_focus_mode_badge_visible:**
   - Setup: Focus Mode active
   - Action: Render FocusModeBadge
   - Assert: Badge visible with "Salir [Esc]" label, positioned top-right

7. **test_idle_tiles_dimmed:**
   - Setup: Focus Mode active, brainStore with mixed statuses
   - Action: Render command center tiles
   - Assert: Idle brain tiles opacity 30%, active brains opacity 100%

8. **test_focus_mode_layout_smooth:**
   - Setup: Focus Mode inactive, canvas 70% width
   - Action: Activate Focus Mode via startTask()
   - Assert: Layout transition smooth (Framer Motion animation runs), no janky width changes

Run: `cd apps/web && pnpm test:run -- FocusMode.e2e.test --watch=false`
Coverage target: 85%+
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run -- FocusMode.e2e.test --watch=false</automated>
  </verify>
  <done>All Focus Mode tests passing, activation/deactivation verified, no re-trapping confirmed, keyboard shortcuts verified, layout transitions smooth, coverage > 85%</done>
</task>

<task type="auto">
  <name>Task 2: Write API Key Manager component tests</name>
  <files>apps/web/src/__tests__/APIKeyManager.test.tsx</files>
  <action>
Create test suite for API key management CRUD:

**Test file: APIKeyManager.test.tsx**

1. **test_loads_key_list:**
   - Setup: Mock GET /api/keys returning 3 masked keys
   - Action: Render APIKeyManager
   - Assert: TanStack Query fetches, keys display with prefix/suffix (e.g., "mmsk_abc...xyz1")

2. **test_create_key_flow:**
   - Setup: APIKeyManager "Create Key" tab
   - Action: Click "Create Key" button → POST /api/keys succeeds → shows full key
   - Assert: Dialog opens, full key displayed in code block, copy button available, warning shown

3. **test_create_key_show_once:**
   - Setup: Key created and displayed
   - Action: Copy key, close dialog
   - Assert: Full key no longer accessible, can't reopen and see it again

4. **test_create_key_security:**
   - Setup: KeyCreateDialog rendering full key
   - Action: Inspect DOM, check localStorage, check network logs
   - Assert: Full key NOT in localStorage, NOT logged to console, only in response body

5. **test_list_keys_masked:**
   - Setup: 5 keys in GET /api/keys response
   - Action: Render KeyListTable
   - Assert: All keys display as masked (prefix "mmsk_abcd" + "..." + suffix "xyz1"), no full keys visible

6. **test_revoke_key_flow:**
   - Setup: 3 keys in list, select one to revoke
   - Action: Click "Revoke" button → confirm dialog → DELETE /api/keys/{id} succeeds
   - Assert: Confirm dialog shown, key removed from list, success toast, cache invalidated

7. **test_revoke_key_confirm:**
   - Setup: Revoke button clicked
   - Action: Cancel in confirm dialog
   - Assert: Key remains in list, DELETE not called

8. **test_revoke_key_error:**
   - Setup: Revoke button clicked, DELETE fails
   - Action: DELETE /api/keys/{id} returns 500
   - Assert: Error toast shown, key remains in list, no cache invalidation

9. **test_key_isolation:**
   - Setup: User A logged in, viewing their keys
   - Action: Mock User B's API key ID, try to delete it
   - Assert: DELETE /api/keys/{other-user-key} returns 403, key not removed

10. **test_tabs_switch:**
    - Setup: APIKeyManager with two tabs (My Keys, Create Key)
    - Action: Click "Create Key" tab
    - Assert: Tab content switches, form/dialog renders

Run: `cd apps/web && pnpm test:run -- APIKeyManager.test --watch=false`
Coverage target: 85%+
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run -- APIKeyManager.test --watch=false</automated>
  </verify>
  <done>All API key tests passing, create show-once verified, masking verified, revoke flow verified, security isolation verified, error handling verified, coverage > 85%</done>
</task>

<task type="auto">
  <name>Task 3: Write Phase 08 full workflow integration test</name>
  <files>apps/web/src/__tests__/phases/Phase08Integration.test.tsx</files>
  <action>
Create end-to-end integration test for entire Phase 08:

**Test scenario (Phase08Integration.test.tsx):**

1. **Setup:** Mock all /api endpoints, mock WS connection, mock router
2. **Render:** Full app with all Phase 08 components

**Test steps:**

1. User on Command Center
   - Assert: BriefInputModal visible, no Focus Mode yet (isFocusMode === false)

2. Submit brief
   - Action: Type brief text, submit form
   - Mock: POST /api/tasks → returns taskId
   - Assert: orchestratorStore.startTask() called, Focus Mode activates

3. Verify Focus Mode activated
   - Assert: Sidebar collapses (width 0), canvas expands to ~100%, FocusModeBadge visible

4. Navigate to Nexus
   - Action: Router navigates to /nexus (NexusPage)
   - Mock: GET /api/tasks/{id}/graph → returns sub-graph with nodes/edges
   - Assert: NexusCanvas renders DAG with Master → Niche Clusters → Brain Executors

5. Simulate WS events (brain updates)
   - Action: Emit WS 'brain:update' events for 3 brains
   - Assert: Brain tiles in command center update status, idle tiles dim to 30%

6. Navigate to Strategy Vault
   - Action: Router navigates to /strategy-vault
   - Mock: GET /api/executions/history → returns past executions
   - Assert: ExecutionList loads, displays table with status/duration/brain count

7. Click past execution
   - Action: Click execution row
   - Mock: GET /api/executions/{id} → returns detail with graph_snapshot, brain_outputs, milestones
   - Assert: ExecutionDetail loads, ReplayNexus renders DAG, accordion shows brain outputs

8. Test scrubber drag
   - Action: Drag scrubber to 75%
   - Assert: currentSnapshotIndex updates, logs panel scrolls to matching timestamp, milestone label updates

9. Filter logs
   - Action: Toggle error level filter off
   - Assert: Error logs hidden, only info/warn logs visible

10. Isolate logs to brain
    - Action: Click brain badge in log
    - Assert: isolatedBrainId set, only that brain's logs shown

11. Navigate to Engine Room
    - Action: Router navigates to /engine-room
    - Assert: Logs tab shows live logs from WS, Config tab placeholder visible

12. Create API key
    - Action: Click "Configuration" tab → "Create Key"
    - Mock: POST /api/keys → returns full key
    - Assert: Dialog shows full key, copy works, key hidden on close

13. List and revoke key
    - Action: Switch to "My Keys" tab → click revoke
    - Mock: DELETE /api/keys/{id} → success
    - Assert: Confirm shown, key removed from list

14. Task completes
    - Action: WS 'task:complete' event
    - Assert: orchestratorStore.completeTask() called, Focus Mode exits, userOverride resets

15. Verify post-task state
    - Action: Inspect final state
    - Assert: Sidebar visible again, canvas back to 70%, no Focus Mode badge, isFocusMode === false

**Implementation notes:**
- Use React Testing Library for UI testing
- Mock fetch for all /api calls
- Mock WebSocket with emit/on helpers
- Use jest.advanceTimersByTime for animations
- Don't depend on actual backend
- Test journey without page reloads (client-side routing)

Run: `cd apps/web && pnpm test:run -- Phase08Integration.test --watch=false`
Coverage target: 90% for critical paths
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run -- phases/Phase08Integration.test --watch=false</automated>
  </verify>
  <done>Integration test passing, full workflow verified (brief → focus → DAG → scrubber → logs → keys → task complete), no breaking changes, all mocks working, coverage > 90%</done>
</task>

</tasks>

<verification>
**Wave 4 completion criteria:**

1. **Focus Mode tests:** 8 tests covering activation, deactivation, escape hatch, no re-trapping, keyboard shortcuts
2. **API Key tests:** 10 tests covering create (show-once), list (masked), revoke, security isolation, errors
3. **Integration test:** 15-step workflow test covering entire Phase 08 from task start to completion
4. **All tests passing:** Coverage > 85% for components, > 90% for critical paths
5. **No regressions:** Phase 07 tests still pass, no new failures

**Phase 08 COMPLETE:**
- ✅ Wave 0 (08-01): Backend infrastructure (7 tasks)
- ✅ Wave 1 (08-02): Strategy Vault frontend (7 tasks)
- ✅ Wave 2 (08-03): Engine Room logs (8 tasks)
- ✅ Wave 3 (08-04): Focus Mode + API keys (7 tasks)
- ✅ Wave 4 (08-05): Integration tests (3 tasks)

**Total Phase 08: 5 plans, 32 tasks, 4 waves (0-3) + integration (Wave 4)**

**v2.1 COMPLETE:**
- Phase 05: Foundation + Auth + WebSocket (5 plans) ✅
- Phase 06: Command Center (3 plans) ✅
- Phase 07: The Nexus (0 plans, visual from Phase 06) ✅
- Phase 08: Strategy Vault, Engine Room, UX Polish (5 plans) ✅
</verification>

<success_criteria>
✅ Integration tests fully verify Phase 08 requirements:

**Measurable completion:**
- `cd apps/web && pnpm test:run -- FocusMode.e2e APIKeyManager Phase08Integration --watch=false` → ALL PASS (>25 tests)
- `npm run build` completes without errors
- Test coverage:
  - Focus Mode: >85%
  - API Keys: >85%
  - Phase 08 Integration: >90% critical paths
  - Overall Phase 08: >85%

**No regressions:**
- Phase 07 tests pass
- Phase 06 tests pass
- Phase 05 tests pass

**Documentation through tests:**
- Focus Mode behavior documented in test cases
- API key security (show-once) documented in test cases
- Full workflow documented in integration test
</success_criteria>

<output>
After completion, create `.planning/phases/08-strategy-vault-engine-room/08-05-SUMMARY.md`

Phase 08 complete. Update ROADMAP.md to finalize v2.1 and outline v2.2.
</output>
