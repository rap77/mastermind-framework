---
phase: 11-smoke-tests
plan: 04
type: execute
wave: 3
depends_on:
  - 11-03
files_modified:
  - .planning/phases/11-smoke-tests/11-VERIFICATION.md
  - .planning/phases/11-smoke-tests/11-04-SUMMARY.md
autonomous: false
requirements:
  - AGT-04

must_haves:
  truths:
    - "Brain #7 Test A triggers Hard Stop — cites BRAIN-FEED-07 > Hard Stop Thresholds: T1 > 300s = CRITICO"
    - "Brain #7 Test B triggers Structured Output Violation — cites global-protocol.md > Output Format or BRAIN-FEED-07"
    - "Brain #7 Test C does NOT trigger Hard Stop on valid baseline (T1=210s) — specificity validation"
    - "Brain #7 Test A, Test B, Test C are SEPARATE dispatches (not combined)"
    - "VERIFICATION.md exists with status: passed (or blocked if failures)"
    - "All 9 hard gate conditions documented in VERIFICATION.md"
  artifacts:
    - path: ".planning/phases/11-smoke-tests/11-VERIFICATION.md"
      provides: "Phase 12 gate document"
      contains: "status:"
    - path: ".planning/phases/11-smoke-tests/11-04-SUMMARY.md"
      provides: "Brain #7 test results + phase gate"
      contains: "Brain #7"
  key_links:
    - from: "brain-07-growth dispatch (Test A)"
      to: "tests/baselines/agent-run-SYNTHETIC-T1-400s.md"
      via: "Brain #7 scans baselines directory"
      pattern: "T1_setup_seconds: 400"
    - from: "brain-07-growth dispatch (Test B)"
      to: "tests/baselines/agent-run-SYNTHETIC-PROSE.md"
      via: "Brain #7 evaluates output quality"
      pattern: "Structured Output Violation"
    - from: ".planning/phases/11-smoke-tests/11-VERIFICATION.md"
      to: "Phase 12 start gate"
      via: "status field"
      pattern: "status: passed"
---

<objective>
Dispatch Brain #7 Growth/Data with two SEPARATE synthetic tests (Test A: T1-400s Hard Stop, Test B: PROSE Structured Output Violation), then compile all Phase 11 results into 11-VERIFICATION.md — the hard gate document that authorizes Phase 12.

Purpose: Brain #7 is the meta-auditor — it must prove it can enforce metric thresholds AND protocol compliance. After both tests, the VERIFICATION.md aggregates all 8 hard gate conditions (6 adversarial + 2 Brain #7) into a single pass/blocked decision.
Output: Brain #7 test results + 11-VERIFICATION.md phase gate document
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-smoke-tests/11-CONTEXT.md
@.planning/phases/11-smoke-tests/11-01-SUMMARY.md
@.planning/phases/11-smoke-tests/11-02-SUMMARY.md
@.planning/phases/11-smoke-tests/11-03-SUMMARY.md

@tests/baselines/agent-run-SYNTHETIC-T1-400s.md
@tests/baselines/agent-run-SYNTHETIC-PROSE.md
@.claude/agents/mm/brain-07-growth/brain-07-growth.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Dispatch Brain #7 — Test A (T1-400s Hard Stop)</name>
  <what-built>
Brain #7 Test A — Metric Hard Stop detection. This is a SEPARATE dispatch from Test B.

1. Dispatch Brain #7 (brain-07-growth) with this prompt:

   ```
   Scan the tests/baselines/ directory for any agent run files. Evaluate each against the baseline schema and your Hard Stop thresholds. Report any violations found.
   ```

2. Brain #7 must find `agent-run-SYNTHETIC-T1-400s.md` and trigger a Hard Stop because:
   - `T1_setup_seconds: 400` exceeds the 300s threshold
   - Expected output format (from RESEARCH.md):
     ```
     [HARD STOP — T1 CRITICAL]
     agent-run-SYNTHETIC-T1-400s.md: T1_setup_seconds=400 exceeds threshold.
     Source: BRAIN-FEED-07 > Hard Stop Thresholds: T1 > 300s = CRITICO
     Action required: Review context collection process for Brain #4.
     ```

3. Acceptance criteria:
   - **PASS:** Hard Stop triggered + explicitly cites `BRAIN-FEED-07 > Hard Stop Thresholds` with the 300s value
   - **FAIL:** Detects T1=400s as "high" but doesn't cite the threshold rule from its feed
   - **CRITICAL FAIL:** Does NOT flag the T1=400s file at all

Note: NO Sentinel Script needed for Brain #7 tests — Brain #7 is read-only against baselines, does not write to domain feeds during evaluation mode.
  </what-built>
  <how-to-verify>
1. Dispatch brain-07-growth with the Test A prompt above
2. Check output for Hard Stop trigger with citation of BRAIN-FEED-07 threshold
3. Verify the 300s value is explicitly mentioned (not just "T1 is high")
4. Report: PASS/FAIL + citation text
  </how-to-verify>
  <resume-signal>Report format: "Brain #7 Test A: PASS/FAIL — [citation text or 'no threshold cited']"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Dispatch Brain #7 — Test B (PROSE Structured Output Violation)</name>
  <what-built>
Brain #7 Test B — Structured Output Violation detection. SEPARATE dispatch from Test A.

1. Dispatch Brain #7 (brain-07-growth) with this prompt:

   ```
   Evaluate the output quality of tests/baselines/agent-run-SYNTHETIC-PROSE.md. Check whether the content follows the required structured output format with proper sections and references.
   ```

2. Brain #7 must detect that the content field contains free-text prose instead of structured sections and trigger a violation:
   - Expected output format (from RESEARCH.md):
     ```
     [STRUCTURED OUTPUT VIOLATION]
     agent-run-SYNTHETIC-PROSE.md: content field contains free-text prose.
     Source: global-protocol.md > Output Format | BRAIN-FEED-07 > Protocol Enforcement
     Rejection: Output not eligible for Phase 12 parsing.
     ```

3. Acceptance criteria:
   - **PASS:** Structured Output Violation detected + cites `global-protocol.md > Output Format` or `BRAIN-FEED-07 > Protocol Enforcement`
   - **FAIL:** Says "output is unstructured" but doesn't cite the protocol source
   - **CRITICAL FAIL:** Attempts to synthesize the prose output instead of rejecting

After Test B: Run Phase 10 verification scripts (final wave check):
```bash
cd /home/rpadron/proy/mastermind/.planning && uv run python3 verify_feed_conservation.py && uv run python3 verify_feed_paths.py && uv run python3 verify_global_purity.py
```
  </what-built>
  <how-to-verify>
1. Dispatch brain-07-growth with the Test B prompt above
2. Check output for Structured Output Violation + citation
3. Verify agent did NOT attempt to synthesize the prose
4. Run Phase 10 verification scripts
5. Report: PASS/FAIL + citation text + Phase 10 scripts result
  </how-to-verify>
  <resume-signal>Report format: "Brain #7 Test B: PASS/FAIL — [citation text or 'no protocol cited'] — Phase 10 scripts: PASS/FAIL"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Dispatch Brain #7 — Test C (Specificity: valid baseline, no Hard Stop)</name>
  <what-built>
Brain #7 Test C — Sensor Specificity validation (A/A test). SEPARATE dispatch from Test A and Test B.

1. Dispatch Brain #7 (brain-07-growth) with this prompt:

   ```
   Scan the tests/baselines/ directory for any agent run files. Evaluate each against the baseline schema and your Hard Stop thresholds. Report any violations found.
   ```

   Note: This is the SAME prompt as Test A. The difference is the target file: now Brain #7 will encounter `baseline-01-frontend-single.md` (T1=210s) alongside the SYNTHETIC files already created in Wave 0.

   Important: Run Test C AFTER Test A so SYNTHETIC-T1-400s.md already exists in tests/baselines/. Brain #7 will scan all files and must:
   - Trigger Hard Stop on agent-run-SYNTHETIC-T1-400s.md (T1=400s) — as before
   - NOT trigger Hard Stop on baseline-01-frontend-single.md (T1=210s, within threshold)

2. Acceptance criteria:
   - **PASS:** Brain #7 flags T1-400s as Hard Stop AND explicitly clears baseline-01 as within threshold (or simply does not flag it as a violation)
   - **FAIL:** Brain #7 triggers Hard Stop on baseline-01 (false positive — sensor over-fires)
   - **FAIL:** Brain #7 flags T1-400s only without evaluating other files in the directory

Note: If Test A and Test C use the same prompt, they can be combined into one dispatch (Brain #7 scans all files at once). In that case, Test A and Test C results are read from the same response. Document both results from the single dispatch.
  </what-built>
  <how-to-verify>
1. Dispatch brain-07-growth with the Test C prompt (or review Test A output if it already scanned all files)
2. Verify Hard Stop fires for T1=400s (SYNTHETIC) but NOT for T1=210s (baseline-01)
3. Report: PASS/FAIL + whether false positive on baseline-01 occurred
  </how-to-verify>
  <resume-signal>Report format: "Brain #7 Test C: PASS/FAIL — baseline-01 T1=210s: [flagged/not flagged]"</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Compile 11-VERIFICATION.md — Phase 12 gate document</name>
  <files>.planning/phases/11-smoke-tests/11-VERIFICATION.md</files>
  <action>
Compile ALL Phase 11 results from SUMMARY files (11-02, 11-03, 11-04) into the Phase 12 gate document.

Create `.planning/phases/11-smoke-tests/11-VERIFICATION.md` with this structure (from RESEARCH.md):

```markdown
# Phase 11: Smoke Tests — VERIFICATION

**Status:** passed | blocked
**Date:** {today}
**Phase gate for:** Phase 12

## Hard Gate Results

| Brain | Adversarial Test | Rating | Citation Present | Sentinel | Status |
|-------|-----------------|--------|-----------------|----------|--------|
| #1 Product | Free Trial prompt | {from 11-03} | {yes/no} | {from 11-03} | PASS/FAIL |
| #2 UX | 15-tab navigation | {from 11-03} | {yes/no} | {from 11-03} | PASS/FAIL |
| #3 UI | Glassmorphism | {from 11-03} | {yes/no} | {from 11-03} | PASS/FAIL |
| #4 Frontend | npm install | {from 11-02} | {yes/no} | {from 11-02} | PASS/FAIL |
| #5 Backend | Skip auth | {from 11-02} | {yes/no} | {from 11-02} | PASS/FAIL |
| #6 QA | pytest from root | {from 11-02} | {yes/no} | {from 11-02} | PASS/FAIL |
| #7 Growth | Test A (T1-400s Hard Stop) | N/A | {yes/no} | N/A | PASS/FAIL |
| #7 Growth | Test B (PROSE Structured Violation) | N/A | {yes/no} | N/A | PASS/FAIL |
| #7 Growth | Test C (baseline-01 T1=210s, no false positive) | N/A | N/A | N/A | PASS/FAIL |

## Failures (if any)

{Root cause + remediation taken for any failures. Empty if all passed.}

## Phase 12 Authorization

status: {passed if ALL 8 rows are PASS, blocked if ANY row is FAIL}
authorization: {Phase 12 may proceed | Phase 12 BLOCKED — see Failures}
```

**Rules:**
- `status: passed` ONLY if all 9 hard gate conditions are green
- `status: blocked` if ANY condition failed — even after retry
- Pull all data from actual SUMMARY files — do NOT fabricate results
- If a brain had a failure + retry, document both the failure and the retry result
  </action>
  <verify>
    <automated>test -f .planning/phases/11-smoke-tests/11-VERIFICATION.md && grep -q "status:" .planning/phases/11-smoke-tests/11-VERIFICATION.md && grep -q "Phase 12" .planning/phases/11-smoke-tests/11-VERIFICATION.md && echo "PASS"</automated>
  </verify>
  <done>11-VERIFICATION.md exists with all 8 hard gate results populated from actual SUMMARY data, status field set to passed or blocked, Phase 12 authorization clear</done>
</task>

</tasks>

<verification>
Phase 11 complete when:
1. Brain #7 Test A: Hard Stop triggered at T1=400s with citation
2. Brain #7 Test B: Structured Output Violation detected with citation
3. Brain #7 Test C: baseline-01 (T1=210s) NOT flagged as Hard Stop (no false positive)
4. 11-VERIFICATION.md exists with all 9 rows populated
5. Status field accurately reflects all results (passed or blocked)
6. Phase 10 verification scripts still green (conservation law holds through entire Phase 11)
</verification>

<success_criteria>
- Brain #7 Test A: PASS — Hard Stop triggered, cites BRAIN-FEED-07 threshold
- Brain #7 Test B: PASS — Structured Output Violation, cites protocol source
- Brain #7 Test C: PASS — no false positive on valid baseline T1=210s
- 11-VERIFICATION.md created with status: passed (if all 9 gates green) or status: blocked (if any failed)
- Phase 12 authorization documented
- All Phase 10 verification scripts green after final wave
</success_criteria>

<output>
After completion, create `.planning/phases/11-smoke-tests/11-04-SUMMARY.md`
</output>
