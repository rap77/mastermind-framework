---
phase: 06-command-center
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/api/routes/brains.py
  - apps/api/src/api/routes/__init__.py
  - apps/api/tests/api/test_brains_endpoint.py
  - apps/api/src/core/brain_registry.py
autonomous: true
requirements:
  - BE-01
user_setup: []

must_haves:
  truths:
    - "GET /api/brains returns brains with pagination support (page, page_size, total)"
    - "Default: page=1, page_size=24 returns all current brains without breaking frontend"
    - "Response includes name, niche, status, uptime, last_called_at for each brain"
    - "Endpoint is protected by JWT authentication"
    - "IDOR protection: WHERE user_id = current_user (CRITICAL - OWASP A01)"
    - "Integration tests verify pagination, JWT protection, and IDOR prevention"
  artifacts:
    - path: "apps/api/src/api/routes/brains.py"
      provides: "GET /api/brains endpoint"
      exports: ["router"]
    - path: "apps/api/tests/api/test_brains_endpoint.py"
      provides: "Integration tests for brains endpoint"
      min_lines: 30
  key_links:
    - from: "apps/api/src/api/routes/brains.py"
      to: "brain_registry.py"
      via: "import and call get_all_brains()"
      pattern: "from.*brain_registry.*import|get_all_brains"
    - from: "apps/api/src/api/routes/__init__.py"
      to: "brains.py"
      via: "router registration"
      pattern: "include_router.*brains"
---

<objective>
Add GET /api/brains endpoint to FastAPI backend — return all 24 brains with live metadata from brain_registry.py, with pagination support for scalability

Purpose: Provide the frontend Command Center with live brain data (name, niche, status, uptime, last_called_at) to power the Bento Grid visualization. Pagination ensures Margin of Safety for future scalability (e.g., 50+ brains). Real-time updates use existing WebSocket from Phase 05.

Output: Working /api/brains endpoint protected by JWT, with pagination (page/page_size), and integration tests
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-command-center/06-CONTEXT-FINAL.md
@.planning/phases/06-command-center/BRAIN-05-BACKEND-CONTEXT.md
@.planning/phases/05-foundation-auth-ws/05-02-SUMMARY.md

# Existing backend patterns
From apps/api/src/core/brain_registry.py:
```python
# Brain registry already exists with brain definitions
# Need to expose get_all_brains() function
```

From apps/api/src/api/routes/tasks.py (existing pattern):
```python
# JWT authentication pattern:
from fastapi import Depends
from src.api.dependencies import get_current_user
# Use: Depends(get_current_user) in route signature
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add paginated get_all_brains() to brain_registry.py</name>
  <files>apps/api/src/core/brain_registry.py</files>
  <behavior>
    - Test 1: Returns paginated response (brains, total, page, page_size)
    - Test 2: Default page_size=24 returns all current brains
    - Test 3: Each brain has name, niche, status, uptime, last_called_at
    - Test 4: Niche is one of: master, software, marketing
    - Test 5: Status is one of: idle, active, error, complete
    - Test 6: page_size=10 with page=1 returns first 10 brains, total=24
  </behavior>
  <action>
    Add get_all_brains() function to brain_registry.py that:
    - Accepts: page (int, default=1), page_size (int, default=24, max=100), user_id (str)
    - Returns Dict: {brains: List[Dict], total: int, page: int, page_size: int}
    - Each brain dict: {id, name, niche, status, uptime, last_called_at}
    - Source from existing BRAIN_DEFINITIONS or registry
    - uptime: float (seconds since last start)
    - last_called_at: Optional[datetime]
    - Implements pagination logic (slice with offset = (page-1) * page_size)
    - CRITICAL: Filter by user_id to prevent IDOR (OWASP A01 - Broken Access Control)
    - Note: For v2.1, all users see same 24 brains. user_id param prepares for multi-tenant future.

    Add PaginatedBrainsResponse Pydantic model:
    - Field brains: List[BrainMetadata]
    - Field total: int
    - Field page: int
    - Field page_size: int

    Security note: In future multi-tenant version, brains will have owner_id field. For now, user_id validation prepares the architecture.
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/core/test_brain_registry.py::test_get_all_brains_paginated -x</automated>
  </verify>
  <done>get_all_brains() returns paginated response with metadata</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create GET /api/brains endpoint with pagination</name>
  <files>apps/api/src/api/routes/brains.py</files>
  <behavior>
    - Test 1: GET /api/brains?page=1&page_size=24 returns 200 with valid JWT
    - Test 2: Returns paginated response with total count
    - Test 3: GET /api/brains (no params) defaults to page=1, page_size=24
    - Test 4: Returns 401 without JWT cookie
    - Test 5: Response matches Zod schema (brains, total, page, page_size)
    - Test 6: IDOR protection: User A cannot access User B's brains (when multi-tenant)
    - Test 7: get_current_user returns User object with valid id field
  </behavior>
  <action>
    Create apps/api/src/api/routes/brains.py:
    - FastAPI router with GET /brains endpoint
    - Query params: page (int, default=1), page_size (int, default=24, max=100)
    - JWT auth via Depends(get_current_user) → extracts user_id from token
    - Call get_all_brains(page, page_size, user_id) from brain_registry
    - Return JSON response with pagination metadata
    - CRITICAL: get_current_user must return User object with id field for IDOR protection

    Note: Real-time brain status updates use existing WebSocket from Phase 05. No SSE needed.

    Register router in apps/api/src/api/routes/__init__.py

    Security implementation:
    ```python
    from src.api.dependencies import get_current_user
    from src.models.user import User

    @router.get("/brains")
    async def get_brains(
        page: int = 1,
        page_size: int = 24,
        current_user: User = Depends(get_current_user)
    ):
        # get_current_user validates JWT and returns User with id
        return get_all_brains(page, page_size, current_user.id)
    ```
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/api/test_brains_endpoint.py -x</automated>
  </verify>
  <done>GET /api/brains with pagination endpoint (WebSocket handles real-time)</done>
</task>

<task type="auto">
  <name>Task 3: Update Zod schema bridge</name>
  <files>apps/web/src/types/api.ts</files>
  <action>
    Run the Zod schema generator to sync new endpoint:
    - Execute schema bridge script from Phase 05
    - Verify BrainResponse type is generated
    - Fields: name, niche, status, uptime, last_called_at
  </action>
  <verify>
    <automated>grep -q "BrainResponse" apps/web/src/types/api.ts && echo "Schema synced"</automated>
  </verify>
  <done>TypeScript types updated with BrainResponse schema</done>
</task>

</tasks>

<verification>
1. Run pytest: `cd apps/api && uv run pytest tests/api/test_brains_endpoint.py -v`
2. Test endpoint manually: `curl -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/brains`
3. Verify response has 24 brains with all required fields
4. Confirm JWT protection: `curl http://localhost:8000/api/brains` returns 401
</verification>

<success_criteria>
- GET /api/brains returns 200 with valid JWT
- Response contains pagination metadata (total, page, page_size)
- Default (no params) returns all 24 current brains
- page_size=10, page=1 returns first 10 brains, total=24
- Endpoint is protected (401 without auth)
- IDOR protection: user_id from JWT is passed to get_all_brains()
- All integration tests pass (pagination + auth + IDOR)
- Zod schema updated in frontend (PaginatedBrainsResponse)
- Real-time updates use existing WebSocket (no SSE)
- Security: OWASP A01 (Broken Access Control) mitigated
</success_criteria>

<output>
After completion, create `.planning/phases/06-command-center/06-01-SUMMARY.md`
</output>
