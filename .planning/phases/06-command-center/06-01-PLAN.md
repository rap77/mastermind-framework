---
phase: 06-command-center
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/api/routes/brains.py
  - apps/api/src/api/routes/__init__.py
  - apps/api/tests/api/test_brains_endpoint.py
  - apps/api/src/core/brain_registry.py
autonomous: true
requirements:
  - BE-01
user_setup: []

must_haves:
  truths:
    - "GET /api/brains returns all 24 brains with metadata"
    - "Response includes name, niche, status, uptime, last_called_at for each brain"
    - "Endpoint is protected by JWT authentication"
    - "Integration tests verify the endpoint works end-to-end"
  artifacts:
    - path: "apps/api/src/api/routes/brains.py"
      provides: "GET /api/brains endpoint"
      exports: ["router"]
    - path: "apps/api/tests/api/test_brains_endpoint.py"
      provides: "Integration tests for brains endpoint"
      min_lines: 30
  key_links:
    - from: "apps/api/src/api/routes/brains.py"
      to: "brain_registry.py"
      via: "import and call get_all_brains()"
      pattern: "from.*brain_registry.*import|get_all_brains"
    - from: "apps/api/src/api/routes/__init__.py"
      to: "brains.py"
      via: "router registration"
      pattern: "include_router.*brains"
---

<objective>
Add GET /api/brains endpoint to FastAPI backend — return all 24 brains with live metadata from brain_registry.py

Purpose: Provide the frontend Command Center with live brain data (name, niche, status, uptime, last_called_at) to power the Bento Grid visualization
Output: Working /api/brains endpoint protected by JWT, with integration tests
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-command-center/06-CONTEXT-FINAL.md
@.planning/phases/06-command-center/BRAIN-05-BACKEND-CONTEXT.md
@.planning/phases/05-foundation-auth-ws/05-02-SUMMARY.md

# Existing backend patterns
From apps/api/src/core/brain_registry.py:
```python
# Brain registry already exists with brain definitions
# Need to expose get_all_brains() function
```

From apps/api/src/api/routes/tasks.py (existing pattern):
```python
# JWT authentication pattern:
from fastapi import Depends
from src.api.dependencies import get_current_user
# Use: Depends(get_current_user) in route signature
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add get_all_brains() to brain_registry.py</name>
  <files>apps/api/src/core/brain_registry.py</files>
  <behavior>
    - Test 1: Returns list of 24 brain dictionaries
    - Test 2: Each brain has name, niche, status, uptime, last_called_at
    - Test 3: Niche is one of: master, software, marketing
    - Test 4: Status is one of: idle, active, error, complete
  </behavior>
  <action>
    Add get_all_brains() function to brain_registry.py that:
    - Returns List[Dict] with all 24 brains
    - Each dict: {id, name, niche, status, uptime, last_called_at}
    - Source from existing BRAIN_DEFINITIONS or registry
    - uptime: float (seconds since last start)
    - last_called_at: Optional[datetime]
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/core/test_brain_registry.py::test_get_all_brains -x</automated>
  </verify>
  <done>get_all_brains() returns 24 brains with required fields</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create GET /api/brains endpoint</name>
  <files>apps/api/src/api/routes/brains.py</files>
  <behavior>
    - Test 1: GET /api/brains returns 200 with valid JWT
    - Test 2: Returns list of brains with correct structure
    - Test 3: Returns 401 without JWT cookie
    - Test 4: Response matches Zod schema (name, niche, status, uptime, last_called_at)
  </behavior>
  <action>
    Create apps/api/src/api/routes/brains.py:
    - FastAPI router with GET /brains endpoint
    - JWT auth via Depends(get_current_user)
    - Call get_all_brains() from brain_registry
    - Return JSON response: {"brains": [...]}
    - Register router in apps/api/src/api/routes/__init__.py
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/api/test_brains_endpoint.py -x</automated>
  </verify>
  <done>GET /api/brains protected endpoint returns all brains</done>
</task>

<task type="auto">
  <name>Task 3: Update Zod schema bridge</name>
  <files>apps/web/src/types/api.ts</files>
  <action>
    Run the Zod schema generator to sync new endpoint:
    - Execute schema bridge script from Phase 05
    - Verify BrainResponse type is generated
    - Fields: name, niche, status, uptime, last_called_at
  </action>
  <verify>
    <automated>grep -q "BrainResponse" apps/web/src/types/api.ts && echo "Schema synced"</automated>
  </verify>
  <done>TypeScript types updated with BrainResponse schema</done>
</task>

</tasks>

<verification>
1. Run pytest: `cd apps/api && uv run pytest tests/api/test_brains_endpoint.py -v`
2. Test endpoint manually: `curl -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/brains`
3. Verify response has 24 brains with all required fields
4. Confirm JWT protection: `curl http://localhost:8000/api/brains` returns 401
</verification>

<success_criteria>
- GET /api/brains returns 200 with valid JWT
- Response contains exactly 24 brains
- Each brain has: name, niche, status, uptime, last_called_at
- Endpoint is protected (401 without auth)
- All integration tests pass
- Zod schema updated in frontend
</success_criteria>

<output>
After completion, create `.planning/phases/06-command-center/06-01-SUMMARY.md`
</output>
