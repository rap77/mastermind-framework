---
phase: 06-command-center
task: UAT Manual
status: in_progress
last_updated: 2026-03-21T22:45:00Z
---

**UAT Manual testing** de Phase 06 después del reinicio de Windows.

**Servicios corriendo:**
- Next.js dev: http://localhost:3000 ✅
- FastAPI (Docker): http://localhost:8000 ✅

**Fixed today:**
- Server Action moved to /app/actions/tasks (route handler conflict)
- Test fixture brains.ts corrected (BrainMetadata)
- Added dynamic export to command-center page
- MM_SECRET_KEY passed explicitly
- **CRITICAL:** fetchBrains() ahora pasa Authorization header con JWT token
- **Login redirect:** Cambiado de `/` a `/command-center` (estaba yendo a WS Test page)
- **Marketing brains fix:** brain_registry.py ahora carga brains-marketing.yaml ✅

**Bug crítico fixeado (Marketing brains):**
- `brain_registry.py` solo cargaba brains.yaml (8 cerebros)
- Ahora carga TODOS los archivos brains*.yaml del config directory
- Marketing brains (M1-M16) mapeados a IDs numéricos (9-24)
- Niche de marketing actualizado a 'marketing-digital' en frontend

**Backend changes:**
- `apps/api/mastermind_cli/brain_registry.py`:
  - `_load_yaml_file()` helper para cargar múltiples YAMLs
  - `load_brain_configs()` carga brains.yaml + brains-marketing.yaml
  - Marketing brains (M1-M16) → numeric IDs (9-24)
  - `get_all_brains()` retorna string IDs (brain-01 a brain-24)

**Frontend changes:**
- `apps/web/src/config/clusters.ts`: niche actualizado a 'marketing-digital'

**Verification:**
- 24 brains totales cargados correctamente
- Software Development: 7 brains (brain-01 a brain-07)
- Universal: 1 brain (brain-08)
- Marketing Digital: 16 brains (brain-09 a brain-24)

**Next:** Probar login → Command Center page debe mostrar 16 Marketing brains
