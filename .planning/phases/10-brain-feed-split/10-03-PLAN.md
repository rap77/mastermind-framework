---
phase: 10-brain-feed-split
plan: "03"
type: execute
wave: 3
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - .planning/BRAIN-FEED.md
autonomous: false
requirements:
  - FEED-01

must_haves:
  truths:
    - "Global BRAIN-FEED.md has zero domain-specific vocabulary (verify_global_purity.py passes silently)"
    - "All 3 verification scripts exit 0 (conservation, paths, purity)"
    - "Global feed has < 20 bullet entries after cleanup"
    - "2 outdated self-referential Phase 09 entries removed from global feed (KNOWN_DELETIONS=2 accounted)"
    - "Stack (Locked) table PRESERVED in global feed — it is product/architecture decision, NOT migrated"
    - "Implemented Features table PRESERVED in global feed — cross-domain reference for all brains"
    - "Human confirms global feed entry count < 20 and reads coherently without domain specifics"
  artifacts:
    - path: ".planning/BRAIN-FEED.md"
      provides: "Global feed — cleaned, purity-verified, cross-domain only"
      contains: "Brain Agent Architecture"
      min_lines: 40
  key_links:
    - from: ".planning/verify_global_purity.py"
      to: ".planning/BRAIN-FEED.md"
      via: "Word-boundary regex on domain vocabulary — silent pass, verbose fail"
      pattern: "sys.exit\\(1\\)"
    - from: ".planning/verify_feed_conservation.py"
      to: "all 8 feed files"
      via: "Set equality with KNOWN_DELETIONS=2"
      pattern: "KNOWN_DELETIONS = 2"
---

<objective>
Clean up the global BRAIN-FEED.md: remove all domain-specific entries (migrated to domain feeds in Plans 10-01 and 10-02), remove the 2 outdated self-referential Phase 09 notes, and verify the three-script conservation law holds. The global feed emerges as a lean cross-domain reference (< 20 entries).

Purpose: Without this cleanup, the global feed remains monolithic and the two-level architecture is incomplete. Agents reading the global feed would still see Frontend/Backend/QA-specific patterns they don't need — adding noise and risk of hallucination in other domains. The purity linter is the objective measure of correctness.

Output: Modified BRAIN-FEED.md (global, lean) + all 3 verification scripts passing. Phase 10 complete.
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-brain-feed-split/10-CONTEXT.md
@.planning/phases/10-brain-feed-split/10-RESEARCH.md
@.planning/BRAIN-FEED.md
@.planning/phases/10-brain-feed-split/10-01-SUMMARY.md
@.planning/phases/10-brain-feed-split/10-02-SUMMARY.md

<interfaces>
<!-- What STAYS in global BRAIN-FEED.md (Ownership-First decision — DO NOT REMOVE) -->

STAY — Stack (Locked) table: product/architecture decision affecting all 7 brains equally.

STAY — Brain Agent Architecture section (7 bullets):
- Brain Bundle = 3-file directory: brain-NN-domain.md + criteria.md + warnings.md — never combine into one file
- `model: inherit` + `mcpServers: notebooklm-mcp` required in all brain agent frontmatter
- `global-protocol.md` is the governance layer — agents read it, never duplicate its constraints inline
- No notebook IDs in agent files — brain-selection.md is the single source of truth
- Brain #7 dispatched AFTER domain brains (#1-#6) always — never in parallel, never without domain context
- Brain #7 uses [CROSS-DOMAIN REALITY] block — synthesizes domain outputs, NOT codebase state

STAY — Delta-Velocity Measurement section (5 bullets): cross-domain measurement framework.

STAY — Implemented Features table: cross-domain "what exists" reference preventing re-building shipped features.

STAY — Active Constraints (5 of 9 bullets):
- No `npm` or `pip` — pnpm for Node, uv for Python
- Brain #7 dispatch order — ALWAYS after domain brains complete, never concurrent
- No notebook IDs in agent files — decouple via brain-selection.md
- Structured output required in all brain agent responses — free-text prose causes information leaks
[The 4 Frontend-specific constraint bullets were migrated to BRAIN-FEED-04-frontend.md in Plan 10-01]

STAY — Phase 09 learnings (4 of 6 bullets — global authoring rules):
- Brain Bundle invariant (3-file pattern) — all brains need to author consistently
- [CORRECTED ASSUMPTIONS] twice rule — authoring rule for all agents
- Output Format section non-negotiable — baseline 04 cascade proof applies cross-domain
- Brain #7 [CROSS-DOMAIN REALITY] distinction — evaluation architecture for all brains to understand

STAY — Anti-patterns (5 of 12 — cross-domain authoring rules):
- Free-text prose in brain output → use Structured Output Format section in every agent
- Notebook IDs hardcoded in agent files → reference brain-selection.md as single source of truth
- Brain #7 dispatched in parallel with domain brains → always dispatch AFTER domain brains complete

<!-- What is REMOVED from global BRAIN-FEED.md -->

REMOVE (migrated — 4 sections now in domain feeds):
- State Management section (4 bullets) → was migrated to BRAIN-FEED-04-frontend.md
- React Flow section (5 bullets) → was migrated to BRAIN-FEED-04-frontend.md
- Auth & Security section (4 bullets) → was migrated to BRAIN-FEED-05-backend.md
- API section (2 bullets) → was migrated to BRAIN-FEED-05-backend.md
- Active Constraints Frontend-specific bullets (4) → was migrated to BRAIN-FEED-04-frontend.md
- Phase 05 learnings (4 bullets) → migrated to BRAIN-FEED-04 (3) and BRAIN-FEED-06 (1)
- Phase 06 learnings (4 bullets) → migrated to BRAIN-FEED-02 (1), BRAIN-FEED-04 (2), BRAIN-FEED-06 (1)
- Phase 09 adversarial baseline bullet → migrated to BRAIN-FEED-06-qa.md
- Anti-patterns Frontend rows (5 rows) → migrated to BRAIN-FEED-04-frontend.md
- Anti-patterns Backend rows (2 rows) → migrated to BRAIN-FEED-05-backend.md
- Anti-patterns QA row (1 row) → migrated to BRAIN-FEED-06-qa.md

REMOVE (intentional deletion — KNOWN_DELETIONS=2, NOT migrated):
1. "BRAIN-FEED-NN-domain.md domain split files do NOT exist yet — Phase 10 creates them" (Architecture section bullet) → outdated once Phase 10 completes
2. "BRAIN-FEED-NN-domain.md created in Phase 09 (Premature — empty files are noise)" (Anti-patterns table row) → outdated once Phase 10 completes

These 2 deletions are the KNOWN_DELETIONS constant in verify_feed_conservation.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up global BRAIN-FEED.md — remove domain entries and outdated notes</name>
  <files>.planning/BRAIN-FEED.md</files>
  <action>
Rewrite BRAIN-FEED.md keeping ONLY the cross-domain content per the Ownership-First rule. Do NOT rewrite from scratch — edit the existing file surgically to remove migrated and deleted entries.

**Sections to REMOVE entirely (entries migrated to domain feeds):**
- `### State Management` subsection (4 bullets)
- `### React Flow` subsection (5 bullets)
- `### Auth & Security` subsection (4 bullets) — rename to note it moved to #5 if you wish, but remove entries
- `### API` subsection (2 bullets)
- `### Phase 05 — Foundation, Auth & WS Infrastructure` subsection (all 4 bullets)
- `### Phase 06 — Command Center` subsection (all 4 bullets)
- The "Phase 07" and "Phase 08" placeholder entries (they say "not yet distilled" — remove entirely)

**From Active Constraints section — REMOVE these 4 bullets (migrated to #4 Frontend):**
- `React Compiler: DISABLED`
- `No inline NODE_TYPES`
- `No layout recalculation on WS events`
- `WS updates touch only 'data' prop of nodes`

**From Phase 09 learnings section — REMOVE this 1 bullet (migrated to #6 QA + #7 Growth):**
- `Adversarial baseline delta_velocity=4 signal`

**From Anti-patterns table — REMOVE these rows (migrated to domain feeds):**
- `useStore()` for brain state
- WS reconnect on every render
- `jwt.verify()` from jsonwebtoken
- `localStorage` for JWT
- Inline `NODE_TYPES` in JSX
- Recalculate dagre on data update
- `tailwind.config.js`
- `uv run pytest` from project root

**INTENTIONAL DELETIONS (KNOWN_DELETIONS=2 — do NOT migrate, just remove):**
- The Brain Agent Architecture bullet that says: "`BRAIN-FEED-NN-domain.md` domain split files do NOT exist yet — Phase 10 creates them"
- The Anti-patterns table row: "`BRAIN-FEED-NN-domain.md` created in Phase 09 | Premature — empty files are noise | Phase 10 creates them with proper content"

**Update the file header** to reflect the new state:
```markdown
> Living document. Updated after each completed phase.
> Two-level architecture: this global feed (cross-domain) + 7 domain feeds (BRAIN-FEED-NN-domain.md).
> Global feed: product decisions, UX decisions, milestones affecting ALL 7 brains equally. Zero technical entries.
> Domain feeds: see .planning/BRAIN-FEED-NN-domain.md for each brain's domain-specific patterns.
> Last updated: 2026-03-28 after Phase 10 (BRAIN-FEED Split)
```

**Remove the gap warning line:** `⚠️ Gap: Phase 07 and 08 learnings not yet distilled` — no longer relevant after cleanup.

After editing, count the bullet entries in the global feed:
`python3 -c "lines = open('.planning/BRAIN-FEED.md').read().splitlines(); bullets = [l for l in lines if l.startswith('- ')]; print(f'Global entries: {len(bullets)}')"`

Target: < 20 bullet entries. If count > 20, there is still domain-specific content — review and continue removing.
  </action>
  <verify>
    <automated>python3 .planning/verify_global_purity.py && python3 .planning/verify_feed_conservation.py && python3 .planning/verify_feed_paths.py && echo "ALL 3 SCRIPTS PASS"</automated>
  </verify>
  <done>All 3 scripts exit 0. Global BRAIN-FEED.md has < 20 bullet entries. verify_global_purity.py produces no output (silent pass). Stack table, Brain Agent Architecture, Delta-Velocity, and Implemented Features table all still present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — global feed coherence and entry count</name>
  <files>none — blocking human-verify checkpoint</files>
  <action>Follow the how-to-verify steps: open BRAIN-FEED.md, confirm preserved sections, confirm removed sections, count bullet entries, run full verification suite.</action>
  <verify><automated>python3 .planning/verify_feed_conservation.py && python3 .planning/verify_feed_paths.py && python3 .planning/verify_global_purity.py && echo "PHASE 10 COMPLETE"</automated></verify>
  <done>Human approved: global feed reads coherently, entry count < 20, all 3 scripts green. Phase 10 complete.</done>
  <what-built>
Global BRAIN-FEED.md has been cleaned up. All 3 verification scripts pass (conservation, paths, purity). The two-level architecture is complete: 1 global feed + 7 domain feeds.
  </what-built>
  <how-to-verify>
1. Open `.planning/BRAIN-FEED.md` and read the full file

2. Confirm these sections ARE present:
   - `## Stack (Locked)` table with 12 rows
   - `### Brain Agent Architecture` with 6+ bullets (the Phase 10 self-referential one was deleted)
   - `### Delta-Velocity Measurement` with 5 bullets
   - `## Implemented Features` table
   - Active Constraints (should have 4-5 bullets: No npm/pip, Brain #7 dispatch order, No notebook IDs, Structured output)
   - Phase 09 learnings (should have 4 bullets: Brain Bundle invariant, CORRECTED ASSUMPTIONS, Output Format, CROSS-DOMAIN REALITY)

3. Confirm these sections are GONE:
   - State Management subsection
   - React Flow subsection
   - Auth & Security subsection
   - API subsection
   - Phase 05 and Phase 06 learnings

4. Count bullet entries: `python3 -c "lines = open('.planning/BRAIN-FEED.md').read().splitlines(); bullets = [l for l in lines if l.startswith('- ')]; print(f'Global entries: {len(bullets)}')"`
   - Target: < 20 entries. If > 20, there is still domain fat.

5. Spot-check BRAIN-FEED-04-frontend.md — confirm it has the State Management and React Flow entries.

6. Run the full verification suite one final time:
   `python3 .planning/verify_feed_conservation.py && python3 .planning/verify_feed_paths.py && python3 .planning/verify_global_purity.py && echo "PHASE 10 COMPLETE"`
  </how-to-verify>
  <resume-signal>Type "approved" if global feed reads coherently without domain specifics and entry count is < 20. Or describe issues if adjustments needed.</resume-signal>
</task>

</tasks>

<verification>
Final phase verification (all must pass):

```bash
python3 .planning/verify_feed_conservation.py
python3 .planning/verify_feed_paths.py
python3 .planning/verify_global_purity.py
```

All 3 scripts must exit 0.

Manual count:
```bash
python3 -c "
lines = open('.planning/BRAIN-FEED.md').read().splitlines()
bullets = [l for l in lines if l.startswith('- ')]
print(f'Global feed bullets: {len(bullets)} (target: < 20)')
"
```

File count (all 8 feeds exist):
```bash
ls .planning/BRAIN-FEED*.md
```
Should list: BRAIN-FEED.md, BRAIN-FEED-01-product.md through BRAIN-FEED-07-growth.md (8 files total).
</verification>

<success_criteria>
1. verify_global_purity.py exits 0 with no output (silent pass)
2. verify_feed_conservation.py exits 0 — conservation law holds with KNOWN_DELETIONS=2
3. verify_feed_paths.py exits 0 — all 7 agent-referenced domain feed paths exist
4. Global BRAIN-FEED.md has < 20 bullet entries
5. Stack table, Brain Agent Architecture, Delta-Velocity Measurement, Implemented Features table all remain in global feed
6. Human approved the cleanup coherence
7. All 8 feed files exist: BRAIN-FEED.md + BRAIN-FEED-01-product.md through BRAIN-FEED-07-growth.md
</success_criteria>

<output>
After completion, create `.planning/phases/10-brain-feed-split/10-03-SUMMARY.md`
</output>
