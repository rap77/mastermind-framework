---
phase: post-v2.0.0-bug-fixes
task: 3
total_tasks: 5
status: in_progress
last_updated: "2026-03-17T03:31:10Z"
---

<current_state>
Post-v2.0.0 release bug fixing session - DAY 4.

Current branch: master
Latest commit: 44a879d "fix(tests): resolve event loop contamination and multi-user test failures"
Working tree: Changes staged but NOT committed (playwright marker fix + ci.yml + pyproject.toml)

SESSION SUMMARY 2026-03-17:
- Implemented professional Playwright/asyncio separation (3 files changed)
- Full suite ran: 422 passed, 48 failed, 5 skipped
- Verified: all 48 failures are pre-existing, nothing broken by our changes
- Decision reached: need to fix all 48 failures (not just categorize them)
- Paused before fixing to decide approach
</current_state>

<completed_work>

## Session 2026-03-17 - Playwright/asyncio Separation

### Changes Made (NOT YET COMMITTED)

| File | Change |
|------|--------|
| `pyproject.toml` | Added `--ignore=tests/e2e` to addopts |
| `tests/e2e/test_auth_smoke.py` | `pytestmark = pytest.mark.e2e` (was `skip`) |
| `tests/e2e/test_dag_smoke.py` | `pytestmark = pytest.mark.e2e` (was `skip`) |
| `tests/e2e/test_dashboard_smoke.py` | `pytestmark = pytest.mark.e2e` (was `skip`) |
| `.github/workflows/ci.yml` | Level 3 no longer runs e2e; added Level 4 E2E job (manual trigger) |

### Test Results After Changes
- `pytest` (full suite, excluding e2e): **422 passed, 48 failed, 5 skipped**
- No regressions introduced by our changes

### Prior Sessions (committed)
- Session 2026-03-15: 6 initial fixes (commit 49ce714)
- Session 2026-03-16 morning: 4 integration test fixes (commit a8d95ec)
- Session 2026-03-16 afternoon: ROOT CAUSE (playwright contamination) + fix (commit 44a879d)
- Session 2026-03-16 afternoon: 343 passed, 10 skipped CONFIRMED after playwright fix

</completed_work>

<remaining_work>

## 48 Remaining Failures

### Category 1: tests/api/ — Test Stubs (38 failures)
**Nature:** `raise AssertionError("Test stub: X not implemented")`
**Code exists:** ✅ `mastermind_cli/api/app.py`, `mastermind_cli/api/routes/`, `mastermind_cli/api/websocket.py`
**Files:** test_app.py, test_audit.py, test_auth.py, test_executions.py, test_sessions.py, test_websocket.py
**Action needed:** Implement actual tests using the existing production code

### Category 2: tests/perf/ — Performance Stubs (8 failures)
**Nature:** Same stub pattern
**Files:** test_db_queries.py, test_websocket_perf.py
**Action needed:** Implement perf tests against real code

### Category 3: Real Failures (2)
1. `tests/integration/test_pure_function_arch.py::TestAuthValidation::test_web_auth_with_database`
   - Not investigated yet this session
2. `tests/test_orchestrator/test_discovery_flow.py::TestDiscoveryFlow::test_parse_follow_up_basic`
   - Not investigated yet this session

### Open Decision
Three options discussed with user:
- **Opción A:** Implement all 46 stubs + 2 real failures (big work)
- **Opción B:** Skip stubs with `pytest.mark.skip` + fix 2 real failures (fast)
- **Opción C:** Mark stubs as `xfail` + implement gradually
- **User had not decided yet when we paused**

</remaining_work>

<decisions_made>

- **pytestmark = pytest.mark.e2e** instead of `pytest.mark.skip` — declarative, not a patch
- **--ignore=tests/e2e in addopts** — explicit config, not runtime magic
- **Level 4 CI job** with `workflow_dispatch` only — e2e needs running server, manual trigger is correct
- **No conftest.py magic** — rejected in favor of explicit separation
- **All 48 failures need fixing** — user explicitly said stop categorizing, just fix them

</decisions_made>

<blockers>

**Uncommitted changes:** The playwright fix (3 files) + ci.yml + pyproject.toml need to be committed before pushing.

**Decision pending:** How to handle 46 stub tests (implement vs skip vs xfail) — user hadn't decided before pausing.

</blockers>

<context>
The session started with a discussion about the Playwright event loop contamination issue (educational). User then challenged whether the solution proposed was professional — we refined it from conftest magic to explicit config separation. After validating the changes work (422 passed), the user pointed out correctly that "pre-existing" is not a useful category when you wrote all the code — just fix it. We were about to decide the approach for fixing the 48 remaining failures when the user chose to pause.

The playwright separation is solid and correct. The 48 remaining failures are the real next task.
</context>

<next_action>

**When resuming:**

1. **Commit the playwright separation first:**
```bash
git add pyproject.toml tests/e2e/test_auth_smoke.py tests/e2e/test_dag_smoke.py tests/e2e/test_dashboard_smoke.py .github/workflows/ci.yml
git commit -m "fix(tests): separate playwright e2e from async test suite"
```

2. **Decide approach for 48 remaining failures:**
   - Option A: Implement all stubs (big work, complete coverage)
   - Option B: Skip stubs + fix 2 real failures (fast, clean suite)
   - Option C: xfail stubs + implement gradually

3. **Investigate 2 real failures:**
```bash
uv run pytest tests/integration/test_pure_function_arch.py::TestAuthValidation::test_web_auth_with_database tests/test_orchestrator/test_discovery_flow.py::TestDiscoveryFlow::test_parse_follow_up_basic --no-cov --tb=long
```

4. **Push 9 commits to GitHub** (8 prior + 1 new playwright fix)

</next_action>
