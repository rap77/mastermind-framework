---
phase: 12-parallel-dispatch-command-update
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/smoke/verify_feed_isolation.sh
  - tests/brain_agents/test_parallel_dispatch.py
  - tests/brain_agents/test_sync_injection.py
autonomous: true
requirements:
  - DISP-01

must_haves:
  truths:
    - "verify_feed_isolation.sh acepta --check barrier-order y --check crosstalk sin romper su interface existente"
    - "test_parallel_dispatch.py existe con stubs que fallan hasta que el dispatch paralelo sea implementado"
    - "test_sync_injection.py existe con stubs que verifican aislamiento de fragmentos SYNC por agente"
    - "El check estático de eliminación MCP está disponible como comando ejecutable"
  artifacts:
    - path: "tests/smoke/verify_feed_isolation.sh"
      provides: "Sentinel script extendido con barrier-order + crosstalk checks"
      contains: "--check barrier-order"
    - path: "tests/brain_agents/test_parallel_dispatch.py"
      provides: "Test stubs para verificación de dispatch paralelo (DISP-01)"
      exports: ["test_barrier_order", "test_brain7_fires_after_domain_agents"]
    - path: "tests/brain_agents/test_sync_injection.py"
      provides: "Test stubs para verificación de aislamiento SYNC (DISP-02)"
      exports: ["test_sync_crosstalk_isolation", "test_sync_characterization"]
  key_links:
    - from: "tests/smoke/verify_feed_isolation.sh"
      to: "tests/brain_agents/test_parallel_dispatch.py"
      via: "complementan la misma Suite de verificación Phase 12"
      pattern: "--check barrier-order"
    - from: "tests/brain_agents/test_sync_injection.py"
      to: ".planning/BRAIN-FEED-04-frontend.md"
      via: "SYNC characterization test usa Brain #4 feed como fixture"
      pattern: "BRAIN-FEED-04"
---

<objective>
Crear el andamiaje de verificación Wave 0 para Phase 12: extender el sentinel script existente con 2 nuevos checks y crear los test stubs de pytest que FALLARAN hasta que el dispatch paralelo esté implementado.

Purpose: Todos los planes Wave 1+ necesitan comandos de verify concretos. Sin Wave 0, los verify blocks son "MISSING". Con Wave 0, cada tarea posterior tiene un comando exacto que corre en < 45s.

Output:
- verify_feed_isolation.sh extendido (no recreado) con flags --check barrier-order y --check crosstalk
- tests/brain_agents/test_parallel_dispatch.py con stubs RED
- tests/brain_agents/test_sync_injection.py con stubs RED
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-parallel-dispatch-command-update/12-CONTEXT.md
@.planning/phases/12-parallel-dispatch-command-update/12-VALIDATION.md

<interfaces>
<!-- Sentinel script existente — interface actual que NO debe romperse -->
<!-- Leer antes de modificar: tests/smoke/verify_feed_isolation.sh -->

Exit codes actuales (preservar):
  0 = PASS  — only expected feed modified (or no feed modified)
  1 = FAIL  — unexpected files modified
  2 = CRITICAL FAIL — global BRAIN-FEED.md modified
  3 = DIRTY WORKTREE — working tree was not clean

Uso actual:
  ./verify_feed_isolation.sh <brain-id> <expected-feed-file>
  ./verify_feed_isolation.sh brain-04-frontend BRAIN-FEED-04-frontend.md

Tests existentes en apps/api/:
  cd apps/api && uv run pytest tests/brain_agents/ -x -q
  (directorio tests/brain_agents/ ya existe con archivos de Phase 11)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender verify_feed_isolation.sh con barrier-order y crosstalk checks</name>
  <files>tests/smoke/verify_feed_isolation.sh</files>
  <action>
Extender el script existente SIN tocar ninguna de las líneas actuales (Steps 1-7 intactos). Agregar al final del script soporte para un flag opcional `--check` que activa verificaciones adicionales.

Estructura a agregar DESPUÉS del bloque PASS (Step 7):

```bash
# ──────────────────────────────────────────────
# Optional checks — activated via --check flag
# Usage: ./verify_feed_isolation.sh brain-04-frontend BRAIN-FEED-04-frontend.md --check barrier-order
#        ./verify_feed_isolation.sh brain-04-frontend BRAIN-FEED-04-frontend.md --check crosstalk
# ──────────────────────────────────────────────
CHECK_MODE="${3:-}"

if [[ "$CHECK_MODE" == "--check" ]]; then
  CHECK_TYPE="${4:-}"

  case "$CHECK_TYPE" in
    barrier-order)
      # Barrier order check — Phase 12 specific
      # Verifica que brain-07-growth NO fue el primer agente en escribir output
      # Observable: si un output file existe para brain-07 pero no para dominio previo = FAIL
      # In Phase 12: this check is MANUAL (observational via Claude Code UI)
      # This flag documents the check requirement and exits 0 (human must confirm)
      echo "CHECK barrier-order: Manual verification required."
      echo "In Claude Code UI: confirm Brain #7 dispatch prompt appears AFTER all 6 domain agent responses."
      echo "Observable: Multiple simultaneous 'thinking' indicators, then Brain #7 fires last."
      echo "PASS (manual confirmation required — script cannot automate UI observation)"
      exit 0
      ;;

    crosstalk)
      # Cross-talk isolation check — Phase 12 specific
      # Verifica que cada agente solo recibió sus propios fragmentos SYNC
      # Test estático: brain-04 solo tiene SYNC tags apuntando a BF-05, nunca a BF-01/02/03/06/07
      echo "CHECK crosstalk: Verifying SYNC tag isolation..."
      FEED_04=".planning/BRAIN-FEED-04-frontend.md"
      if [[ ! -f "$FEED_04" ]]; then
        echo "FAIL: BRAIN-FEED-04-frontend.md not found at .planning/"
        exit 1
      fi
      # Brain #4 feed should ONLY have SYNC tags pointing to BF-05 (backend)
      # Any SYNC tag pointing to other feeds = cross-talk violation
      CROSSTALK=$(grep '\[SYNC:' "$FEED_04" | grep -v 'BF-05' || true)
      if [[ -n "$CROSSTALK" ]]; then
        echo "FAIL: Brain #4 feed has SYNC tags pointing outside BF-05 (cross-talk risk):"
        echo "$CROSSTALK"
        exit 1
      fi
      echo "PASS: Brain #4 SYNC tags all point to BF-05 (backend) only. No cross-talk detected."
      exit 0
      ;;

    mcp-elimination)
      # Static check: confirm no MCP steps remain in command/skill files
      echo "CHECK mcp-elimination: Scanning for residual mcp__notebooklm-mcp__ steps..."
      VIOLATIONS=$(grep -r "mcp__notebooklm-mcp__notebook_query" .claude/commands/mm/ .claude/skills/mm/ 2>/dev/null || true)
      if [[ -n "$VIOLATIONS" ]]; then
        echo "FAIL: Found residual MCP steps (DISP-02 violation):"
        echo "$VIOLATIONS"
        exit 1
      fi
      echo "PASS: No mcp__notebooklm-mcp__notebook_query found in command/skill files."
      exit 0
      ;;

    *)
      echo "Unknown check type: $CHECK_TYPE"
      echo "Valid options: barrier-order, crosstalk, mcp-elimination"
      exit 1
      ;;
  esac
fi
```

Consideraciones críticas:
- El flag `--check` es el tercer argumento posicional. La firma existente es `$1=brain-id $2=expected-feed`. Agregar `$3=--check $4=check-type`.
- El script existente tiene `set -euo pipefail`. Si `grep` no encuentra nada retorna exit 1 bajo `-e`. Usar `|| true` en los grep opcionales para no triggear el pipefail.
- El crosstalk check es estático (lee el feed en disco) — no necesita dispatch real.
- El barrier-order check es manual (no puede automatizarse desde bash). El script documenta qué observar y retorna 0 (para no bloquear pipelines automatizados).
  </action>
  <verify>
    <automated>bash tests/smoke/verify_feed_isolation.sh brain-04-frontend BRAIN-FEED-04-frontend.md --check crosstalk 2>&1 | grep -E "PASS|FAIL"</automated>
  </verify>
  <done>Script acepta los 3 nuevos flags sin errores. El check crosstalk retorna PASS en el estado actual (Brain #4 solo tiene SYNC tags BF-05). El check mcp-elimination puede correr sin errores.</done>
</task>

<task type="auto">
  <name>Task 2: Crear test stubs RED para dispatch paralelo y SYNC injection</name>
  <files>tests/brain_agents/test_parallel_dispatch.py, tests/brain_agents/test_sync_injection.py</files>
  <action>
Crear dos archivos de tests en el directorio `apps/api/tests/brain_agents/` (donde viven los tests de Phase 11). Los stubs deben FALLAR con `pytest.fail("Not yet implemented")` — son RED intencionalmente hasta que los plans Wave 1+ implementen la lógica.

**tests/brain_agents/test_parallel_dispatch.py:**
```python
"""
Phase 12 — Parallel Dispatch verification stubs (DISP-01)

These tests are RED by design until moment-2.md parallel dispatch is implemented.
They document the behavioral requirements for parallel Agent dispatch.
"""
import pytest


class TestParallelDispatch:
    """DISP-01: All 7 domain brains dispatched in one orchestrator message."""

    def test_barrier_order_brain7_fires_after_domain_agents(self):
        """
        Brain #7 must be dispatched AFTER all 6 domain agents return outputs.
        Observable: Brain #7's dispatch prompt contains domain agent return values.

        This test is a documentation stub — parallel dispatch barrier is behavioral
        (observable in Claude Code UI), not unit-testable in isolation.
        Manual acceptance: Claude Code UI shows Brain #7 fires after 6 simultaneous
        domain agent "thinking" indicators complete.
        """
        pytest.fail(
            "STUB — Not yet implemented. "
            "Implement when moment-2.md parallel dispatch is written (Plan 12-02). "
            "Manual verification: Brain #7 receives 6 domain outputs as context, "
            "fires in a separate orchestrator message after domain wave completes."
        )

    def test_total_time_approximates_max_not_sum(self):
        """
        T1 total ≈ Max(T_brain_1..6) + T_brain_7, NOT Sum(T_brain_1..6) + T_brain_7.
        If total time equals sum, dispatch is sequential (FAIL).

        Manual acceptance criterion from Brain #6 QA:
        T1 target < 120s for Delta-Velocity Rating 4.
        """
        pytest.fail(
            "STUB — Not yet implemented. "
            "Manual timing verification required after moment-2.md is written. "
            "Time mm:brain-context Momento 2 end-to-end. Target: < 120s."
        )

    def test_global_brain_feed_unchanged_after_parallel_dispatch(self):
        """
        BRAIN-FEED.md (global) must be identical to pre-dispatch state.
        No domain agent may write to global feed — it is READ-ONLY for agents.
        verify_feed_isolation.sh already covers this (Phase 11 sentinel).
        """
        import subprocess
        result = subprocess.run(
            ["bash", "tests/smoke/verify_feed_isolation.sh", "--check", "crosstalk"],
            capture_output=True, text=True, cwd="."
        )
        # This is a static structural check, can run without real dispatch
        # If verify_feed_isolation.sh returns 0 on crosstalk check = PASS
        assert result.returncode == 0, f"Crosstalk check failed:\n{result.stdout}\n{result.stderr}"
```

**tests/brain_agents/test_sync_injection.py:**
```python
"""
Phase 12 — SYNC tag resolution verification stubs (DISP-01, DISP-02)

These tests verify SYNC fragment isolation — each brain only receives its own SYNC content.
"""
import pytest
import re
from pathlib import Path


SYNC_PATTERN = re.compile(r'\[SYNC:\s+BF-(\d{2})-(\w+)\]')
FEEDS_DIR = Path(".planning")


class TestSyncInjection:
    """DISP-02: SYNC tags resolved inline, no cross-talk between agent prompts."""

    def test_brain04_sync_tags_point_only_to_brain05(self):
        """
        Brain #4 Frontend feed has 4 SYNC tags.
        All must point to BF-05 (Backend). Zero cross-domain leakage.
        This is a static check — reads the feed file directly.
        """
        feed_04 = FEEDS_DIR / "BRAIN-FEED-04-frontend.md"
        assert feed_04.exists(), f"BRAIN-FEED-04-frontend.md not found at {feed_04}"

        content = feed_04.read_text()
        sync_tags = SYNC_PATTERN.findall(content)

        assert len(sync_tags) > 0, "BRAIN-FEED-04-frontend.md has no SYNC tags — expected 4 from Phase 10"

        for owner_id, section_id in sync_tags:
            assert owner_id == "05", (
                f"Brain #4 has SYNC tag pointing to BF-{owner_id}-{section_id}, "
                f"expected only BF-05 (Backend). Cross-talk detected."
            )

    def test_sync_characterization_brain04_cites_injected_bf05_fragment(self):
        """
        SYNC characterization test (from Brain #6 QA spec):
        WITHOUT SYNC injection: Brain #4 returns "No impact" for BF-05-WS-AUTH question.
        WITH SYNC injection (correct): Brain #4 cites the BF-05 fragment explicitly.

        This test documents the requirement — real execution requires moment-2.md to be written.
        Manual acceptance: temporarily modify BF-05-WS-AUTH, invoke Brain #4,
        verify citation of the injected fragment appears in response.
        """
        pytest.fail(
            "STUB — Not yet implemented. "
            "Manual characterization test after moment-2.md implements SYNC injection. "
            "Steps: (1) Modify .planning/BRAIN-FEED-05-backend.md BF-05-WS-AUTH section. "
            "(2) Invoke Brain #4 with a WS auth question. "
            "(3) Verify Brain #4 cites the BF-05 fragment explicitly. "
            "No citation = SYNC injection failed."
        )

    def test_no_sync_tags_in_global_feed(self):
        """
        Global BRAIN-FEED.md must have zero SYNC tags.
        SYNC tags are a domain-feed-only construct.
        """
        global_feed = FEEDS_DIR / "BRAIN-FEED.md"
        assert global_feed.exists(), "BRAIN-FEED.md not found"

        content = global_feed.read_text()
        sync_tags = SYNC_PATTERN.findall(content)

        assert len(sync_tags) == 0, (
            f"Global BRAIN-FEED.md contains SYNC tags (must be zero): {sync_tags}"
        )
```

Notas de implementación:
- El path de los tests es `apps/api/tests/brain_agents/` — confirmar con `ls apps/api/tests/brain_agents/` antes de escribir.
- Los tests usan paths relativos para los feeds (`.planning/`). Al correr con `cd apps/api && uv run pytest`, el cwd es `apps/api/`, pero los feeds están en el root del repo. Usar `Path("../../.planning")` o configurar un fixture que detecte el repo root.
- `test_brain04_sync_tags_point_only_to_brain05` y `test_no_sync_tags_in_global_feed` deben ser GREEN inmediatamente (checks estáticos). Los stubs con `pytest.fail()` son los que son RED.
- Usar `apps/api/` como cwd base para las rutas — ajustar los paths de `.planning/` con `Path(__file__).parent.parent.parent.parent / ".planning"` (subir 4 niveles: test file → brain_agents → tests → api → apps → root → .planning).
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/brain_agents/test_sync_injection.py::TestSyncInjection::test_brain04_sync_tags_point_only_to_brain05 tests/brain_agents/test_sync_injection.py::TestSyncInjection::test_no_sync_tags_in_global_feed -v 2>&1 | tail -10</automated>
  </verify>
  <done>Los dos tests estáticos (test_brain04_sync_tags_point_only_to_brain05 + test_no_sync_tags_in_global_feed) son GREEN. Los stubs con pytest.fail() son RED marcados como XFAIL o simplemente FAILED con mensaje explicativo. cd apps/api && uv run pytest tests/brain_agents/ -x -q corre sin errores de importación.</done>
</task>

</tasks>

<verification>
bash tests/smoke/verify_feed_isolation.sh brain-04-frontend BRAIN-FEED-04-frontend.md --check crosstalk
bash tests/smoke/verify_feed_isolation.sh brain-01-product BRAIN-FEED-01-product.md --check mcp-elimination
cd apps/api && uv run pytest tests/brain_agents/test_sync_injection.py -v
cd apps/api && uv run pytest tests/brain_agents/ -x -q
</verification>

<success_criteria>
- verify_feed_isolation.sh acepta --check barrier-order, --check crosstalk, --check mcp-elimination sin romper la interface existente (exit codes 0-3 preservados)
- Los checks crosstalk y mcp-elimination son ejecutables en < 5 segundos (estáticos)
- test_brain04_sync_tags_point_only_to_brain05 GREEN
- test_no_sync_tags_in_global_feed GREEN
- Los stubs RED documentan exactamente qué se verificará cuando los planes Wave 1+ completen la implementación
</success_criteria>

<output>
After completion, create `.planning/phases/12-parallel-dispatch-command-update/12-01-SUMMARY.md`
</output>
