---
phase: 12-parallel-dispatch-command-update
plan: "03"
type: execute
wave: 2
depends_on:
  - "12-02"
files_modified:
  - .claude/skills/mm/brain-context/workflows/moment-3.md
  - .claude/commands/mm/ask-all.md
autonomous: true
requirements:
  - DISP-01
  - DISP-02

must_haves:
  truths:
    - "moment-3.md ya no contiene mcp__notebooklm-mcp__notebook_query — usa Agent dispatch para Brain #7"
    - "ask-all.md despacha los 6 cerebros dominio en paralelo con el mismo patrón Phase A/B/C de moment-2.md"
    - "ask-all.md incluye la restricción anti-mediocre en el prompt de Brain #7"
    - "El sentinel retorna PASS en el check mcp-elimination para .claude/skills/mm/ y .claude/commands/mm/"
  artifacts:
    - path: ".claude/skills/mm/brain-context/workflows/moment-3.md"
      provides: "Brain #7 pre-execution validation workflow — Agent dispatch reemplaza MCP"
      contains: "brain-07-growth"
    - path: ".claude/commands/mm/ask-all.md"
      provides: "Full parallel dispatch command — convenience alias para el flujo completo"
      contains: "Phase B"
  key_links:
    - from: ".claude/commands/mm/ask-all.md"
      to: ".claude/skills/mm/brain-context/workflows/moment-2.md"
      via: "ask-all.md implementa el mismo patrón Phase A/B/C definido en moment-2.md"
      pattern: "Phase A"
    - from: ".claude/skills/mm/brain-context/workflows/moment-3.md"
      to: ".claude/agents/mm/brain-07-growth/brain-07-growth.md"
      via: "moment-3.md despacha brain-07-growth como Agent, ya no como MCP query"
      pattern: "brain-07-growth"
---

<objective>
Completar la migración MCP → Agent dispatch para moment-3.md (Brain #7 pre-execution validation) y ask-all.md (convenience command para full parallel dispatch).

Purpose: moment-3.md aún tiene `mcp__notebooklm-mcp__notebook_query` en Step 3. ask-all.md aún despacha secuencialmente via MCP. Estos son los últimos archivos de alto nivel que necesitan la migración después de moment-2.md.

Output:
- moment-3.md: Step 3 reemplazado con dispatch de brain-07-growth via Agent tool
- ask-all.md: despacha 6+1 cerebros con pattern Phase A/B/C, sin MCP steps
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-parallel-dispatch-command-update/12-CONTEXT.md
@.planning/phases/12-parallel-dispatch-command-update/12-02-SUMMARY.md

<interfaces>
<!-- moment-2.md reescrito en Plan 02 — usar como referencia de patrón Phase A/B/C -->
<!-- Leer .claude/skills/mm/brain-context/workflows/moment-2.md para el patrón exacto -->

<!-- ask-all.md actual (contenido a reemplazar): -->
<!-- Tiene "Query all 7 brains sequentially via MCP" en Step 3 -->
<!-- Debe convertirse en: Phase A (SYNC) + Phase B (parallel 6) + Phase C (Brain #7) -->

<!-- moment-3.md actual (contenido a reemplazar): -->
<!-- Step 3 tiene: mcp__notebooklm-mcp__notebook_query(notebook_id="d8de74d6-...", query="...") -->
<!-- Debe convertirse en: dispatch brain-07-growth via Task() con el mismo contexto que actualmente se pasa al MCP -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar moment-3.md — reemplazar MCP query con Agent dispatch</name>
  <files>.claude/skills/mm/brain-context/workflows/moment-3.md</files>
  <action>
Leer moment-3.md completo. El único cambio requerido es en Step 3 ("Query Brain #7").

Step 3 actual contiene:
```python
mcp__notebooklm-mcp__notebook_query(
    notebook_id="d8de74d6-7028-44ed-b4d5-784d6a9256e6",
    query="[Full context block from Step 2]"
)
```

Reemplazar con:

```markdown
## Step 3 — Dispatch brain-07-growth via Agent

Dispatch `brain-07-growth` using the Task tool (Agent dispatch). Pass the full context block from Step 2 as the agent prompt.

```
Task(
    subagent_type="brain-07-growth",
    prompt="[Full context block from Step 2 — same content previously passed to MCP]"
)
```

The agent reads its own feeds, applies the intermediary protocol, and returns the structured 5-section output (Domain Summary + Second-Order Effects + Systemic Metric + Cascade Risk + Verdict).

Note: Brain #7 already has the dispatch constraint in its system prompt — it will request domain brain outputs if they are not provided. For Moment 3 (plan validation), the PLAN SUMMARY in the context block serves as the "domain outputs" Brain #7 evaluates. This is correct — Moment 3 evaluates plans, not live agent outputs.
```

Todo lo demás en moment-3.md (Steps 1, 2, 4-7, escalation protocol, iteration loop table) se conserva sin cambios. Solo Step 3 cambia.

Eliminar el notebook_id hardcodeado ("d8de74d6-7028-44ed-b4d5-784d6a9256e6") — los agentes ahora acceden a NotebookLM directamente via sus herramientas configuradas en el system prompt.
  </action>
  <verify>
    <automated>grep -c "mcp__notebooklm-mcp__notebook_query" .claude/skills/mm/brain-context/workflows/moment-3.md</automated>
  </verify>
  <done>grep retorna 0 — ninguna instancia de "mcp__notebooklm-mcp__notebook_query" en moment-3.md. El Step 3 contiene "Task(" y "brain-07-growth".</done>
</task>

<task type="auto">
  <name>Task 2: Reescribir ask-all.md con full parallel dispatch Pattern</name>
  <files>.claude/commands/mm/ask-all.md</files>
  <action>
Reemplazar completamente ask-all.md. El archivo actual tiene 31 líneas con dispatch secuencial MCP. El nuevo archivo implementa el mismo patrón Phase A/B/C de moment-2.md, optimizado para una pregunta directa (sin el setup de plan context).

Nuevo contenido de ask-all.md:

```markdown
---
description: Consult ALL 7 MasterMind brains simultaneously via parallel Agent dispatch
argument-hint: [your question requiring comprehensive multi-domain analysis]
---

<objective>
Query all 6 domain brain agents simultaneously (parallel dispatch) and Brain #7 as evaluator after.
Returns: 6 domain expert perspectives + Brain #7 synthesis with Delta-Velocity score.

Architecture: Phase A (SYNC resolution) → Phase B (6 domain brains, 1 message) → Phase C (Brain #7 after all return).
Expected T1: ~90-110s vs previous ~210-270s sequential MCP.
</objective>

<context>
Project reality: @ .planning/BRAIN-FEED.md
Domain feeds: @ .planning/BRAIN-FEED-01-product.md @ .planning/BRAIN-FEED-02-ux.md @ .planning/BRAIN-FEED-03-ui.md @ .planning/BRAIN-FEED-04-frontend.md @ .planning/BRAIN-FEED-05-backend.md @ .planning/BRAIN-FEED-06-qa.md
</context>

<process>

## Phase A — SYNC Tag Resolution (Pre-Dispatch)

Before dispatching, resolve inline SYNC tags for agents that have them.

Scan each domain feed for `[SYNC: BF-NN-ID]` tags. For each tag:
1. Open the owner feed (`.planning/BRAIN-FEED-{NN}-{domain}.md`)
2. Extract only the referenced section (search for the section ID)
3. Store as inline injection: `"INJECTED FROM BRAIN-FEED-{NN}: [text]"`
4. If section not found: log warning, continue without injection

Cross-talk rule: Brain #4's prompt gets BF-05 fragments ONLY. No other agent receives SYNC injections from other agents' feeds. Build a per-brain injection map.

## Phase B — Parallel Domain Dispatch (Single Orchestrator Message)

Dispatch these 6 domain brains SIMULTANEOUSLY in a SINGLE orchestrator message:

- `brain-01-product` — Include: global feed + domain feed + WHAT I NEED (product strategy perspective for: $ARGUMENTS)
- `brain-02-ux` — Include: global feed + domain feed + WHAT I NEED (UX/research perspective for: $ARGUMENTS)
- `brain-03-ui` — Include: global feed + domain feed + WHAT I NEED (UI/design perspective for: $ARGUMENTS)
- `brain-04-frontend` — Include: global feed + domain feed + [INJECTED BF-05 fragments if any] + WHAT I NEED (frontend architecture perspective for: $ARGUMENTS)
- `brain-05-backend` — Include: global feed + domain feed + WHAT I NEED (backend/API perspective for: $ARGUMENTS)
- `brain-06-qa` — Include: global feed + domain feed + WHAT I NEED (QA/reliability perspective for: $ARGUMENTS)

All 6 in one message. Wait for all to return.

## Phase C — Brain #7 Evaluation (After Phase B Completes)

Only after ALL 6 domain agents return: dispatch `brain-07-growth` with:

```
[DOMAIN AGENT OUTPUTS — paste all 6 returns]

[ANTI-MEDIOCRE CONSTRAINT]
Do NOT reconcile contradictions. Name the conflict. Pick the strongest expert position.

[WHAT I NEED]
Evaluate these 6 domain outputs. Return:
1. Global Rating (0-100) — health of this dispatch
2. Brain Alerts — Rating 1 or 2 violations
3. Consolidated View — synthesis naming all conflicts and picking winners
4. Delta-Velocity vs Phase 09 baselines (target: 3.5-4.5)
5. Human-review flags — cross-domain patterns for global feed (flag only, do NOT write)
```

</process>

<success_criteria>
- 6 domain brains returned expert perspectives simultaneously
- Brain #7 received all 6 outputs and returned synthesis with verdict
- Global BRAIN-FEED.md unchanged (READ-ONLY — agents do not write it)
- All recommendations grounded in codebase reality (not generic advice)
</success_criteria>
```

Verificar que el archivo no contenga "mcp__notebooklm-mcp__notebook_query" ni "sequentially via MCP".
  </action>
  <verify>
    <automated>bash tests/smoke/verify_feed_isolation.sh brain-01-product BRAIN-FEED-01-product.md --check mcp-elimination 2>&1 | grep -E "PASS|FAIL"</automated>
  </verify>
  <done>El sentinel mcp-elimination retorna PASS: cero instancias de "mcp__notebooklm-mcp__notebook_query" en .claude/commands/mm/ y .claude/skills/mm/. ask-all.md contiene "Phase B" y "brain-07-growth". moment-3.md no tiene MCP steps.</done>
</task>

</tasks>

<verification>
grep -rc "mcp__notebooklm-mcp__notebook_query" .claude/skills/mm/ .claude/commands/mm/ask-all.md
bash tests/smoke/verify_feed_isolation.sh brain-01-product BRAIN-FEED-01-product.md --check mcp-elimination
grep -c "Phase B" .claude/commands/mm/ask-all.md
grep -c "brain-07-growth" .claude/skills/mm/brain-context/workflows/moment-3.md
cd apps/api && uv run pytest tests/brain_agents/ -x -q
</verification>

<success_criteria>
- Zero instancias de "mcp__notebooklm-mcp__notebook_query" en moment-3.md y ask-all.md
- ask-all.md implementa Phase A/B/C — mismo patrón que moment-2.md
- moment-3.md despacha brain-07-growth via Task() en lugar de MCP
- Sentinel mcp-elimination PASS para .claude/skills/mm/ + .claude/commands/mm/ask-all.md
- Suite pytest sin regresiones nuevas
</success_criteria>

<output>
After completion, create `.planning/phases/12-parallel-dispatch-command-update/12-03-SUMMARY.md`
</output>
