---
phase: 09-baselines-agent-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/baselines/baseline-schema.md
  - tests/baselines/baseline-01-frontend-single.md
  - tests/baselines/baseline-02-backend-single.md
  - tests/baselines/baseline-03-qa-single.md
  - tests/baselines/baseline-04-multibrain-e2e.md
  - tests/baselines/baseline-05-multibrain-e2e.md
autonomous: true
requirements:
  - BASE-01
  - BASE-02

must_haves:
  truths:
    - "6 files exist in tests/baselines/ BEFORE any .claude/agents/mm/ file is created"
    - "baseline-schema.md contains all required fields: context_id, T1_setup_seconds, T2_ai_latency_seconds, T3_review_seconds, delta_velocity_score, characterization_diff, human_intervention_log"
    - "Each of 5 baselines has a Frozen Context Block, a populated cognitive_trace, and a delta_velocity_score 1-5"
    - "3 baselines are single-brain (Frontend #4, Backend #5, QA #6); 2 are multi-brain E2E (Brains #4+#5+#7)"
    - "2 baselines use retrospective tickets from Phase 07/08 history; 3 use adversarial ad-hoc tickets"
    - "Git commit for these 6 files precedes any commit to .claude/agents/mm/ — verifiable via git log"
  artifacts:
    - path: "tests/baselines/baseline-schema.md"
      provides: "Delta-Velocity Matrix + Cognitive Load Split schema spec"
      contains: "context_id"
    - path: "tests/baselines/baseline-01-frontend-single.md"
      provides: "Brain #4 Frontend single-brain retrospective baseline"
      contains: "delta_velocity_score"
    - path: "tests/baselines/baseline-02-backend-single.md"
      provides: "Brain #5 Backend single-brain adversarial baseline"
      contains: "delta_velocity_score"
    - path: "tests/baselines/baseline-03-qa-single.md"
      provides: "Brain #6 QA single-brain adversarial baseline"
      contains: "delta_velocity_score"
    - path: "tests/baselines/baseline-04-multibrain-e2e.md"
      provides: "Multi-brain E2E retrospective baseline (Brains #4+#5 → #7)"
      contains: "delta_velocity_score"
    - path: "tests/baselines/baseline-05-multibrain-e2e.md"
      provides: "Multi-brain E2E adversarial baseline (Brains #4+#5 → #7)"
      contains: "delta_velocity_score"
  key_links:
    - from: "tests/baselines/baseline-schema.md"
      to: "tests/baselines/baseline-01..05.md"
      via: "YAML frontmatter schema applied to all 5 records"
      pattern: "context_id|delta_velocity_score"
    - from: "tests/baselines/"
      to: ".planning/phases/09-baselines-agent-authoring/"
      via: "git timestamp — baselines committed first, agents later"
      pattern: "git log --diff-filter=A"
---

<objective>
Create the pre-migration measurement foundation for Phase 09. This plan produces 6 files in tests/baselines/ — the schema definition plus 5 structured consultation records — that MUST be committed before any .claude/agents/mm/ file exists. These files are the ground truth that will make the v2.2 vs manual skill comparison meaningful.

Purpose: Establish measurable baseline BEFORE migration. Without this, there is no way to know if agents improve on manual mm:brain-context skill workflows. The git timestamp ordering is not a formality — it IS the proof that measurement preceded migration.

Output: tests/baselines/baseline-schema.md + 5 baseline records (3 single-brain + 2 multi-brain E2E).
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-baselines-agent-authoring/09-CONTEXT.md
@.planning/phases/09-baselines-agent-authoring/09-RESEARCH.md

<interfaces>
<!-- Schema spec — from 09-CONTEXT.md Brain #6 consultation. Use EXACTLY these field names. -->

From 09-CONTEXT.md baseline-schema.md required fields:
```yaml
context_id: <git rev-parse HEAD>
brain_feed_snapshot:
  - .planning/BRAIN-FEED.md
  - .planning/STATE.md
  - <relevant code file>
input_prompt_raw: |
  <exact prompt/ticket text given to brain>
cognitive_trace:
  T1_setup_seconds: <int>        # context gathering time — profitability gate (T1 < 300s = profitable)
  T2_ai_latency_seconds: <int>   # model response wait
  T3_review_seconds: <int>       # human validation + filtering time
delta_velocity_score: <1-5>
characterization_diff: |
  Expected: ...
  Observed: ...
human_intervention_log:
  - gap: <description>
    correction: <what human had to add/change>
```

Delta-Velocity Matrix (from 09-CONTEXT.md):
- 1 Blocker: Hallucinates libraries, breaks TypeScript types, ignores anti-patterns
- 2 Junior: Works but generic — doesn't use existing Zustand stores or React Flow architecture
- 3 Peer: Correct, respects stack and context, integrates with NexusCanvas. PR-ready.
- 4 Senior: Detects optimization not in the ticket (e.g., critical useMemo or unasked WS error handling)
- 5 Architect: Proposes game-changing solution (e.g., Dynamic DAG optimization reducing DOM load 40%)

Frozen Context Block (required in EVERY baseline, from Brain #1):
```markdown
## Frozen Context Block

**Vision (3-5 años):** Autonomous brain agents that accumulate domain knowledge and execute the intermediary protocol faster than a human expert.
**Strategic Intent:** v2.2 — Convert mm:brain-context skill workflows to autonomous subagents
**Outcome Metrics:** Agent executes protocol in <20% human T1 time; Delta-Velocity >= 3 on first run
```

Brain selection reference:
| Brain | Domain | Notebook ID |
|-------|--------|-------------|
| 4 | Frontend | 85e47142-0a65-41d9-9848-49b8b5d2db33 |
| 5 | Backend | c6befbbc-b7dd-4ad0-a677-314750684208 |
| 6 | QA/DevOps | 74cd3a81-1350-4927-af14-c0c4fca41a8e |
| 7 | Growth/Data (Evaluator) | d8de74d6-7028-44ed-b4d5-784d6a9256e6 |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create baseline-schema.md (BASE-02 schema definition)</name>
  <files>tests/baselines/baseline-schema.md</files>
  <action>
Create tests/baselines/ directory and tests/baselines/baseline-schema.md.

The schema file defines the measurement contract used across ALL 5 baselines. It is the reference document for post-migration comparison. Write it in Markdown with YAML frontmatter showing the full schema structure.

Content structure:
1. Header: "# Baseline Schema — Delta-Velocity + Cognitive Load Split" with purpose statement (this is the measurement framework, not a planning doc — it lives in tests/ to signal it is part of the quality pipeline)
2. Full YAML frontmatter schema block with ALL required fields (copy exact field names from the interfaces section — do NOT rename them)
3. Delta-Velocity Matrix section: Levels 1-5 as a table (Blocker / Junior / Peer / Senior / Architect) with the full definitions from 09-CONTEXT.md
4. Cognitive Load Split section: T1/T2/T3 table with steps and what each measures
5. T1 Profitability Threshold section: T1 < 300 seconds (5 min) = agent is profitable; flag baselines where T1 > 300s as "agent-unprofitable" candidates for automation
6. Frozen Context Block template (static per phase — only ticket changes between baselines; product context is the control variable)
7. Ticket Mix spec: 2 retrospective (Phase 07/08 history) + 3 adversarial ad-hoc. Retrospective = precision test (known ground truth). Adversarial = resilience test (ground truth = adherence to system principles).
8. Post-migration usage note: "compare manual baselines (Phase 09) vs agent runs (Phase 11) using identical tickets"

Do NOT add commentary beyond what is specified. The schema is a contract, not a tutorial.
  </action>
  <verify>
    <automated>grep -c "context_id\|T1_setup_seconds\|delta_velocity_score\|characterization_diff\|human_intervention_log" /home/rpadron/proy/mastermind/tests/baselines/baseline-schema.md</automated>
  </verify>
  <done>baseline-schema.md exists with all 5 required field names present; file is >= 60 lines covering schema + matrix + cognitive split + profitability threshold</done>
</task>

<task type="auto">
  <name>Task 2: Create 5 baseline records (BASE-01)</name>
  <files>
    tests/baselines/baseline-01-frontend-single.md,
    tests/baselines/baseline-02-backend-single.md,
    tests/baselines/baseline-03-qa-single.md,
    tests/baselines/baseline-04-multibrain-e2e.md,
    tests/baselines/baseline-05-multibrain-e2e.md
  </files>
  <action>
Create all 5 baseline records. Each file is a structured Markdown document with YAML frontmatter (schema from Task 1) capturing a simulated manual mm:brain-context skill consultation.

These are SIMULATED baselines — we are documenting what a manual consultation WOULD look like. The YAML frontmatter captures the structure; the body captures the reasoning. Use realistic (not fabricated) values: T1/T2/T3 times that reflect the actual cognitive work of manually executing the 6-step intermediary protocol.

Context capture for context_id field: run `git rev-parse HEAD` and embed the result in every baseline frontmatter.

**baseline-01-frontend-single.md** — Brain #4 Frontend, retrospective ticket
- brain_id: 4 (Frontend)
- ticket_type: retrospective (Phase 07/08 history — WS event dispatch to brainStore)
- Ticket: "Phase 07 introduced NexusCanvas. Review whether our current RAF batching implementation in brainStore correctly prevents re-renders when 5 WS events arrive simultaneously during dagre layout computation."
- This is retrospective: we KNOW the answer (RAF batching in brainStore, not wsDispatcher, handles this correctly via the 16ms drain cycle). Use it to calibrate what Rating 3 looks like.
- Simulated cognitive_trace: T1 ~ 180-240s (reading BRAIN-FEED + brainStore.ts), T2 ~ 45s, T3 ~ 120-180s
- Simulated delta_velocity_score: 3 (Peer — correct answer, stack-aware, but no novel optimization detected)
- characterization_diff: "Expected: Brain would suggest looking at wsDispatcher / Observed: Brain correctly identified brainStore RAF queue as the right location"
- human_intervention_log: at least 1 entry (e.g., brain suggested useStore() hook, correction was to use useBrainState(id) targeted selector)

**baseline-02-backend-single.md** — Brain #5 Backend, adversarial ticket
- brain_id: 5 (Backend)
- ticket_type: adversarial
- Ticket: "Design the endpoint contract for streaming brain consultation results via Server-Sent Events (SSE). The endpoint must support mid-stream cancellation, return partial results on cancellation, and use asyncio.TaskGroup for concurrent brain queries. Specify exact Pydantic v2 models for request and response."
- Adversarial rationale: SSE + asyncio.TaskGroup + mid-stream cancellation is a real Phase 10+ challenge not yet solved. Ground truth = adherence to system principles (Pydantic v2 strict mode, no dict[str, Any], asyncio not Celery).
- Simulated cognitive_trace: T1 ~ 210-270s, T2 ~ 60s, T3 ~ 150-200s
- Simulated delta_velocity_score: 3 (correct Pydantic v2 usage, correct asyncio pattern, no novel optimization spontaneously offered)
- characterization_diff: reflect whether brain correctly avoided dict[str, Any] and suggested Celery (it should NOT)

**baseline-03-qa-single.md** — Brain #6 QA/DevOps, adversarial ticket
- brain_id: 6 (QA/DevOps)
- ticket_type: adversarial
- Ticket: "We're adding 7 new Claude Code subagents to .claude/agents/mm/. Define the integration test strategy for verifying that each agent: (1) reads both BRAIN-FEED files before querying, (2) writes ONLY to its own domain feed, (3) rejects Stack Hallucination suggestions. No e2e tests that require live NotebookLM calls — tests must be runnable offline."
- Adversarial rationale: Agent integration testing without live MCP calls is a genuinely unsolved problem for this project. Ground truth = brain must propose a testing approach that respects offline constraint and covers all 3 behaviors.
- Simulated cognitive_trace: T1 ~ 200-260s, T2 ~ 50s, T3 ~ 160-220s
- Simulated delta_velocity_score: 3 (correct strategy but requires human to specify mock patterns for MCP tools)
- human_intervention_log: reflect that brain suggested using pytest-mock for MCP but didn't specify the exact fixture structure

**baseline-04-multibrain-e2e.md** — Multi-brain E2E, retrospective ticket
- brain_ids: [4, 5, 7] (Frontend + Backend + Evaluator)
- ticket_type: retrospective (Phase 08 — Strategy Vault implementation)
- Ticket: "Phase 08 introduced Strategy Vault. Review the state management design: Zustand slice for vault items + TanStack Query for server state. Evaluate whether the separation of concerns between Zustand (UI state) and TanStack Query (server cache) is architecturally sound for this domain."
- Flow: Brain #4 evaluates Zustand slice design → Brain #5 evaluates server-side API contract → Brain #7 consolidates and identifies cross-domain gaps
- Key multi-brain success metric: "Did Brain #7 understand what Brain #4 said about the Zustand slice?" — information preservation test across the 6-step protocol
- Simulated cognitive_trace per brain: T1 ~ 180-240s each, cascade overhead ~ 60s between brains
- Simulated delta_velocity_score: 3 (correct architecture validation, no novel proposals)
- Include section "Multi-Brain Information Leak" documenting what Brain #7 received vs what Brain #4 actually said

**baseline-05-multibrain-e2e.md** — Multi-brain E2E, adversarial ticket
- brain_ids: [4, 5, 7] (Frontend + Backend + Evaluator)
- ticket_type: adversarial
- Ticket: "Implement partial hydration for the Niche Clusters view when the DAG exceeds 50 nodes. The current React Flow implementation renders all nodes eagerly. Propose a virtualization strategy that: (1) preserves dagre layout positions, (2) doesn't break the WS illumination updates, (3) keeps TypeScript strict mode. No off-the-shelf virtualization library unless it's already in pnpm-lock.yaml."
- Adversarial rationale: This is a genuine unsolved Phase 10+ performance problem. Ground truth = no suggestions of unlocked libraries, preserves dagre + WS, strict TypeScript.
- Flow: Brain #4 proposes virtualization → Brain #5 evaluates backend data contract needs → Brain #7 identifies second-order effects
- Simulated cognitive_trace per brain: T1 ~ 240-300s, T2 ~ 60-90s, T3 ~ 180-240s
- Simulated delta_velocity_score: 4 (Brain #4 should detect the IntersectionObserver approach using locked @xyflow/react v12 APIs — a Senior-level insight not in the ticket)

Format for ALL 5 files:
```markdown
---
[YAML frontmatter with all schema fields]
---

# Baseline NN — [Descriptive Title]

## Frozen Context Block

**Vision (3-5 años):** ...
**Strategic Intent:** ...
**Outcome Metrics:** ...

## Ticket

[ticket text verbatim]

## Brain(s) Consulted

[Brain NN — Domain name]

## Raw Brain Response (Summary)

[Summary of what NotebookLM returned — realistic, not fabricated. Use "SIMULATED:" prefix to mark this is a hypothetical response for baseline purposes]

## Filtered Insights

[What survived Step 5 grep verification]

## Gaps Found

[Real gaps that required human intervention]

## Multi-Brain Information Leak (multi-brain only)

[What Brain #7 received vs what Brain #4 originally said — for E2E baselines]
```
  </action>
  <verify>
    <automated>ls /home/rpadron/proy/mastermind/tests/baselines/baseline-0*.md | wc -l</automated>
  </verify>
  <done>5 baseline files exist in tests/baselines/; each has YAML frontmatter with delta_velocity_score and cognitive_trace fields; baseline-01/04 are retrospective, baseline-02/03/05 are adversarial; git log shows these 6 files committed before any .claude/agents/mm/ file</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Structural: schema has required fields
grep -c "context_id\|T1_setup_seconds\|delta_velocity_score\|characterization_diff\|human_intervention_log" /home/rpadron/proy/mastermind/tests/baselines/baseline-schema.md
# Expect: >= 5

# Structural: 5 baseline files exist
ls /home/rpadron/proy/mastermind/tests/baselines/baseline-0*.md | wc -l
# Expect: 5

# Structural: all baselines have T1 times
grep "T1_setup_seconds" /home/rpadron/proy/mastermind/tests/baselines/baseline-0*.md
# Expect: 5 matches with numeric values

# Structural: all baselines have delta_velocity_score
grep "delta_velocity_score" /home/rpadron/proy/mastermind/tests/baselines/baseline-0*.md
# Expect: 5 matches with 1-5 values

# Regression: backend suite stays green
cd /home/rpadron/proy/mastermind && uv run pytest apps/api/ -x -q

# Regression: frontend suite stays green
pnpm --prefix /home/rpadron/proy/mastermind/apps/web test run
```
</verification>

<success_criteria>
- tests/baselines/ directory exists with 6 files (schema + 5 baselines)
- baseline-schema.md contains all required fields: context_id, brain_feed_snapshot, input_prompt_raw, cognitive_trace (T1/T2/T3), delta_velocity_score, characterization_diff, human_intervention_log
- Baseline mix: 2 retrospective (01, 04) + 3 adversarial (02, 03, 05)
- Brain mix: 3 single-brain (01 = #4, 02 = #5, 03 = #6) + 2 multi-brain E2E (04, 05 = #4+#5+#7)
- Each baseline has Frozen Context Block with Vision / Strategic Intent / Outcome Metrics
- Git commit for these files precedes any .claude/agents/mm/ commit (verifiable via git log)
- Backend suite (uv run pytest apps/api/ -x -q) stays green — no regressions
- Frontend suite (pnpm test run) stays green — no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/09-baselines-agent-authoring/09-01-SUMMARY.md` documenting:
- Files created (list all 6 with one-line descriptions)
- Delta-Velocity scores assigned to each baseline
- T1 times for each baseline (flag any > 300s as agent-unprofitable candidates)
- git commit hash that contains these files (the timestamp anchor for Phase 09 SUCCESS CRITERIA #1)
- Any decisions made during authoring that deviate from the spec
</output>
