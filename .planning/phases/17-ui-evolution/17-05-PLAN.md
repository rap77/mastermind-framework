# Plan 17-05: Command Palette with Global Search

> **Status:** 📋 Planned
> **Dependencies:** 17-01 (Three-column layout), 17-02 (Company switcher)
> **Brain #7 Mitigations:** Accessibility ✅, Visual Regression ✅

---

## Brain-Informed Updates

### HIGH Priority Changes from Brain #2 (UX Research)

**Domain Grouping for Brain Commands**
- **Problem:** "Brains" category has 24 items — exceeds Miller's limit (7±2)
- **Fix:** Group brains by domain (Product Strategy, UX Research, UI Design, etc.)
- **Priority:** HIGH — affects command palette usability
- **Acceptance:** Brains grouped by domain within command palette
- **Impact:** Task 3 command categories definition

### MEDIUM Priority Changes from Brain #2 (UX Research)

**Cmd+K Discoverability**
- **Problem:** Power user shortcut — not discoverable for new users
- **Fix:** Add "Search... ⌘K" placeholder in search bar OR onboarding hint
- **Priority:** MEDIUM — affects new user activation
- **Impact:** Task 2 CommandPalette dialog

### HIGH Priority Changes from Brain #4 (Frontend)

**Command Virtualization**
- **Problem:** 50 commands planned (4 screens + 24 brains + actions + settings)
- **Fix:** Set virtualization threshold to 75 items (safer margin)
- **Acceptance:** If command count > 75, use react-window
- **Impact:** Task 2 CommandList component

**Dialog Backdrop Blur**
- **Problem:** "Overlay dialog" but backdrop style unclear
- **Fix:** Specify backdrop: `background: oklch(0 0 0 / 0.5); backdrop-filter: blur(4px);`
- **Impact:** Task 2 CommandPalette dialog

**Selected Item Highlight**
- **Problem:** How do users know which item is selected?
- **Fix:** Add clear visual indicator (accent background + border-left)
- **Impact:** Task 2 CommandPalette dialog

### MEDIUM Priority Changes from Brain #5 (Backend)

**Brain Trigger Idempotency**
- **Problem:** What if user Cmd+K → Enter twice rapidly?
- **Fix:** Add idempotency key or duplicate detection
- **Impact:** Task 5 action execution logic

**Async Handling for Brain Trigger**
- **Problem:** Brain trigger is async (takes seconds) — should palette close immediately?
- **Fix:** Add loading state, close palette after completion
- **Impact:** Task 5 action execution logic

---

## Objectives

Build the command palette (Cmd+K) for global search with:
- Fuzzy search across 4 categories (screens, brains, actions, settings)
- Keyboard navigation (Arrow keys, Enter, Escape)
- Category grouping with icons
- Action execution (navigate, trigger, create)
- Visual regression baseline for overlay dialog

## Success Criteria

1. Command palette opens on Cmd+K (Mac) / Ctrl+K (Windows/Linux)
2. Fuzzy search filters items across 4 categories (screens, brains, actions, settings)
3. Keyboard navigation works (Arrow Up/Down, Enter to select, Escape to close)
4. Categories grouped with icons (Navigation, Brains, Actions, Settings)
5. Actions execute correctly (navigate to screen, trigger brain, open settings)
6. Visual regression baseline captured for dialog overlay

## Tasks

### Task 1: Create commandStore for command palette state

**Files:**
- `apps/web/src/stores/commandStore.ts` (new)

**Subtasks:**
- [ ] Create Zustand store (no Immer needed, simple state)
- [ ] State: isOpen (boolean), query (string), selectedIndex (number)
- [ ] Actions: open, close, setQuery, setSelectedIndex, executeCommand
- [ ] Add keyboard shortcut listener (Cmd+K / Ctrl+K)
- [ ] Add TypeScript types for Command interface (id, label, category, action)

**Acceptance:**
- Store persists command palette state
- Cmd+K / Ctrl+K opens palette (global listener)
- Escape closes palette
- TypeScript strict mode passes

---

### Task 2: Build CommandPalette dialog component

**Files:**
- `apps/web/src/components/command/CommandPalette.tsx` (new)
- `apps/web/src/components/command/CommandInput.tsx` (new)
- `apps/web/src/components/command/CommandList.tsx` (new)

**Subtasks:**
- [ ] Create CommandPalette component (use client directive)
- [ ] Use Dialog component from shadcn/ui (overlay, trap focus)
- [ ] Create CommandInput (search input with icon)
- [ ] Create CommandList (virtualized list of filtered commands)
- [ ] Add fuzzy search logic (fuse.js or custom implementation)
- [ ] Add keyboard navigation (Arrow Up/Down to move, Enter to execute)
- [ ] Styling: Fixed overlay (centered), max-width 600px, max-height 400px

**Acceptance:**
- CommandPalette renders as overlay dialog
- Cmd+K opens dialog, Escape closes
- Input field auto-focuses on open
- Arrow keys navigate through filtered items
- Enter executes selected command

---

### Task 3: Define command categories and items

**Files:**
- `apps/web/src/lib/commands.ts` (new)

**Subtasks:**
- [ ] Define 4 categories: Navigation, Brains, Actions, Settings
- [ ] Navigation items: Command Center, The Nexus, Strategy Vault, Engine Room
- [ ] Brains items: Trigger Product Strategy, Trigger UX Research, etc. (all 24)
- [ ] Actions items: Create Company, Switch Company, Export Data
- [ ] Settings items: API Keys, Brain Configuration, User Profile
- [ ] Add icons per category (Lucide React icons)
- [ ] Add actions per command (navigate, trigger, open)

**Acceptance:**
- 4 categories defined with icons
- Navigation commands (4 screens)
- Brain commands (24 brains)
- Action commands (3-5 common actions)
- Settings commands (3-5 settings pages)

---

### Task 4: Implement fuzzy search logic

**Files:**
- `apps/web/src/lib/fuzzySearch.ts` (new)

**Subtasks:**
- [ ] Implement fuzzy search algorithm (score-based matching)
- [ ] Search across command label and category
- [ ] Sort results by relevance score
- [ ] Highlight matching characters in results
- [ ] Add debouncing (300ms) to reduce re-renders
- [ ] Fallback to exact match if no fuzzy matches

**Acceptance:**
- Fuzzy search finds commands with typos (e.g., "strat" → "Strategy Vault")
- Search is case-insensitive
- Results sorted by relevance (exact match > fuzzy match)
- Matching characters highlighted in results (bold)
- Debouncing reduces input lag (≤ 300ms)

---

### Task 5: Add action execution logic

**Files:**
- `apps/web/src/lib/commands.ts` (extend)

**Subtasks:**
- [ ] Implement navigation actions (router.push)
- [ ] Implement brain trigger actions (POST /api/brains/{id}/trigger)
- [ ] Implement action creators (create company, switch company)
- [ ] Implement settings navigation (open specific settings tab)
- [ ] Add error handling (toast notifications for failures)
- [ ] Close palette after execution

**Acceptance:**
- Navigation commands route to correct screen
- Brain trigger commands call API and show toast
- Action creators execute correctly (create company modal, etc.)
- Settings commands open correct settings tab
- Palette closes after successful execution

---

### Task 6: Capture visual regression baseline

**Files:**
- `e2e/baselines/command-palette/` (new directory)

**Subtasks:**
- [ ] Run `pnpm capture-baselines` script
- [ ] Capture CommandPalette screenshots (open with query, closed)
- [ ] Verify overlay baseline captured (z-index, backdrop blur)
- [ ] Store baselines in git (commit before layout changes)

**Acceptance:**
- Baseline screenshots exist for CommandPalette (open/closed states)
- Overlay baseline captured (backdrop, z-index)
- Baselines committed to git for future diff comparisons

---

### Task 7: Accessibility audit for command palette

**Files:**
- `e2e/accessibility/command-palette.spec.ts` (new)

**Subtasks:**
- [ ] Run axe-core scan on CommandPalette dialog
- [ ] Fix any WCAG violations (focus trap, ARIA labels)
- [ ] Test keyboard navigation (Arrow keys, Enter, Escape)
- [ ] Test screen reader announcements (command count, selected item)
- [ ] Add ARIA roles: dialog, searchbox, listbox, option
- [ ] Add focus management (auto-focus input, trap focus in dialog)

**Acceptance:**
- Zero WCAG Level A violations (automated)
- Keyboard navigation works (Arrow keys, Enter, Escape)
- Screen reader announces command count ("5 commands found")
- Focus trap works (Tab cycles within dialog, not behind it)
- Auto-focus input on open (no focus lost)

---

## Brain #7 Mitigation Checklist

Antes de marcar este plan como COMPLETE, verificar:

- [ ] **Accessibility:** ¿Pasó axe-core + keyboard/screen reader (focus trap, ARIA labels)? ✅ **REQUIRED**
- [ ] **Visual Regression:** ¿Se capturó baseline ANTES de modificar overlays? ✅ **REQUIRED**
- [ ] **Mobile Testing:** ¿Se probaron command palette en BrowserStack (mobile keyboard)? ✅ **REQUIRED**
- [ ] **Cross-browser:** ¿Se probaron Cmd+K (Mac) vs Ctrl+K (Windows/Linux)? ✅ **REQUIRED**

**Todos los 4 items deben estar checkeados antes de comenzar la ejecución.**

---

## Implementation Notes

**Fuzzy search library choice:**
- Option A: fuse.js (battle-tested, but adds dependency)
- Option B: Custom implementation (lighter, full control)
- **Recommendation:** Custom implementation (simple score-based matching)

**Keyboard shortcut implementation:**
- Use `useEffect` to add global listener for Cmd+K / Ctrl+K
- Prevent default browser behavior (Ctrl+K opens dev console in some browsers)
- Remove listener on component unmount (cleanup)

**Dialog component:**
- Use shadcn/ui Dialog (built on Radix UI)
- Focus trap: Tab cycles within dialog
- Escape key: Closes dialog (built-in)
- Click outside: Closes dialog (built-in)

**Virtualization:**
- If command list > 100 items, use react-window (virtualized list)
- For MVP (~50 commands), no virtualization needed
- Monitor performance, add virtualization if laggy

---

## Testing Strategy

**Unit Tests (Vitest):**
- commandStore actions (open, close, setQuery, executeCommand)
- Fuzzy search logic (score calculation, highlighting)
- Command execution (navigation, API calls)

**Integration Tests (Playwright):**
- Command palette opens on Cmd+K
- Fuzzy search filters commands correctly
- Keyboard navigation works (Arrow keys, Enter, Escape)
- Actions execute correctly

**Accessibility Tests:**
- axe-core scan (zero Level A violations)
- Keyboard navigation (Tab trap, Arrow keys, Enter, Escape)
- Screen reader announcements (command count, selected item)

---

## Rollback Plan

If fuzzy search causes performance issues:
1. Replace with exact match search (startsWith)
2. Reduce debouncing to 100ms (faster feedback)
3. Limit results to 20 items (reduce DOM size)

If keyboard navigation conflicts:
1. Remove global listener, add button trigger only
2. Use different shortcut (Cmd+Shift+P instead of Cmd+K)
3. Add setting to disable/customize shortcut

---

**Estimated effort:** 2-3 days
**Risk level:** Low (well-understood pattern, no performance critical path)
**Next plan:** 17-06 (Onboarding Wizard + Mobile Polish)
