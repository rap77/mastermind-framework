---
phase: 17-ui-evolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(protected)/layout.tsx
  - apps/web/src/components/layout/CompanyRail.tsx
  - apps/web/src/components/layout/AppSidebar.tsx
  - apps/web/src/components/layout/ThreeColumnLayout.tsx
  - apps/web/src/stores/layoutStore.ts
  - apps/web/src/app/globals.css
autonomous: true
requirements:
  - UIE-01
must_haves:
  truths:
    - User sees three-column layout (CompanyRail + Sidebar + Content) on desktop
    - User can collapse/expand each column independently
    - Layout is responsive (single column on mobile, three columns on desktop)
    - Layout state persists across page navigations
  artifacts:
    - path: apps/web/src/components/layout/ThreeColumnLayout.tsx
      provides: Three-column layout wrapper with collapsible columns
      min_lines: 150
    - path: apps/web/src/components/layout/CompanyRail.tsx
      provides: Left column for company switcher (placeholder for Plan 02)
      min_lines: 80
    - path: apps/web/src/components/layout/AppSidebar.tsx
      provides: Center column for navigation items
      min_lines: 100
    - path: apps/web/src/stores/layoutStore.ts
      provides: Layout state management (collapsed/expanded state)
      exports: ["useLayoutStore", "LayoutState"]
    - path: apps/web/src/app/globals.css
      provides: CSS variables for column widths and transitions
      contains: "--company-rail-width", "--sidebar-width"
  key_links:
    - from: apps/web/src/app/(protected)/layout.tsx
      to: apps/web/src/components/layout/ThreeColumnLayout.tsx
      via: Import and render ThreeColumnLayout wrapper
      pattern: "import.*ThreeColumnLayout"
    - from: apps/web/src/components/layout/ThreeColumnLayout.tsx
      to: apps/web/src/stores/layoutStore.ts
      via: Zustand store for layout state
      pattern: "useLayoutStore"
---

<objective>
Build the foundation three-column layout (CompanyRail + Sidebar + Content) with collapsible columns and responsive breakpoints.

Purpose: Establish the core layout structure that will host all 10 UX patterns from Paperclip.
Output: Working three-column layout with state persistence and responsive behavior.
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ui-evolution/17-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/app/(protected)/layout.tsx
@apps/web/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create layoutStore for state management</name>
  <files>apps/web/src/stores/layoutStore.ts</files>
  <action>
    Create Zustand store with Immer middleware for layout state:
    - State: companyRailCollapsed, sidebarCollapsed, propertiesPanelOpen, densityMode ('compact' | 'normal' | 'detailed')
    - Actions: toggleCompanyRail, toggleSidebar, togglePropertiesPanel, setDensityMode
    - Persist to localStorage (layout state)
    - Use same pattern as brainStore.ts (Immer + Map for performance)
  </action>
  <verify>
    <automated>pnpm test -- --grep "layoutStore"</automated>
  </verify>
  <done>Store created with 4 state properties, 4 actions, localStorage persistence working</done>
</task>

<task type="auto">
  <name>Task 2: Build ThreeColumnLayout component</name>
  <files>apps/web/src/components/layout/ThreeColumnLayout.tsx</files>
  <action>
    Create Client Component (use client directive):
    - Props: children (ReactNode), showPropertiesPanel (boolean)
    - Layout: Grid with 3 columns (180px + 240px + auto)
    - Collapse behavior: CSS transitions for smooth width changes
    - Responsive: Single column on mobile (< 768px), three columns on desktop
    - Integrate with layoutStore for state management
    - Use Tailwind grid: grid-cols-[auto_1fr] for desktop, grid-cols-1 for mobile
    - Add collapse toggle buttons (chevron icons)
  </action>
  <verify>
    <automated>pnpm test -- --grep "ThreeColumnLayout"</automated>
  </verify>
  <done>Three-column layout renders correctly, collapses/expands with smooth transitions, responsive breakpoints working</done>
</task>

<task type="auto">
  <name>Task 3: Build CompanyRail placeholder</name>
  <files>apps/web/src/components/layout/CompanyRail.tsx</files>
  <action>
    Create Client Component:
    - Placeholder content: "CompanyRail" (full implementation in Plan 02)
    - Fixed width: 180px (collapsed: 60px)
    - Collapse button at top
    - Drag handle for future reordering (Plan 02)
    - Styling: Border-right, bg-muted, flex-col layout
  </action>
  <verify>
    <automated>pnpm test -- --grep "CompanyRail"</automated>
  </verify>
  <done>CompanyRail renders in left column, collapse button working, width transitions smooth</done>
</task>

<task type="auto">
  <name>Task 4: Build AppSidebar with navigation</name>
  <files>apps/web/src/components/layout/AppSidebar.tsx</files>
  <action>
    Create Client Component:
    - Nav items: Command Center, The Nexus, Strategy Vault, Engine Room (link to existing routes)
    - Active state highlighting (usePathname from next/navigation)
    - Fixed width: 240px (collapsed: 60px)
    - Collapse button at top
    - Icons for each nav item (Lucide React)
    - Styling: Border-right, bg-background, flex-col layout
  </action>
  <verify>
    <automated>pnpm test -- --grep "AppSidebar"</automated>
  </verify>
  <done>Sidebar renders with 4 nav items, active route highlighted, collapse working, keyboard navigation working</done>
</task>

<task type="auto">
  <name>Task 5: Add CSS variables and responsive classes</name>
  <files>apps/web/src/app/globals.css</files>
  <action>
    Add CSS variables for layout:
    - --company-rail-width: 180px (collapsed: 60px)
    - --sidebar-width: 240px (collapsed: 60px)
    - --transition-duration: 200ms
    - Add @media queries for mobile (< 768px): single column, bottom nav styles (for Plan 06)
    - Ensure OKLCH color system variables are used for consistency
  </action>
  <verify>
    <automated>grep -n "company-rail-width\|sidebar-width" apps/web/src/app/globals.css</automated>
  </verify>
  <done>CSS variables defined, responsive breakpoints working, transitions smooth</done>
</task>

<task type="auto">
  <name>Task 6: Integrate ThreeColumnLayout into protected layout</name>
  <files>apps/web/src/app/(protected)/layout.tsx</files>
  <action>
    Modify existing AuthGuardLayout:
    - Wrap children with ThreeColumnLayout
    - Pass showPropertiesPanel={false} (will be conditional in future plans)
    - Preserve existing WSBrainBridge and ErrorBoundary
    - Ensure no breaking changes to existing auth flow
  </action>
  <verify>
    <automated>pnpm test -- --grep "protected.*layout"</automated>
  </verify>
  <done>Three-column layout renders on all protected routes, existing auth flow preserved, no breaking changes</done>
</task>

</tasks>

<verification>
1. Manual verification: Visit localhost:3000, confirm three-column layout visible on desktop
2. Manual verification: Test collapse/expand buttons for CompanyRail and Sidebar
3. Manual verification: Check responsive behavior (resize browser to mobile width)
4. Automated: Run pnpm test, all layout tests passing
5. Automated: Check localStorage persists layout state across page refreshes
</verification>

<success_criteria>
1. Three-column layout renders correctly on desktop (CompanyRail + Sidebar + Content)
2. Each column can collapse/expand independently with smooth transitions
3. Layout is responsive (single column on mobile, three columns on desktop)
4. Layout state persists across page navigations via localStorage
5. All existing 4 screens (Command Center, Nexus, Strategy Vault, Engine Room) still work
6. No breaking changes to existing auth flow or WebSocket infrastructure
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-evolution/17-01-SUMMARY.md` with:
- Tasks completed (6/6)
- Files created/modified (6 files)
- Tests passing (layout unit tests)
- Deviations encountered
- Next steps: Plan 17-02 (multi-tenant company switcher)
</output>
