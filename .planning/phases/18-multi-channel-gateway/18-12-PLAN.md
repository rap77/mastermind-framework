---
phase: 18-multi-channel-gateway
plan: 12
type: execute
wave: gap-closure-frontend-fixes
depends_on: [18-11]
files_modified:
  - apps/web/src/components/VirtualizedCommandList.tsx
  - apps/web/src/components/CommandCenterWrapper.tsx
  - apps/web/src/app/(protected)/command-center/page.tsx
autonomous: true
gap_closure: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Dev server starts without build errors"
    - "All components compile successfully"
    - "No TypeScript or import errors"
  artifacts:
    - path: "apps/web/src/components/VirtualizedCommandList.tsx"
      provides: "Virtualized command list component"
      contains: "Fixed imports, no build errors"
    - path: "apps/web/src/components/CommandCenterWrapper.tsx"
      provides: "Command center wrapper component"
      contains: "Fixed imports, no build errors"
  key_links:
    - from: "pnpm run dev"
      to: "Next.js dev server"
      via: "Start server successfully"
---

<objective>
Fix build errors in VirtualizedCommandList and CommandCenterWrapper components, start dev server, and verify /messaging route works.

Purpose: Complete the frontend gap closure by fixing remaining build errors that prevent dev server from starting
Output: Working dev server with /messaging route accessible
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/18-multi-channel-gateway/18-11-PLAN.md
@.planning/phases/18-multi-channel-gateway/18-UAT.md
@apps/web/src/components/VirtualizedCommandList.tsx
@apps/web/src/components/CommandCenterWrapper.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix build errors in VirtualizedCommandList.tsx</name>
  <files>apps/web/src/components/VirtualizedCommandList.tsx</files>
  <action>
    Investigate and fix any build errors in VirtualizedCommandList.tsx:

    1. Read the file to identify the specific error
    2. Common issues to check:
       - Missing imports (React, components, utilities)
       - Incorrect import paths
       - TypeScript errors
       - Missing dependencies in package.json
       - Client/server component boundary issues

    3. Fix identified issues:
       - Add missing imports
       - Correct import paths
       - Add 'use client' directive if needed
       - Fix TypeScript errors
       - Install missing dependencies if needed

    4. Verify fix by checking for any remaining build errors
  </action>
  <acceptance>
    - VirtualizedCommandList.tsx has no build errors
    - All imports resolve correctly
    - TypeScript compilation succeeds
  </acceptance>
</task>

<task type="auto">
  <name>Task 2: Fix build errors in CommandCenterWrapper.tsx</name>
  <files>apps/web/src/components/CommandCenterWrapper.tsx</files>
  <action>
    Investigate and fix any build errors in CommandCenterWrapper.tsx:

    1. Read the file to identify the specific error
    2. Common issues to check:
       - Missing imports (React, components, utilities)
       - Incorrect import paths
       - TypeScript errors
       - Missing dependencies in package.json
       - Client/server component boundary issues

    3. Fix identified issues:
       - Add missing imports
       - Correct import paths
       - Add 'use client' directive if needed
       - Fix TypeScript errors
       - Install missing dependencies if needed

    4. Verify fix by checking for any remaining build errors
  </action>
  <acceptance>
    - CommandCenterWrapper.tsx has no build errors
    - All imports resolve correctly
    - TypeScript compilation succeeds
  </acceptance>
</task>

<task type="auto">
  <name>Task 3: Start dev server and verify /messaging route</name>
  <files>apps/web/</files>
  <action>
    Start the Next.js dev server and verify the /messaging route works:

    1. Navigate to apps/web directory
    2. Start dev server: pnpm run dev
    3. Wait for server to start (look for "Ready" message)
    4. Check for any build errors or warnings
    5. Note the server URL (usually http://localhost:3000 or :3001)
    6. Verify /messaging route is accessible by checking the output
    7. Document any remaining issues

    If server fails to start:
    - Capture the error message
    - Identify the problematic file
    - Fix the issue
    - Retry starting the server
  </action>
  <acceptance>
    - Dev server starts successfully
    - No build errors in console
    - Server shows "Ready" or similar message
    - /messaging route is listed in available routes
  </acceptance>
</task>

<task type="auto">
  <name>Task 4: Create commit with fixes</name>
  <files>apps/web/src/components/</files>
  <action>
    Create a git commit with the build error fixes:

    1. Check git status to see modified files
    2. Stage the fixed files
    3. Create commit with descriptive message:
       ```
       fix(18-12): fix build errors in VirtualizedCommandList and CommandCenterWrapper

       - Fixed missing imports in VirtualizedCommandList.tsx
       - Fixed missing imports in CommandCenterWrapper.tsx
       - Dev server now starts without errors
       ```
    4. Verify commit was created successfully
  </action>
  <acceptance>
    - Git commit created successfully
    - Commit message follows conventional commits format
    - All fixes included in commit
  </acceptance>
</task>

</tasks>

<success_criteria>
1. VirtualizedCommandList.tsx compiles without errors
2. CommandCenterWrapper.tsx compiles without errors
3. Dev server starts successfully (pnpm run dev)
4. No build errors in console output
5. /messaging route is accessible
6. All changes committed to git
</success_criteria>

<next_steps>
After this plan completes:
1. Inform user that dev server is ready for manual UAT
2. Provide server URL and /messaging route URL
3. List the 6 UAT tests that need manual verification
4. Wait for user to complete manual UAT or request additional fixes
</next_steps>
