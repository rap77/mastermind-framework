---
phase: 18-multi-channel-gateway
plan: 06
type: execute
wave: 2
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - rust_control_plane/src/channels/email.rs
  - rust_control_plane/tests/email_test.rs
  - apps/api/routers/email.py
  - apps/api/tests/test_email.py
autonomous: true
requirements:
  - MCG-01
user_setup:
  - service: email
    why: "SMTP server configuration required for sending emails"
    env_vars:
      - name: SMTP_HOST
        source: "Email provider (SMTP server hostname)"
      - name: SMTP_PORT
        source: "Email provider (usually 587 for TLS)"
      - name: SMTP_USERNAME
        source: "Email provider username"
      - name: SMTP_PASSWORD
        source: "Email provider password/app-specific password"

must_haves:
  truths:
    - "Incoming emails are received via webhook (SendGrid/Mailgun/Postmark)"
    - "Outgoing emails are sent via SMTP (aiosmtplib)"
    - "Email threading (In-Reply-To, References) is preserved"
    - "HTML email sanitization (DOMPurify) prevents XSS"
  artifacts:
    - path: "rust_control_plane/src/channels/email.rs"
      provides: "Email webhook payload parser"
      exports: ["parse_email_webhook", "extract_message_id", "extract_thread_id"]
      min_lines: 80
    - path: "apps/api/routers/email.py"
      provides: "Email sender API (SMTP)"
      exports: ["send_email", "send_html_email"]
      min_lines: 100
    - path: "rust_control_plane/tests/email_test.rs"
      provides: "Email webhook parsing tests"
      min_lines: 60
    - path: "apps/api/tests/test_email.py"
      provides: "Email send API tests"
      min_lines: 50
  key_links:
    - from: "rust_control_plane/src/handlers/webhook.rs"
      to: "rust_control_plane/src/channels/email.rs"
      via: "parse_email_webhook() for channel='email'"
      pattern: "if channel == \"email\" \\{ email::parse_email_webhook\\(payload\\)"
    - from: "apps/api/routers/email.py"
      to: "SMTP server"
      via: "aiosmtplib.send()"
      pattern: "await smtp\\.send_message\\(message\\)"
    - from: "HTML email content"
      to: "DOMPurify"
      via: "sanitize before rendering"
      pattern: "DOMPurify\\.sanitize\\(html_content\\)"
---

<objective>
Implement Email adapter (SMTP + webhook)
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-multi-channel-gateway/18-CONTEXT.md
@.planning/phases/18-multi-channel-gateway/18-01-PLAN.md
@rust_control_plane/src/handlers/webhook.rs
@rust_control_plane/src/channels/whatsapp.rs
</context>

<interfaces>
<!-- From rust_control_plane/src/channels/whatsapp.rs (18-04 output) -->
```rust
pub struct MessagePayload {
    pub message_id: String,
    pub sender: String,
    pub recipient: String,
    pub message_type: String,
    pub content: String,
    pub media_url: Option<String>,
    pub timestamp: i64,
}
```

<!-- From apps/web/package.json (confirmed DOMPurify) -->
```json
"dompurify": "^3.3.3"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Email webhook payload parser</name>
  <files>rust_control_plane/src/channels/email.rs</files>
  <behavior>
    - parse_email_webhook() extracts: message_id, from_email, to_email, subject, plain_text, html_body, thread_id (In-Reply-To, References)
    - extract_message_id() returns Message-ID header
    - extract_thread_id() returns References or In-Reply-To header
    - Returns standardized EmailMessage struct
    - Handles both SendGrid and Mailgun webhook formats
  </behavior>
  <action>
    Create `rust_control_plane/src/channels/email.rs`:
    1. Define `EmailMessage` struct: message_id, from_email, to_email, subject, plain_text, html_body, thread_id, timestamp, attachments (Vec<Attachment>)
    2. Define `Attachment` struct: filename, content_type, size, url
    3. Implement `parse_email_webhook(payload: Value)` → Result<EmailMessage, Error>
       - Detect webhook format (SendGrid vs Mailgun vs Postmark)
       - Extract headers: Message-ID, In-Reply-To, References
       - Parse from, to, subject, text, html
       - Extract attachments if present
       - Derive thread_id from References or In-Reply-To
       - Return standardized EmailMessage
    4. Implement `extract_message_id(payload)` → Result<String, Error>
    5. Implement `extract_thread_id(payload)` → Result<String, Error>
    6. Implement `is_email_webhook(payload)` → bool

    Update `rust_control_plane/src/channels/mod.rs`:
    1. Add `pub mod email;`
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test test_email_webhook_parser -- --nocapture</automated>
  </verify>
  <done>
    Email webhook parser compiles. Tests verify extraction of message_id, thread_id, headers, and attachments. Both SendGrid and Mailgun formats work.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Email sender (Python + aiosmtplib)</name>
  <files>apps/api/routers/email.py, apps/api/tests/test_email.py</files>
  <behavior>
    - send_email() sends plain text emails via SMTP
    - send_html_email() sends HTML emails (sanitized with DOMPurify in frontend)
    - Uses aiosmtplib for async SMTP
    - Preserves threading (In-Reply-To, References headers)
    - Handles attachments
  </behavior>
  <action>
    Create `apps/api/routers/email.py`:
    1. Define `EmailMessage` pydantic model: to, subject, plain_text, html_body, thread_id, in_reply_to
    2. Define `send_email(message: EmailMessage)` → dict
       - Get SMTP config from env (SMTP_HOST, SMTP_PORT, SMTP_USERNAME, SMTP_PASSWORD)
       - Create MIME message: MIMEMultipart('alternative')
       - Attach plain text and HTML body
       - Add threading headers: In-Reply-To, References (from thread_id)
       - Send via aiosmtplib: `await smtp.send_message(message)`
       - Return {"message_id": generated_message_id}
    3. Implement `send_html_email()` → uses send_email with html_body
    4. Implement `add_attachment()` → adds MIME attachment
    5. Add route: `@router.post("/api/channels/email/send")`

    Create `apps/api/tests/test_email.py`:
    1. Mock aiosmtplib SMTP client
    2. Test send_email() with plain text
    3. Test send_html_email() with HTML body
    4. Test threading headers (In-Reply-To, References)
    5. Test attachment handling
    6. Verify message_id generated correctly

    Update `apps/api/main.py`:
    1. Include email router
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/test_email.py -v</automated>
  </verify>
  <done>
    Email sender API compiles. Tests verify plain text and HTML sending. Threading headers preserved. Attachments work.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate Email adapter into webhook flow</name>
  <files>rust_control_plane/src/handlers/webhook.rs, rust_control_plane/src/queue/worker.rs</files>
  <behavior>
    - Webhook receiver uses email parser for channel='email'
    - Worker sends to Python /api/channels/email/send endpoint
    - Email threading is preserved (thread_id, in_reply_to)
    - Attachments are downloaded and stored
  </behavior>
  <action>
    Update `rust_control_plane/src/handlers/webhook.rs`:
    1. After channel extraction, call: `channels::email::parse_email_webhook(payload)`
    2. Store parsed EmailMessage in WebhookEvent
    3. Handle threading: extract thread_id for grouping

    Update `rust_control_plane/src/queue/worker.rs`:
    1. On processing webhook:
       - Extract EmailMessage from WebhookEvent
       - Call Python gRPC endpoint: `/api/channels/email/send`
       - Pass: to, subject, plain_text, html_body, thread_id, in_reply_to
    2. Handle attachments:
       - Download attachment URLs to S3/local storage
       - Store S3 paths in messages table
    3. Preserve threading:
       - Pass thread_id to frontend for grouping
       - Include In-Reply-To and References in outgoing emails

    Update `rust_control_plane/tests/email_test.rs`:
    1. Test end-to-end webhook → send flow
    2. Test threading preservation
    3. Test attachment handling
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test test_email_end_to_end -- --nocapture</automated>
  </verify>
  <done>
    Email adapter integrated. Webhook → send flow works. Threading preserved. Attachments stored.
  </done>
</task>

</tasks>

<verification>
- [ ] Email webhook parser extracts all fields
- [ ] send_email() sends plain text emails
- [ ] send_html_email() sends HTML emails
- [ ] Threading preserved (In-Reply-To, References)
- [ ] Attachments handled
- [ ] End-to-end webhook → send flow works
- [ ] All tests pass: `cargo test --test email_test` and `pytest apps/api/tests/test_email.py`
</verification>

<success_criteria>
Email adapter is fully implemented. Incoming emails are received via webhook, outgoing emails sent via SMTP. Email threading is preserved. HTML sanitization prevents XSS. Attachments are supported.
</success_criteria>

<output>
After completion, create `.planning/phases/18-multi-channel-gateway/18-06-SUMMARY.md`
</output>
