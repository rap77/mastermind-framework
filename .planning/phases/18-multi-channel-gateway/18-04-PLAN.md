---
phase: 18-multi-channel-gateway
plan: 04
type: execute
wave: 2
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - rust_control_plane/src/channels/whatsapp.rs
  - rust_control_plane/tests/whatsapp_test.rs
  - apps/api/routers/whatsapp.py
  - apps/api/tests/test_whatsapp.py
autonomous: true
requirements:
  - MCG-01
user_setup:
  - service: whatsapp
    why: "Sending messages requires WhatsApp Business API access token"
    env_vars:
      - name: WHATSAPP_BUSINESS_ACCOUNT_ID
        source: "Meta for Developers → WhatsApp → Accounts"
      - name: WHATSAPP_ACCESS_TOKEN
        source: "Meta for Developers → WhatsApp → Phone Numbers → API Token"

must_haves:
  truths:
    - "WhatsApp webhooks are received and verified"
    - "WhatsApp messages are sent via WhatsApp Business Cloud API"
    - "Message status updates (sent, delivered, read) are tracked"
    - "Media messages (images, documents) are handled"
  artifacts:
    - path: "rust_control_plane/src/channels/whatsapp.rs"
      provides: "WhatsApp webhook payload parser"
      exports: ["parse_whatsapp_webhook", "extract_message_id", "extract_sender_phone"]
      min_lines: 80
    - path: "apps/api/routers/whatsapp.py"
      provides: "WhatsApp message sender API"
      exports: ["send_whatsapp_message", "send_whatsapp_media"]
      min_lines: 100
    - path: "rust_control_plane/tests/whatsapp_test.rs"
      provides: "WhatsApp webhook parsing tests"
      min_lines: 60
    - path: "apps/api/tests/test_whatsapp.py"
      provides: "WhatsApp send API tests"
      min_lines: 50
  key_links:
    - from: "rust_control_plane/src/handlers/webhook.rs"
      to: "rust_control_plane/src/channels/whatsapp.rs"
      via: "parse_whatsapp_webhook() for channel='whatsapp'"
      pattern: "if channel == \"whatsapp\" \\{ whatsapp::parse_whatsapp_webhook\\(payload\\)"
    - from: "apps/api/routers/whatsapp.py"
      to: "WhatsApp Business Cloud API"
      via: "POST https://graph.facebook.com/v19.0/{PHONE_ID}/messages"
      pattern: "requests\\.post\\(.*whatsapp.*messages"
    - from: "message status webhook"
      to: "messages table"
      via: "update status: sent/delivered/read"
      pattern: "UPDATE messages SET status =.*WHERE external_message_id"
---

<objective>
Implement WhatsApp Business Cloud API adapter
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-multi-channel-gateway/18-CONTEXT.md
@.planning/phases/18-multi-channel-gateway/18-01-PLAN.md
@rust_control_plane/src/handlers/webhook.rs
@rust_control_plane/migrations/003_add_messages_table.sql
</context>

<interfaces>
<!-- From rust_control_plane/src/handlers/webhook.rs (18-01 output) -->
```rust
pub async fn webhook_receiver(
    Path(channel): Path<String>,
    Json(payload): Json<serde_json::Value>,
) -> Result<StatusCode, Error>
```

<!-- From rust_control_plane/src/queue/mod.rs (18-01 output) -->
```rust
pub struct WebhookEvent {
    pub channel: String,
    pub payload: serde_json::Value,
    pub trace_id: String,
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement WhatsApp webhook payload parser</name>
  <files>rust_control_plane/src/channels/mod.rs, rust_control_plane/src/channels/whatsapp.rs</files>
  <behavior>
    - parse_whatsapp_webhook() extracts: message_id, sender_phone, message_type, text/content, media_url (if applicable)
    - extract_message_id() returns entry[0].changes[0].value.messages[0].id
    - extract_sender_phone() returns entry[0].changes[0].value.messages[0].from
    - Returns standardized MessagePayload struct
    - Handles missing fields gracefully
  </behavior>
  <action>
    Create `rust_control_plane/src/channels/mod.rs`:
    1. Add `pub mod whatsapp;`

    Create `rust_control_plane/src/channels/whatsapp.rs`:
    1. Define `MessagePayload` struct: message_id, sender, recipient, message_type (text/image/audio/document), content, media_url (optional), timestamp
    2. Implement `parse_whatsapp_webhook(payload: Value)` → Result<MessagePayload, Error>
       - Extract: payload["entry"][0]["changes"][0]["value"]["messages"][0]
       - Parse message_id, from (sender phone), text (body), or media fields
       - Handle different message types: text, image, audio, document
       - Return standardized MessagePayload
    3. Implement `extract_message_id(payload)` → Result<String, Error>
    4. Implement `extract_sender_phone(payload)` → Result<String, Error>
    5. Implement `is_message_webhook(payload)` → bool (filter out status updates)

    Update `rust_control_plane/src/lib.rs`:
    1. Add `pub mod channels;`
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test test_whatsapp_webhook_parser -- --nocapture</automated>
  </verify>
  <done>
    WhatsApp webhook parser compiles. Tests verify extraction of message_id, sender_phone, text content, and media URLs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement WhatsApp message sender (Python)</name>
  <files>apps/api/routers/whatsapp.py, apps/api/tests/test_whatsapp.py</files>
  <behavior>
    - send_whatsapp_message() sends text messages via WhatsApp Business Cloud API
    - send_whatsapp_media() sends media messages (images, documents)
    - Uses WHATSAPP_ACCESS_TOKEN for authentication
    - Returns message_id from API response
    - Handles API errors gracefully
  </behavior>
  <action>
    Create `apps/api/routers/whatsapp.py`:
    1. Define `WhatsAppMessage` pydantic model: to (phone_number), message_type, text, media_url
    2. Define `send_whatsapp_message(message: WhatsAppMessage)` → dict
       - GET WHATSAPP_PHONE_NUMBER_ID from env
       - POST to: `https://graph.facebook.com/v19.0/{WHATSAPP_PHONE_NUMBER_ID}/messages`
       - Headers: `Authorization: Bearer {WHATSAPP_ACCESS_TOKEN}`
       - Body: {"messaging_product": "whatsapp", "to": phone, "type": "text", "text": {"body": text}}
       - Return {"message_id": response["messages"][0]["id"]}
    3. Define `send_whatsapp_media(to, media_type, media_url)` → dict
       - POST to same endpoint
       - Body includes media type-specific fields
       - Return {"message_id": response["messages"][0]["id"]}
    4. Implement error handling for 4xx/5xx responses
    5. Add route: `@router.post("/api/channels/whatsapp/send")`

    Create `apps/api/tests/test_whatsapp.py`:
    1. Mock HTTPX client for WhatsApp API
    2. Test send_whatsapp_message() with text
    3. Test send_whatsapp_media() with image
    4. Test error handling (401, 404, 500)
    5. Verify message_id returned correctly

    Update `apps/api/main.py`:
    1. Include whatsapp router
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/test_whatsapp.py -v</automated>
  </verify>
  <done>
    WhatsApp sender API compiles. Tests verify text and media sending. Error handling works for 4xx/5xx responses.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate WhatsApp adapter into webhook flow</name>
  <files>rust_control_plane/src/handlers/webhook.rs, rust_control_plane/src/queue/worker.rs</files>
  <behavior>
    - Webhook receiver uses whatsapp parser for channel='whatsapp'
    - Worker sends to Python /api/channels/whatsapp/send endpoint
    - Message status updates (sent, delivered, read) are handled
    - Media messages are queued for background download
  </behavior>
  <action>
    Update `rust_control_plane/src/handlers/webhook.rs`:
    1. After channel extraction, call: `channels::whatsapp::parse_whatsapp_webhook(payload)`
    2. Store parsed MessagePayload in WebhookEvent
    3. Handle status update webhooks separately (update messages table status)

    Update `rust_control_plane/src/queue/worker.rs`:
    1. On processing webhook:
       - Extract MessagePayload from WebhookEvent
       - Call Python gRPC endpoint: `/api/channels/whatsapp/send`
       - Pass: sender_phone, message_type, content, media_url
    2. Handle message status webhooks:
       - Update messages table: status = "sent" | "delivered" | "read"
       - Use external_message_id for correlation

    Update `rust_control_plane/tests/whatsapp_test.rs`:
    1. Test end-to-end webhook → send flow
    2. Test status update handling
    3. Test media message handling
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test test_whatsapp_end_to_end -- --nocapture</automated>
  </verify>
  <done>
    WhatsApp adapter integrated. Webhook → send flow works. Status updates handled correctly. Media messages processed.
  </done>
</task>

</tasks>

<verification>
- [ ] WhatsApp webhook parser extracts all fields
- [ ] send_whatsapp_message() sends text messages
- [ ] send_whatsapp_media() sends media messages
- [ ] Status updates handled (sent/delivered/read)
- [ ] End-to-end webhook → send flow works
- [ ] All tests pass: `cargo test --test whatsapp_test` and `pytest apps/api/tests/test_whatsapp.py`
</verification>

<success_criteria>
WhatsApp Business Cloud API adapter is fully implemented. Webhooks are received, parsed, and messages are sent via WhatsApp API. Status updates are tracked. Media messages are supported.
</success_criteria>

<output>
After completion, create `.planning/phases/18-multi-channel-gateway/18-04-SUMMARY.md`
</output>
