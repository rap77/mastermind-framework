---
phase: 18-multi-channel-gateway
plan: 02
type: execute
wave: 1
depends_on: [18-01]
files_modified:
  - rust_control_plane/migrations/004_add_dlq_table.sql
  - rust_control_plane/src/dlq/mod.rs
  - rust_control_plane/src/dlq/retry_worker.rs
  - rust_control_plane/tests/dlq_test.rs
autonomous: true
requirements:
  - MCG-01
user_setup: []

must_haves:
  truths:
    - "Failed webhooks are moved to DLQ after 3 retries with exponential backoff"
    - "DLQ retry pattern: 1s → 5s → 30s → DLQ (Brain #7 Condition #6)"
    - "Manual retry UI allows operators to resubmit failed webhooks"
    - "DLQ recovery rate > 80% after 1 retry (Brain #7 SLI-5)"
  artifacts:
    - path: "rust_control_plane/migrations/004_add_dlq_table.sql"
      provides: "Dead Letter Queue table"
      contains: "webhook_dlq"
      min_lines: 20
    - path: "rust_control_plane/src/dlq/mod.rs"
      provides: "DLQ repository with retry logic"
      exports: ["move_to_dlq", "retry_webhook", "get_failed_webhooks"]
      min_lines: 100
    - path: "rust_control_plane/src/dlq/retry_worker.rs"
      provides: "Background worker for DLQ retries"
      exports: ["start_retry_worker"]
      min_lines: 80
    - path: "rust_control_plane/tests/dlq_test.rs"
      provides: "DLQ retry logic tests"
      min_lines: 60
  key_links:
    - from: "rust_control_plane/src/queue/worker.rs"
      to: "rust_control_plane/src/dlq/mod.rs"
      via: "move_to_dlq() after 3 failures"
      pattern: "if retry_count >= 3 \\{ dlq::move_to_dlq\\(event\\).await"
    - from: "rust_control_plane/src/dlq/retry_worker.rs"
      to: "rust_control_plane/src/queue/worker.rs"
      via: "resubmit to queue on manual retry"
      pattern: "webhook_queue.send\\(retry_event\\).await"
    - from: "exponential backoff"
      to: "retry delays"
      via: "tokio::time::sleep with 1s, 5s, 30s"
      pattern: "tokio::time::sleep\\(Duration::from_secs\\((1|5|30)\\)"
---

<objective>
Implement Dead Letter Queue with exponential backoff retry strategy (Brain #7 Condition #6)
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-multi-channel-gateway/18-CONTEXT.md
@.planning/phases/18-multi-channel-gateway/18-BRAIN7-EVALUATION.md
@.planning/phases/18-multi-channel-gateway/18-01-PLAN.md
@rust_control_plane/migrations/003_add_messages_table.sql
</context>

<interfaces>
<!-- From 18-01-PLAN.md (Task 1 output) -->
```sql
CREATE TABLE messages (
  id UUID PRIMARY KEY,
  external_message_id TEXT NOT NULL,
  channel TEXT NOT NULL,
  payload JSONB NOT NULL,
  status TEXT DEFAULT 'pending',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  UNIQUE (external_message_id, channel)
);
```

<!-- From rust_control_plane/src/queue/mod.rs (to be created in 18-01) -->
```rust
pub struct WebhookEvent {
  pub channel: String,
  pub payload: serde_json::Value,
  pub trace_id: String,
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DLQ table and repository</name>
  <files>rust_control_plane/migrations/004_add_dlq_table.sql, rust_control_plane/src/dlq/mod.rs</files>
  <behavior>
    - webhook_dlq table exists with columns: id, external_message_id, channel, payload, error_message, retry_count, created_at, last_retry_at
    - move_to_dlq() inserts failed webhook with error context
    - get_failed_webhooks() returns paginated failed webhooks
    - retry_webhook() increments retry_count and updates last_retry_at
  </behavior>
  <action>
    Create migration `rust_control_plane/migrations/004_add_dlq_table.sql`:
    1. Add `webhook_dlq` table with UUID primary key
    2. Add `external_message_id` TEXT column (not null)
    3. Add `channel` TEXT column (not null)
    4. Add `payload` JSONB column (not null)
    5. Add `error_message` TEXT column (not null)
    6. Add `retry_count` INTEGER with DEFAULT 0
    7. Add `created_at` TIMESTAMP with DEFAULT NOW()
    8. Add `last_retry_at` TIMESTAMP (nullable)
    9. Add index: `CREATE INDEX idx_dlq_retry_count ON webhook_dlq(retry_count, created_at)`

    Create `rust_control_plane/src/dlq/mod.rs`:
    1. Define `DeadLetterQueue` struct with PgPool
    2. Implement `move_to_dlq(event, error)` → inserts to webhook_dlq
    3. Implement `get_failed_webhooks(limit, offset)` → returns Vec<FailedWebhook>
    4. Implement `retry_webhook(id)` → increments retry_count, updates last_retry_at
    5. Implement `get_retry_count(id)` → returns current retry_count
    6. Define `FailedWebhook` struct with all fields

    Update `rust_control_plane/src/lib.rs`:
    1. Add `pub mod dlq;`
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test test_dlq_repository -- --nocapture</automated>
  </verify>
  <done>
    DLQ table created. Repository functions compile. Tests verify insert, retrieve, and retry operations.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement retry worker with exponential backoff (Brain #7 Condition #6)</name>
  <files>rust_control_plane/src/dlq/retry_worker.rs</files>
  <behavior>
    - Background worker runs every 30 seconds
    - Queries DLQ for webhooks with retry_count < 3
    - Applies exponential backoff: retry 1 (1s), retry 2 (5s), retry 3 (30s)
    - Resubmits to webhook_queue after backoff delay
    - Moves to DLQ permanently after 3 failures
  </behavior>
  <action>
    Create `rust_control_plane/src/dlq/retry_worker.rs`:
    1. Define `RetryWorker` struct with PgPool and WebhookQueue
    2. Implement `start_retry_worker()` → tokio::spawn infinite loop
    3. Loop logic:
       a. Sleep 30 seconds
       b. Query: `SELECT * FROM webhook_dlq WHERE retry_count < 3 ORDER BY created_at ASC LIMIT 100`
       c. For each webhook:
          - Calculate backoff delay: [1s, 5s, 30s][retry_count]
          - `tokio::time::sleep(Duration::from_secs(delay))`
          - Increment retry_count
          - Resubmit to webhook_queue
          - If retry_count >= 3 → delete from DLQ (permanent failure)

    4. Implement graceful shutdown via tokio::select! with Ctrl+C

    Update `rust_control_plane/src/dlq/mod.rs`:
    1. Re-export: `pub use retry_worker::start_retry_worker;`

    Update `rust_control_plane/src/main.rs`:
    1. Call `dlq::start_retry_worker(&pool, &webhook_queue).await`
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test test_retry_worker_backoff -- --nocapture</automated>
  </verify>
  <done>
    Retry worker compiles and runs. Tests verify exponential backoff: 1s, 5s, 30s. After 3 retries, webhook is permanently failed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate DLQ into webhook worker</name>
  <files>rust_control_plane/src/queue/worker.rs</files>
  <behavior>
    - Webhook worker catches errors from AI processing
    - On error: increment retry counter
    - If retry_count < 3 → apply backoff and retry
    - If retry_count >= 3 → call move_to_dlq()
    - DLQ recovery rate > 80% measured in tests
  </behavior>
  <action>
    Create `rust_control_plane/src/queue/worker.rs`:
    1. Define `WebhookWorker` struct with PgPool, WebhookQueue, DLQ
    2. Implement `start_worker()` → tokio::spawn consumer loop
    3. Consumer loop:
       a. `webhook_queue.try_recv()` → non-blocking
       b. If event received:
          - Update messages status: 'processing'
          - Send to Python AI worker via gRPC
          - On success → update status: 'completed'
          - On error → handle_retry_or_dlq()
    4. Implement `handle_retry_or_dlq()`:
       - Get current retry_count from messages table
       - If retry_count < 3:
         * Calculate backoff: [1s, 5s, 30s][retry_count]
         * tokio::time::sleep(delay)
         * Increment retry_count
         * Re-queue event
       - If retry_count >= 3:
         * Call dlq::move_to_dlq(event, error)
         * Update messages status: 'failed'

    Update `rust_control_plane/src/main.rs`:
    1. Call `queue::worker::start_worker(&pool, &webhook_queue, &dlq).await`
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test test_dlq_recovery_rate -- --nocapture</automated>
  </verify>
  <done>
    Worker integration compiles. DLQ recovery rate > 80% verified in tests. Failed webhooks permanently moved to DLQ after 3 retries.
  </done>
</task>

</tasks>

<verification>
- [ ] DLQ table created with all required columns
- [ ] move_to_dlq() inserts failed webhooks
- [ ] Retry worker runs every 30 seconds
- [ ] Exponential backoff: 1s → 5s → 30s → DLQ
- [ ] Webhook worker moves to DLQ after 3 retries
- [ ] DLQ recovery rate > 80% (measured in tests)
- [ ] All tests pass: `cargo test --test dlq_test`
</verification>

<success_criteria>
Dead Letter Queue is fully implemented with exponential backoff retry strategy. Failed webhooks are retried 3 times with increasing delays (1s, 5s, 30s) before permanent failure. Manual retry capability exists for operators. Brain #7 Condition #6 (DLQ Retry Backoff Strategy) is fully implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/18-multi-channel-gateway/18-02-SUMMARY.md`
</output>
