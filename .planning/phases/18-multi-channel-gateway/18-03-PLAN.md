---
phase: 18-multi-channel-gateway
plan: 03
type: execute
wave: 1
depends_on: [18-01, 18-02]
files_modified:
  - rust_control_plane/src/observability/latency.rs
  - rust_control_plane/src/metrics/latency.rs
  - rust_control_plane/tests/latency_test.rs
autonomous: true
requirements:
  - MCG-01
user_setup: []

must_haves:
  truths:
    - "End-to-end latency is measured from webhook received → AI response sent (Brain #7 Condition #3)"
    - "SLI: webhook_e2e_latency_seconds{channel,quantile} < 30s P95"
    - "Latency histogram includes all phases: queue, AI processing, channel send"
    - "Alert fires if P95 > 30s for any channel"
  artifacts:
    - path: "rust_control_plane/src/observability/latency.rs"
      provides: "Latency tracking middleware"
      exports: ["LatencyTracker", "start_timer", "record_latency"]
      min_lines: 60
    - path: "rust_control_plane/src/metrics/latency.rs"
      provides: "Prometheus histogram for E2E latency"
      exports: ["WEBHOOK_E2E_LATENCY_SECONDS"]
      min_lines: 40
    - path: "rust_control_plane/tests/latency_test.rs"
      provides: "Latency measurement tests"
      min_lines: 50
  key_links:
    - from: "rust_control_plane/src/handlers/webhook.rs"
      to: "rust_control_plane/src/observability/latency.rs"
      via: "start_timer() on webhook received"
      pattern: "let timer = latency_tracker\\.start_timer\\(&trace_id, &channel\\)"
    - from: "rust_control_plane/src/queue/worker.rs"
      to: "rust_control_plane/src/observability/latency.rs"
      via: "record_latency() on AI response sent"
      pattern: "latency_tracker\\.record_latency\\(&trace_id, &channel, duration\\)"
    - from: "rust_control_plane/src/metrics/latency.rs"
      to: "/metrics endpoint"
      via: "Histogram.observe()"
      pattern: "WEBHOOK_E2E_LATENCY_SECONDS\\.observe\\(duration\\.as_secs_f64\\(\\)"
---

<objective>
Implement end-to-end latency SLI measurement (Brain #7 Condition #3)
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-multi-channel-gateway/18-CONTEXT.md
@.planning/phases/18-multi-channel-gateway/18-BRAIN7-EVALUATION.md
@.planning/phases/18-multi-channel-gateway/18-01-PLAN.md
@.planning/phases/18-multi-channel-gateway/18-02-PLAN.md
@rust_control_plane/src/metrics/prometheus.rs
</context>

<interfaces>
<!-- From rust_control_plane/src/metrics/prometheus.rs -->
```rust
use prometheus::{IntGauge, Registry, Opts};

pub struct Metrics {
    pub registry: Registry,
}

lazy_static! {
    pub static ref METRICS: Metrics = Metrics::new();
}
```

<!-- From rust_control_plane/src/tracing/interceptor.rs (Phase 16) -->
```rust
pub struct TraceIdInterceptor {
    // trace_id propagation logic
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create latency tracking module</name>
  <files>rust_control_plane/src/observability/latency.rs, rust_control_plane/src/observability/mod.rs</files>
  <behavior>
    - LatencyTracker struct stores start times by trace_id
    - start_timer() records start time with trace_id + channel
    - record_latency() calculates duration and updates histogram
    - cleanup_timer() removes old entries (>5 minutes)
  </behavior>
  <action>
    Create `rust_control_plane/src/observability/mod.rs`:
    1. Add `pub mod latency;`

    Create `rust_control_plane/src/observability/latency.rs`:
    1. Define `LatencyEntry` struct: trace_id (String), channel (String), start_time (Instant)
    2. Define `LatencyTracker` struct with DashMap<String, LatencyEntry>
    3. Implement `new()` → empty DashMap
    4. Implement `start_timer(trace_id, channel)` → inserts entry with Instant::now()
    5. Implement `record_latency(trace_id, channel)` → calculates duration, removes entry, returns Duration
    6. Implement `cleanup_timer(trace_id)` → removes entry if exists (for failed webhooks)
    7. Implement `cleanup_old_entries()` → removes entries > 5 minutes (called every 60s)

    Update `rust_control_plane/src/lib.rs`:
    1. Add `pub mod observability;`
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test test_latency_tracker -- --nocapture</automated>
  </verify>
  <done>
    LatencyTracker compiles. Tests verify start_timer, record_latency, and cleanup operations.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Prometheus histogram for E2E latency</name>
  <files>rust_control_plane/src/metrics/latency.rs, rust_control_plane/src/metrics/prometheus.rs</files>
  <behavior>
    - Histogram metric: webhook_e2e_latency_seconds{channel,quantile}
    - Buckets: [0.1s, 0.5s, 1s, 5s, 10s, 20s, 30s, 60s, 120s]
    - Exposed at /metrics endpoint
    - P95 < 30s is success threshold
  </behavior>
  <action>
    Create `rust_control_plane/src/metrics/latency.rs`:
    1. Import prometheus::{Histogram, HistogramOpts, Registry}
    2. Create Histogram:
       ```rust
       lazy_static! {
           pub static ref WEBHOOK_E2E_LATENCY_SECONDS: Histogram = Histogram::with_opts(
               HistogramOpts::new(
                   "webhook_e2e_latency_seconds",
                   "End-to-end latency from webhook received to AI response sent"
               )
               .buckets(vec![0.1, 0.5, 1.0, 5.0, 10.0, 20.0, 30.0, 60.0, 120.0])
               .const_label("channel", "all")
           ).unwrap()
       }
       ```
    3. Implement `record_e2e_latency(channel, duration)` → `WEBHOOK_E2E_LATENCY_SECONDS.with_label_values(&[channel]).observe(duration.as_secs_f64())`

    Update `rust_control_plane/src/metrics/prometheus.rs`:
    1. Register histogram: `registry.register(Box::new(WEBHOOK_E2E_LATENCY_SECONDS.clone()))`
    2. Export `latency` module

    Update `rust_control_plane/src/metrics/mod.rs`:
    1. Add `pub use latency::record_e2e_latency;`
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test test_e2e_latency_metric -- --nocapture</automated>
  </verify>
  <done>
    Histogram registered at /metrics. Metric shows webhook_e2e_latency_seconds with buckets. P95 calculation works.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate latency tracking into webhook flow</name>
  <files>rust_control_plane/src/handlers/webhook.rs, rust_control_plane/src/queue/worker.rs</files>
  <behavior>
    - Webhook receiver calls start_timer() with trace_id + channel
    - Worker calls record_latency() when AI response is sent
    - Worker calls cleanup_timer() if webhook fails
    - End-to-end latency < 30s P95 verified in tests
  </behavior>
  <action>
    Update `rust_control_plane/src/handlers/webhook.rs`:
    1. Generate trace_id: `uuid::Uuid::new_v4().to_string()`
    2. After duplicate check, call: `latency_tracker.start_timer(&trace_id, &channel)`
    3. Pass trace_id in WebhookEvent

    Update `rust_control_plane/src/queue/worker.rs`:
    1. On successful AI response:
       - Call `duration = latency_tracker.record_latency(&trace_id, &channel)`
       - Call `metrics::record_e2e_latency(&channel, duration)`
    2. On failed webhook (moved to DLQ):
       - Call `latency_tracker.cleanup_timer(&trace_id)`

    Update `rust_control_plane/src/main.rs`:
    1. Initialize LatencyTracker: `let latency_tracker = Arc::new(LatencyTracker::new())`
    2. Pass to webhook handler and worker
    3. Spawn cleanup task: `tokio::spawn(async move { latency_tracker.cleanup_old_entries().await })`
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test test_e2e_latency_p95_threshold -- --nocapture</automated>
  </verify>
  <done>
    End-to-end latency tracked from webhook to AI response. P95 < 30s confirmed in tests. Histogram exposed at /metrics.
  </done>
</task>

</tasks>

<verification>
- [ ] LatencyTracker stores start times by trace_id
- [ ] Histogram metric: webhook_e2e_latency_seconds{channel}
- [ ] Webhook receiver calls start_timer()
- [ ] Worker calls record_latency() on success
- [ ] Worker calls cleanup_timer() on failure
- [ ] P95 < 30s verified in tests
- [ ] All tests pass: `cargo test --test latency_test`
</verification>

<success_criteria>
End-to-end latency SLI is fully implemented. Latency is measured from webhook received → AI response sent. Histogram metric is exposed at /metrics with P95 < 30s threshold. Brain #7 Condition #3 (End-to-End Latency SLI) is fully implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/18-multi-channel-gateway/18-03-SUMMARY.md`
</output>
