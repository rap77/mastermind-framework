---
phase: 18-multi-channel-gateway
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rust_control_plane/src/handlers/webhook.rs
  - rust_control_plane/src/queue/mod.rs
  - rust_control_plane/src/queue/worker.rs
  - rust_control_plane/src/metrics/queue.rs
  - rust_control_plane/tests/webhook_test.rs
autonomous: true
requirements:
  - MCG-01
user_setup:
  - service: whatsapp
    why: "Webhook verification requires app secret"
    env_vars:
      - name: WHATSAPP_WEBHOOK_VERIFY_TOKEN
        source: "Meta for Developers → WhatsApp → Configuration"
      - name: WHATSAPP_APP_SECRET
        source: "Meta for Developers → WhatsApp → App Settings"
  - service: instagram
    why: "Webhook signature verification"
    env_vars:
      - name: INSTAGRAM_APP_SECRET
        source: "Meta for Developers → Instagram → App Settings"

must_haves:
  truths:
    - "Webhooks from WhatsApp/Instagram are received and verified within 100ms P95"
    - "Duplicate webhooks are rejected via UNIQUE constraint (no double processing)"
    - "Queue depth is monitored via Prometheus metric"
    - "Webhooks are rejected at 90% queue capacity (503 response)"
    - "SRE alert fires at 75% queue capacity"
  artifacts:
    - path: "rust_control_plane/src/handlers/webhook.rs"
      provides: "Webhook receiver with HMAC verification"
      exports: ["webhook_receiver", "verify_hmac_signature"]
      min_lines: 120
    - path: "rust_control_plane/src/queue/mod.rs"
      provides: "tokio::sync::mpsc queue with depth monitoring"
      exports: ["WebhookQueue", "queue_depth_percent"]
      min_lines: 80
    - path: "rust_control_plane/src/metrics/queue.rs"
      provides: "Prometheus metrics for queue depth"
      exports: ["webhook_queue_depth_percent", "webhook_queue_capacity"]
      min_lines: 40
    - path: "rust_control_plane/migrations/003_add_messages_table.sql"
      provides: "Messages table with UNIQUE constraint"
      contains: "UNIQUE (external_message_id, channel)"
      min_lines: 25
    - path: "rust_control_plane/tests/webhook_test.rs"
      provides: "Unit tests for webhook flow"
      min_lines: 80
  key_links:
    - from: "rust_control_plane/src/handlers/webhook.rs"
      to: "rust_control_plane/src/queue/mod.rs"
      via: "webhook_queue.send()"
      pattern: "webhook_queue\\.send\\(WebhookEvent"
    - from: "rust_control_plane/src/handlers/webhook.rs"
      to: "rust_control_plane/migrations/003_add_messages_table.sql"
      via: "is_duplicate() query"
      pattern: "SELECT.*WHERE external_message_id.*AND channel"
    - from: "rust_control_plane/src/metrics/queue.rs"
      to: "/metrics endpoint"
      via: "prometheus::Registry::register()"
      pattern: "registry\\.register\\(WEBHOOK_QUEUE_DEPTH"
    - from: "queue_depth_percent"
      to: "503 response"
      via: "depth check before send"
      pattern: "if queue_depth_percent\\(\\) > 90 \\{ return StatusCode::SERVICE_UNAVAILABLE"
---

<objective>
Implement webhook receiver with idempotency, queue depth monitoring, and Brain #7 Condition #2 (Queue Depth Monitoring)
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-multi-channel-gateway/18-CONTEXT.md
@.planning/phases/18-multi-channel-gateway/18-BRAIN7-EVALUATION.md
@rust_control_plane/src/handlers/mod.rs
@rust_control_plane/src/metrics/prometheus.rs
@rust_control_plane/src/state.rs
</context>

<interfaces>
<!-- From rust_control_plane/src/handlers/mod.rs -->
```rust
pub mod health;
pub mod auth;
pub mod audit;
```

<!-- From rust_control_plane/src/metrics/prometheus.rs -->
```rust
use prometheus::{IntGauge, Registry,Opts};

pub struct Metrics {
    pub registry: Registry,
}

lazy_static! {
    pub static ref METRICS: Metrics = Metrics::new();
}
```

<!-- From rust_control_plane/src/state.rs -->
```rust
use sqlx::PgPool;
use std::sync::Arc;

#[derive(Clone)]
pub struct AppState {
    pub db: PgPool,
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create messages table with UNIQUE constraint</name>
  <files>rust_control_plane/migrations/003_add_messages_table.sql</files>
  <behavior>
    - Table `messages` exists with columns: id (UUID), external_message_id (TEXT), channel (TEXT), payload (JSONB), status (TEXT), created_at (TIMESTAMP)
    - UNIQUE constraint on (external_message_id, channel) prevents duplicates
    - Index on (external_message_id, channel) for fast duplicate detection
    - Status enum: pending, processing, completed, failed
  </behavior>
  <action>
    Create migration file:
    1. Add `messages` table with UUID primary key
    2. Add `external_message_id` TEXT column (not null)
    3. Add `channel` TEXT column (not null) — values: 'whatsapp', 'instagram', 'email'
    4. Add UNIQUE constraint: `UNIQUE (external_message_id, channel)`
    5. Add index: `CREATE INDEX idx_messages_external_channel ON messages(external_message_id, channel)`
    6. Add `payload` JSONB column (not null)
    7. Add `status` TEXT column with DEFAULT 'pending'
    8. Add `created_at` TIMESTAMP with DEFAULT NOW()
    9. Add `updated_at` TIMESTAMP with DEFAULT NOW()

    Run migration: `sqlx migrate run`
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test --test webhook_test test_messages_table_unique_constraint -- --nocapture</automated>
  </verify>
  <done>
    Migration file created and applied. Table structure verified via \d messages in psql.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement webhook queue with depth monitoring (Brain #7 Condition #2)</name>
  <files>rust_control_plane/src/queue/mod.rs, rust_control_plane/src/metrics/queue.rs</files>
  <behavior>
    - WebhookQueue exists with bounded channel (capacity: 1000)
    - queue_depth_percent() returns 0-100 value
    - Prometheus metric `webhook_queue_depth_percent{channel="all"}` is exposed
    - Alert at 75% capacity (test: metric >= 75)
    - Reject webhooks at 90% capacity (test: returns error)
  </behavior>
  <action>
    Create `rust_control_plane/src/queue/mod.rs`:
    1. Define `WebhookEvent` struct: channel (String), payload (serde_json::Value), trace_id (String)
    2. Define `WebhookQueue` struct with tokio::sync::mpsc::channel (capacity: 1000)
    3. Implement `queue_depth_percent()` → calculates (sender.len() / capacity) * 100
    4. Implement `send_with_backpressure()` → rejects if depth > 90%
    5. Implement `try_recv()` → non-blocking receive for worker

    Create `rust_control_plane/src/metrics/queue.rs`:
    1. Add IntGauge: `webhook_queue_depth_percent{channel="all"}`
    2. Add IntGauge: `webhook_queue_capacity`
    3. Register metrics with prometheus::Registry
    4. Update function to sync queue depth to metrics every 100ms

    Update `rust_control_plane/src/metrics/mod.rs`:
    1. Export `queue` module
    2. Call `queue::start_metrics_updater()` in Metrics::new()
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test test_queue_depth_monitoring -- --nocapture</automated>
  </verify>
  <done>
    Queue module compiles. Metric `/metrics` shows webhook_queue_depth_percent. Test verifies rejection at 90%.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement webhook receiver with HMAC verification</name>
  <files>rust_control_plane/src/handlers/webhook.rs, rust_control_plane/src/handlers/mod.rs</files>
  <behavior>
    - POST /webhooks/{channel} endpoint exists
    - HMAC signature verification (X-Hub-Signature-256 for WhatsApp, X-Hub-Signature for Instagram)
    - Duplicate detection via UNIQUE constraint query
    - Idempotent: 204 No Content on duplicate, 200 OK on new
    - Webhook ACK < 100ms P95 (measured in test)
    - Returns 503 Service Unavailable if queue depth > 90%
  </behavior>
  <action>
    Create `rust_control_plane/src/handlers/webhook.rs`:
    1. Add `webhook_receiver` Axum handler: Path(channel), Json(payload), HeaderMap(headers)
    2. Extract HMAC signature from headers (X-Hub-Signature-256 or X-Hub-Signature)
    3. Verify signature using HMAC_SHA256 (use WHATSAPP_APP_SECRET or INSTAGRAM_APP_SECRET env var)
    4. Extract external_message_id from payload (WhatsApp: entry[0].changes[0].value.messages[0].id, Instagram: changes[0].value.id)
    5. Query messages table: `SELECT id FROM messages WHERE external_message_id = $1 AND channel = $2`
    6. If exists → return 204 No Content (idempotent)
    7. If not exists → insert to messages table (status: pending)
    8. Push to queue: `webhook_queue.send(WebhookEvent).await`
    9. Return 200 OK with trace_id

    Add to `rust_control_plane/src/handlers/mod.rs`:
    1. Add `pub mod webhook;`
    2. Export `webhook::webhook_receiver`

    Update `rust_control_plane/src/main.rs`:
    1. Register route: `app.route("/webhooks/:channel", post(webhook_receiver))`

    Env vars needed:
    - WHATSAPP_APP_SECRET (from Meta for Developers)
    - INSTAGRAM_APP_SECRET (from Meta for Developers)
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test test_webhook_receiver_ack_latency -- --nocapture</automated>
  </verify>
  <done>
    Webhook endpoint responds to POST. HMAC verification works. Duplicate webhooks return 204. Queue rejection at 90% works. P95 latency < 100ms confirmed via test.
  </done>
</task>

</tasks>

<verification>
- [ ] Migration creates messages table with UNIQUE constraint
- [ ] Queue depth metric exposed at /metrics
- [ ] Webhook receiver accepts POST /webhooks/{channel}
- [ ] HMAC verification works (WhatsApp + Instagram)
- [ ] Duplicate detection returns 204 No Content
- [ ] Queue rejection at 90% returns 503
- [ ] Webhook ACK P95 < 100ms (measured in tests)
- [ ] All tests pass: `cargo test --test webhook_test`
</verification>

<success_criteria>
Webhooks from WhatsApp and Instagram are received, verified for authenticity, checked for duplicates, and queued for processing. Queue depth is monitored and webhooks are rejected at 90% capacity. Brain #7 Condition #2 (Queue Depth Monitoring) is fully implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/18-multi-channel-gateway/18-01-SUMMARY.md`
</output>
