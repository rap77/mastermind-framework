---
phase: 18-multi-channel-gateway
plan: 05
type: execute
wave: 2
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - rust_control_plane/src/channels/instagram.rs
  - rust_control_plane/tests/instagram_test.rs
  - apps/api/routers/instagram.py
  - apps/api/tests/test_instagram.py
autonomous: true
requirements:
  - MCG-01
user_setup:
  - service: instagram
    why: "Instagram Graph API requires access token"
    env_vars:
      - name: INSTAGRAM_BUSINESS_ACCOUNT_ID
        source: "Meta for Developers → Instagram → Accounts"
      - name: INSTAGRAM_ACCESS_TOKEN
        source: "Meta for Developers → Instagram → Graph API Explorer"

must_haves:
  truths:
    - "Instagram webhooks (comments, mentions) are received and verified"
    - "Instagram messages are sent via Instagram Graph API"
    - "Media attachments (photos, videos) are handled"
    - "Comment threading is preserved"
  artifacts:
    - path: "rust_control_plane/src/channels/instagram.rs"
      provides: "Instagram webhook payload parser"
      exports: ["parse_instagram_webhook", "extract_comment_id", "extract_media_url"]
      min_lines: 80
    - path: "apps/api/routers/instagram.py"
      provides: "Instagram message sender API"
      exports: ["send_instagram_comment", "send_instagram_direct"]
      min_lines: 100
    - path: "rust_control_plane/tests/instagram_test.rs"
      provides: "Instagram webhook parsing tests"
      min_lines: 60
    - path: "apps/api/tests/test_instagram.py"
      provides: "Instagram send API tests"
      min_lines: 50
  key_links:
    - from: "rust_control_plane/src/handlers/webhook.rs"
      to: "rust_control_plane/src/channels/instagram.rs"
      via: "parse_instagram_webhook() for channel='instagram'"
      pattern: "if channel == \"instagram\" \\{ instagram::parse_instagram_webhook\\(payload\\)"
    - from: "apps/api/routers/instagram.py"
      to: "Instagram Graph API"
      via: "POST https://graph.facebook.com/v19.0/{COMMENT_ID}/replies"
      pattern: "requests\\.post\\(.*instagram.*replies"
    - from: "media webhook"
      to: "S3 storage"
      via: "download and store media URL"
      pattern: "s3_client\\.upload_file\\(media_url"
---

<objective>
Implement Instagram Graph API adapter
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-multi-channel-gateway/18-CONTEXT.md
@.planning/phases/18-multi-channel-gateway/18-01-PLAN.md
@rust_control_plane/src/handlers/webhook.rs
@rust_control_plane/src/channels/whatsapp.rs
</context>

<interfaces>
<!-- From rust_control_plane/src/channels/whatsapp.rs (18-04 output) -->
```rust
pub struct MessagePayload {
    pub message_id: String,
    pub sender: String,
    pub recipient: String,
    pub message_type: String,  // text, image, audio, document
    pub content: String,
    pub media_url: Option<String>,
    pub timestamp: i64,
}
```

<!-- From rust_control_plane/src/handlers/webhook.rs (18-01 output) -->
```rust
pub async fn webhook_receiver(
    Path(channel): Path<String>,
    Json(payload): Json<serde_json::Value>,
) -> Result<StatusCode, Error>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Instagram webhook payload parser</name>
  <files>rust_control_plane/src/channels/instagram.rs</files>
  <behavior>
    - parse_instagram_webhook() extracts: comment_id, media_id, username, comment_text, media_url
    - extract_comment_id() returns changes[0].value.id
    - extract_media_url() returns changes[0].value.media.image_url
    - Returns standardized InstagramComment struct
    - Handles both comments and direct messages
  </behavior>
  <action>
    Create `rust_control_plane/src/channels/instagram.rs`:
    1. Define `InstagramComment` struct: comment_id, media_id, username, comment_text, media_url, timestamp, parent_comment_id (for threading)
    2. Implement `parse_instagram_webhook(payload: Value)` → Result<InstagramComment, Error>
       - Extract: payload["changes"][0]["value"]
       - Parse comment_id, media_id, from (username), message (text)
       - Extract media_url if available
       - Handle parent_comment_id for replies
       - Return standardized InstagramComment
    3. Implement `extract_comment_id(payload)` → Result<String, Error>
    4. Implement `extract_media_url(payload)` → Result<String, Error>
    5. Implement `is_comment_webhook(payload)` → bool (filter out likes, follows)

    Update `rust_control_plane/src/channels/mod.rs`:
    1. Add `pub mod instagram;`
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test test_instagram_webhook_parser -- --nocapture</automated>
  </verify>
  <done>
    Instagram webhook parser compiles. Tests verify extraction of comment_id, username, comment_text, and media URLs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Instagram message sender (Python)</name>
  <files>apps/api/routers/instagram.py, apps/api/tests/test_instagram.py</files>
  <behavior>
    - send_instagram_comment() posts comments via Instagram Graph API
    - send_instagram_direct() sends direct messages
    - Uses INSTAGRAM_ACCESS_TOKEN for authentication
    - Returns comment_id from API response
    - Handles media comments (image replies)
  </behavior>
  <action>
    Create `apps/api/routers/instagram.py`:
    1. Define `InstagramComment` pydantic model: media_id, comment_text, access_token
    2. Define `send_instagram_comment(comment: InstagramComment)` → dict
       - GET INSTAGRAM_BUSINESS_ACCOUNT_ID from env
       - POST to: `https://graph.facebook.com/v19.0/{INSTAGRAM_BUSINESS_ACCOUNT_ID}/comments`
       - Headers: `Authorization: Bearer {INSTAGRAM_ACCESS_TOKEN}`
       - Body: {"message": comment_text, "attachment_id": media_id (if media)}
       - Return {"comment_id": response["id"]}
    3. Define `send_instagram_direct(recipient_id, message_text)` → dict
       - POST to: `https://graph.facebook.com/v19.0/me/messages`
       - Body includes recipient_id and message
       - Return {"message_id": response["messages"][0]["id"]}
    4. Implement error handling for 4xx/5xx responses
    5. Add route: `@router.post("/api/channels/instagram/send")`

    Create `apps/api/tests/test_instagram.py`:
    1. Mock HTTPX client for Instagram API
    2. Test send_instagram_comment() with text
    3. Test send_instagram_comment() with media
    4. Test send_instagram_direct()
    5. Test error handling (401, 404, 500)
    6. Verify comment_id returned correctly

    Update `apps/api/main.py`:
    1. Include instagram router
  </action>
  <verify>
    <automated>cd apps/api && uv run pytest tests/test_instagram.py -v</automated>
  </verify>
  <done>
    Instagram sender API compiles. Tests verify comment and direct message sending. Error handling works for 4xx/5xx responses.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate Instagram adapter into webhook flow</name>
  <files>rust_control_plane/src/handlers/webhook.rs, rust_control_plane/src/queue/worker.rs</files>
  <behavior>
    - Webhook receiver uses instagram parser for channel='instagram'
    - Worker sends to Python /api/channels/instagram/send endpoint
    - Comment threading is preserved (parent_comment_id)
    - Media attachments are downloaded and stored
  </behavior>
  <action>
    Update `rust_control_plane/src/handlers/webhook.rs`:
    1. After channel extraction, call: `channels::instagram::parse_instagram_webhook(payload)`
    2. Store parsed InstagramComment in WebhookEvent
    3. Handle comment replies separately (preserve threading)

    Update `rust_control_plane/src/queue/worker.rs`:
    1. On processing webhook:
       - Extract InstagramComment from WebhookEvent
       - Call Python gRPC endpoint: `/api/channels/instagram/send`
       - Pass: media_id, comment_text, parent_comment_id
    2. Handle media attachments:
       - Download media_url to S3/local storage
       - Store S3 path in messages table
    3. Preserve comment threading:
       - If parent_comment_id exists → reply to comment
       - Else → top-level comment

    Update `rust_control_plane/tests/instagram_test.rs`:
    1. Test end-to-end webhook → send flow
    2. Test comment threading (replies)
    3. Test media attachment handling
  </action>
  <verify>
    <automated>cd rust_control_plane && cargo test test_instagram_end_to_end -- --nocapture</automated>
  </verify>
  <done>
    Instagram adapter integrated. Webhook → send flow works. Comment threading preserved. Media attachments stored.
  </done>
</task>

</tasks>

<verification>
- [ ] Instagram webhook parser extracts all fields
- [ ] send_instagram_comment() posts comments
- [ ] send_instagram_direct() sends DMs
- [ ] Comment threading preserved
- [ ] Media attachments stored
- [ ] End-to-end webhook → send flow works
- [ ] All tests pass: `cargo test --test instagram_test` and `pytest apps/api/tests/test_instagram.py`
</verification>

<success_criteria>
Instagram Graph API adapter is fully implemented. Webhooks are received, parsed, and messages are sent via Instagram API. Comment threading is preserved. Media attachments are supported.
</success_criteria>

<output>
After completion, create `.planning/phases/18-multi-channel-gateway/18-05-SUMMARY.md`
</output>
