---
phase: 18-multi-channel-gateway
plan: 07
type: execute
wave: 3
depends_on: [18-01, 18-02, 18-03, 18-04, 18-05, 18-06]
files_modified:
  - apps/web/src/stores/messageStore.ts
  - apps/web/src/components/messaging/UnifiedInboxPage.tsx
  - apps/web/src/components/messaging/ChannelRail.tsx
  - apps/web/src/components/messaging/ThreadList.tsx
  - apps/web/src/components/messaging/ThreadDetail.tsx
  - apps/web/src/components/messaging/messages/WhatsAppMessage.tsx
  - apps/web/src/components/messaging/messages/InstagramMessage.tsx
  - apps/web/src/components/messaging/messages/EmailMessage.tsx
autonomous: false
requirements:
  - MCG-01
user_setup: []

must_haves:
  truths:
    - "Users see unified inbox across WhatsApp, Instagram, Email (3-pane layout)"
    - "Message list render time (1000 msgs) < 100ms (Brain #7 Frontend condition)"
    - "LocalStorage quota monitoring: alert at 80%, block at 90% (Brain #7 Condition #4)"
    - "Manual thread merge UI: select 2+ threads → 'Merge' action (Brain #7 Condition #1)"
    - "Channel-specific message components (WhatsApp, Instagram, Email)"
    - "WebSocket integration: real-time message updates"
    - "Keyboard navigation: J/K for next/prev thread"
  artifacts:
    - path: "apps/web/src/stores/messageStore.ts"
      provides: "Message state management (Zustand + Immer + persist)"
      exports: ["useMessageStore", "addMessage", "updateMessageStatus"]
      min_lines: 120
    - path: "apps/web/src/components/messaging/UnifiedInboxPage.tsx"
      provides: "3-pane inbox layout (ChannelRail + ThreadList + ThreadDetail)"
      min_lines: 80
    - path: "apps/web/src/components/messaging/ChannelRail.tsx"
      provides: "Channel selector (whatsapp | instagram | email)"
      min_lines: 40
    - path: "apps/web/src/components/messaging/ThreadList.tsx"
      provides: "Virtualized thread list (react-virtuoso)"
      min_lines: 60
    - path: "apps/web/src/components/messaging/ThreadDetail.tsx"
      provides: "Active thread with message composer"
      min_lines: 80
    - path: "apps/web/src/components/messaging/messages/WhatsAppMessage.tsx"
      provides: "WhatsApp-specific message bubble"
      min_lines: 40
    - path: "apps/web/src/components/messaging/messages/InstagramMessage.tsx"
      provides: "Instagram-specific message bubble"
      min_lines: 40
    - path: "apps/web/src/components/messaging/messages/EmailMessage.tsx"
      provides: "Email-specific message bubble with subject"
      min_lines: 50
  key_links:
    - from: "messageStore.ts"
      to: "localStorage"
      via: "zustand persist middleware (drafts only)"
      pattern: "persist\\(\\{ name: 'mastermind-messages', partialize: \\(state\\) => \\(\\{ drafts: state.drafts \\}\\) \\}\\)"
    - from: "UnifiedInboxPage.tsx"
      to: "messageStore"
      via: "useMessageStore() hook"
      pattern: "const \\{ messages, drafts, addMessage \\} = useMessageStore\\(\\)"
    - from: "ThreadList.tsx"
      to: "react-virtuoso"
      via: "Virtuoso component for virtualization"
      pattern: "<Virtuoso.*data=\\{threads\\}"
    - from: "WebSocket updates"
      to: "messageStore"
      via: "wsDispatcher.subscribe('thread_updates')"
      pattern: "wsDispatcher\\.subscribe\\('thread_updates', \\(event\\) =>"
    - from: "localStorage quota monitoring"
      to: "alert/block logic"
      via: "quota check on saveDraft"
      pattern: "if \\(quota_percent > 90\\) \\{ block_persistence\\(\\)"
---

<objective>
Implement unified inbox UI with LocalStorage quota monitoring (Brain #7 Conditions #1, #4, #5)
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-multi-channel-gateway/18-CONTEXT.md
@.planning/phases/18-multi-channel-gateway/18-BRAIN7-EVALUATION.md
@apps/web/src/stores/costStore.ts
@apps/web/src/stores/brainStore.ts
@apps/web/package.json
</context>

<interfaces>
<!-- From apps/web/src/stores/costStore.ts (pattern reference) -->
```typescript
import { create } from 'zustand'
import { immer } from 'zustand/middleware/immer'
import { persist } from 'zustand/middleware'

interface CostStore {
  costs: Map<string, BrainCost>
  addCost: (brainId: string, cost: BrainCost) => void
}

export const useCostStore = create<CostStore>()(
  immer(),
  persist({
    name: 'mastermind-costs',
    partialize: (state) => ({ costs: state.costs }),
  })
)
```

<!-- From apps/web/src/stores/brainStore.ts (RAF batching pattern) -->
```typescript
// Lines 44-66
messageStore.subscribe((state, previousState) => {
  state.messages.forEach((msg) => {
    if (!previousState.messages.has(msg.id)) {
      eventQueue.push({ type: 'MESSAGE_ADDED', message: msg })
    }
  })
})
```

<!-- From apps/web/package.json (confirmed dependencies) -->
```json
"react-virtuoso": "^4.18.3",
"zustand": "^5.0.12",
"immer": "^11.1.4",
"dompurify": "^3.3.3"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create messageStore with LocalStorage quota monitoring (Brain #7 Condition #4)</name>
  <files>apps/web/src/stores/messageStore.ts, apps/web/src/stores/__tests__/messageStore.test.ts</files>
  <behavior>
    - messageStore manages messages (Map<id, MessageState>) and drafts (Map<channelId, MessageDraft>)
    - addMessage() adds new message to Map
    - updateMessageStatus() updates message status (sent, delivered, read)
    - saveDraft() persists draft to localStorage with quota check
    - LocalStorage quota monitored: alert at 80%, block at 90%
    - O(1) targeted selector: useMessage(id)
  </behavior>
  <action>
    Create `apps/web/src/stores/messageStore.ts`:
    1. Define `MessageState` interface: id, channel (whatsapp|instagram|email), sender, content, media_url, status, timestamp, thread_id
    2. Define `MessageDraft` interface: channel_id, content, media_attachments
    3. Define `MessageStore` interface:
       - messages: Map<string, MessageState>
       - drafts: Map<string, MessageDraft>
       - localStorage_quota_percent: number
       - useMessage(id: string) → MessageState | undefined (O(1) selector)
       - addMessage(message: MessageState) → void
       - updateMessageStatus(id: string, status: string) → void
       - saveDraft(channelId: string, draft: MessageDraft) → void
       - checkLocalStorageQuota() → number (returns 0-100)
    4. Implement quota monitoring:
       - calculateQuotaPercent() → (JSON.stringify(localStorage).length / quota_limit) * 100
       - saveDraft() checks quota before persisting
       - Alert at 80% (toast notification)
       - Block at 90% (return early, show warning)
    5. Use immer() for immutable updates
    6. Use persist() middleware (partialize: drafts only)
    7. Implement RAF batching (extend brainStore pattern lines 44-66)

    Create `apps/web/src/stores/__tests__/messageStore.test.ts`:
    1. Test addMessage() adds to Map
    2. Test updateMessageStatus() updates status
    3. Test saveDraft() persists to localStorage
    4. Test quota monitoring: alert at 80%, block at 90%
    5. Test useMessage() O(1) selector
    6. Test RAF batching
  </action>
  <verify>
    <automated>pnpm --prefix apps/web test run messageStore</automated>
  </verify>
  <done>
    messageStore compiles. Tests verify Map operations, quota monitoring (80% alert, 90% block), and O(1) selector.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create 3-pane inbox layout (ChannelRail + ThreadList + ThreadDetail)</name>
  <files>apps/web/src/components/messaging/UnifiedInboxPage.tsx, apps/web/src/components/messaging/ChannelRail.tsx, apps/web/src/components/messaging/ThreadList.tsx, apps/web/src/components/messaging/ThreadDetail.tsx</files>
  <behavior>
    - UnifiedInboxPage renders 3-pane layout (ChannelRail | ThreadList | ThreadDetail)
    - ChannelRail shows channel icons (whatsapp, instagram, email) with unread badges
    - ThreadList uses react-virtuoso for virtualization (1000+ messages < 100ms)
    - ThreadDetail shows active thread with message composer
    - Keyboard navigation: J/K for next/prev thread
  </behavior>
  <action>
    Create `apps/web/src/components/messaging/UnifiedInboxPage.tsx`:
    1. Layout: Grid with 3 columns (60px | 300px | 1fr)
    2. State: selectedChannel (default: 'all'), selectedThreadId (default: null)
    3. Render: ChannelRail, ThreadList, ThreadDetail
    4. Add keyboard listener: J (next thread), K (prev thread)
    5. WebSocket integration: wsDispatcher.subscribe('thread_updates')
    6. Route: /messaging

    Create `apps/web/src/components/messaging/ChannelRail.tsx`:
    1. Render channel icons (WhatsApp, Instagram, Email)
    2. Show unread badge per channel
    3. Highlight selected channel
    4. On click → filter ThreadList by channel

    Create `apps/web/src/components/messaging/ThreadList.tsx`:
    1. Use react-virtuoso for virtualization
    2. Filter threads by selectedChannel
    3. Sort by timestamp (newest first)
    4. Render ThreadItem component (selected indicator, unread badge, status icon)
    5. On click → select thread, update ThreadDetail
    6. Performance target: 1000 threads < 100ms render

    Create `apps/web/src/components/messaging/ThreadDetail.tsx`:
    1. Render message list (Virtuoso for virtualization)
    2. Render channel-specific message components
    3. Render MessageComposer (channel-specific UI)
    4. Show thread info (subject, participants)
    5. Add "Merge Thread" action (manual merge UI for Brain #7 Condition #1)
    6. WebSocket integration: real-time message updates
  </action>
  <verify>
    <automated>pnpm --prefix apps/web test run UnifiedInboxPage</automated>
  </verify>
  <done>
    3-pane layout renders. ChannelRail filters threads. ThreadList virtualizes (1000 threads < 100ms). ThreadDetail shows messages. Keyboard nav works (J/K).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create channel-specific message components</name>
  <files>apps/web/src/components/messaging/messages/WhatsAppMessage.tsx, apps/web/src/components/messaging/messages/InstagramMessage.tsx, apps/web/src/components/messaging/messages/EmailMessage.tsx</files>
  <behavior>
    - WhatsAppMessage: green bubble, checkmarks (sent, delivered, read)
    - InstagramMessage: gradient border, media grid
    - EmailMessage: blue/gray, subject line, thread view, HTML sanitized (DOMPurify)
    - All components render < 16ms (60fps target)
  </behavior>
  <action>
    Create `apps/web/src/components/messaging/messages/WhatsAppMessage.tsx`:
    1. Render green bubble (bg-green-500 text-white)
    2. Show checkmarks: ✓ (sent), ✓✓ (delivered), ✓✓✓ (read)
    3. Render text content
    4. Render media attachments (images, documents)
    5. Show timestamp

    Create `apps/web/src/components/messaging/messages/InstagramMessage.tsx`:
    1. Render gradient border (pink-purple gradient)
    2. Show username
    3. Render comment text
    4. Render media grid (images, videos)
    5. Show timestamp

    Create `apps/web/src/components/messaging/messages/EmailMessage.tsx`:
    1. Render blue/gray bubble (outgoing/incoming)
    2. Show subject line
    3. Render HTML body (sanitized with DOMPurify)
    4. Show threading (In-Reply-To, References)
    5. Show attachments
    6. Show timestamp

    Update `apps/web/src/components/messaging/ThreadDetail.tsx`:
    1. Import channel-specific message components
    2. Render based on message.channel
    3. Measure render time (performance.mark)
  </action>
  <verify>
    <automated>pnpm --prefix apps/web test run WhatsAppMessage InstagramMessage EmailMessage</automated>
  </verify>
  <done>
    Channel-specific components render. WhatsApp checkmarks work. Instagram gradient shows. Email HTML sanitized. All render < 16ms.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete unified inbox UI (messageStore + 3-pane layout + channel-specific components + WebSocket integration + LocalStorage quota monitoring)</what-built>
  <how-to-verify>
    1. Visit http://localhost:3000/messaging
    2. Verify 3-pane layout (ChannelRail | ThreadList | ThreadDetail)
    3. Click channel icons → ThreadList filters correctly
    4. Select thread → ThreadDetail shows messages
    5. Test keyboard nav: Press J (next thread), K (prev thread)
    6. Send test message → Verify WebSocket update
    7. Check localStorage quota: Add 1000+ messages → Verify alert at 80%, block at 90%
    8. Test manual thread merge: Select 2+ threads → Click "Merge" → Verify merge action
    9. Verify channel-specific components: WhatsApp (green bubble, checkmarks), Instagram (gradient), Email (subject, HTML)
    10. Measure performance: 1000 threads should render < 100ms
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] messageStore manages messages + drafts
- [ ] LocalStorage quota monitored: 80% alert, 90% block
- [ ] 3-pane layout renders (ChannelRail | ThreadList | ThreadDetail)
- [ ] ThreadList virtualizes (1000 threads < 100ms)
- [ ] Channel-specific components render (WhatsApp, Instagram, Email)
- [ ] WebSocket integration: real-time updates
- [ ] Keyboard navigation: J/K works
- [ ] Manual thread merge UI works
- [ ] All tests pass: `pnpm --prefix apps/web test run`
- [ ] Human verification approved
</verification>

<success_criteria>
Unified inbox UI is fully implemented. Users see messages across all channels in one place. LocalStorage quota is monitored and enforced. Manual thread merge UI exists. Channel-specific components render correctly. Performance targets met (< 100ms for 1000 threads). Brain #7 Conditions #1, #4, #5 are fully implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/18-multi-channel-gateway/18-07-SUMMARY.md`
</output>
