---
phase: 03-web-ui-platform
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/api/test_app.py
  - tests/api/test_auth.py
  - tests/api/test_websocket.py
  - tests/api/test_executions.py
  - tests/api/test_audit.py
  - tests/api/test_sessions.py
  - tests/perf/test_db_queries.py
  - tests/perf/test_websocket.py
  - tests/e2e/mobile.spec.ts
  - tests/e2e/graph.spec.ts
  - tests/e2e/execution.spec.ts
  - tests/e2e/perf.spec.ts
  - playwright.config.ts
  - pyproject.toml
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08
  - UI-09
  - UI-10
  - ARCH-03
  - PAR-08
  - PERF-02
  - PERF-03
  - PERF-04
user_setup: []
must_haves:
  truths:
    - "All 14 test stub files exist with pytest test functions"
    - "pytest configuration in pyproject.toml includes asyncio and benchmark settings"
    - "Playwright configuration file exists for E2E tests"
    - "All required testing dependencies are installed"
    - "Test stubs have descriptive names matching their requirements"
    - "Wave 0 completion enables all downstream tasks to reference automated tests"
  artifacts:
    - path: "tests/api/test_app.py"
      provides: "Test stubs for FastAPI app creation, route mounting (UI-01)"
      min_lines: 20
      exports: ["test_app_creates", "test_routes_registered"]
    - path: "tests/api/test_auth.py"
      provides: "Test stubs for login, refresh token rotation, password hashing (UI-02, UI-03)"
      min_lines: 30
      exports: ["test_login_success", "test_login_invalid", "test_refresh_token_rotation"]
    - path: "tests/api/test_websocket.py"
      provides: "Test stubs for WebSocket connection, broadcast, reconnection (UI-04, PAR-08)"
      min_lines: 30
      exports: ["test_websocket_connects", "test_progress_updates", "test_reconnection"]
    - path: "tests/api/test_executions.py"
      provides: "Test stubs for CRUD operations, export endpoints (UI-06)"
      min_lines: 25
      exports: ["test_create_task", "test_export_json", "test_export_yaml"]
    - path: "tests/api/test_audit.py"
      provides: "Test stubs for audit log creation, querying (UI-07)"
      min_lines: 20
      exports: ["test_audit_log_created", "test_audit_entries_include_user"]
    - path: "tests/api/test_sessions.py"
      provides: "Test stubs for multi-user isolation, concurrent requests (UI-08)"
      min_lines: 25
      exports: ["test_concurrent_requests_isolated", "test_user_cannot_access_other_tasks"]
    - path: "tests/perf/test_db_queries.py"
      provides: "Test stubs for query latency benchmarks (PERF-02, requires pytest-benchmark)"
      min_lines: 20
      exports: ["test_query_latency", "test_index_performance"]
    - path: "tests/perf/test_websocket.py"
      provides: "Test stubs for broadcast latency benchmarks (PAR-08, PERF-03)"
      min_lines: 20
      exports: ["test_broadcast_latency", "test_throttling_performance"]
    - path: "tests/e2e/mobile.spec.ts"
      provides: "Test stubs for mobile responsiveness tests (UI-05)"
      min_lines: 25
    - path: "tests/e2e/graph.spec.ts"
      provides: "Test stubs for React Flow rendering tests (UI-09)"
      min_lines: 25
    - path: "tests/e2e/execution.spec.ts"
      provides: "Test stubs for end-to-end execution flow (UI-10)"
      min_lines: 25
    - path: "tests/e2e/perf.spec.ts"
      provides: "Test stubs for page load performance tests (PERF-04)"
      min_lines: 20
    - path: "playwright.config.ts"
      provides: "Playwright configuration for E2E tests"
      min_lines: 30
    - path: "pyproject.toml"
      provides: "Updated with pytest-asyncio, pytest-benchmark, pytest-playwright"
      exports: ["tool.pytest.ini_options", "project.dependencies"]
  key_links:
    - from: "tests/api/test_auth.py"
      to: "mastermind_cli/api/routes/auth.py"
      via: "Test functions reference auth endpoints that will be created in Plan 01"
      pattern: "test_login|test_refresh"
    - from: "tests/api/test_websocket.py"
      to: "mastermind_cli/api/websocket.py"
      via: "Test functions reference WebSocket manager that will be created in Plan 01"
      pattern: "test_websocket_connect"
    - from: "tests/perf/test_db_queries.py"
      to: "mastermind_cli/state/repositories.py"
      via: "Benchmark tests reference TaskRepository that will be extended in Plan 01"
      pattern: "test_query_latency|benchmark"
---

<objective>
Create test infrastructure stubs for all Phase 3 requirements to enable Nyquist-compliant automated verification throughout execution.

Purpose: Ensure every task in downstream plans (01-03) has existing test files to reference in `<automated>` verify elements, eliminating "MISSING — Wave 0 must create" gaps.

Output: 14 test stub files (8 API/integration, 2 performance, 4 E2E), Playwright config, and updated pyproject.toml with testing dependencies.
</objective>

<execution_context>
@/home/rpadron/.claude/get-shit-done/workflows/execute-plan.md
@/home/rpadron/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-web-ui-platform/03-CONTEXT.md
@.planning/phases/03-web-ui-platform/03-VALIDATION.md

# Phase 2 Test Patterns (follow existing conventions)
@.planning/phases/02-parallel-execution-core/02-VALIDATION.md

# Existing Test Infrastructure
@pyproject.toml
@tests/conftest.py
</context>

<interfaces>
<!-- Existing test patterns from Phase 2 -->

From tests/conftest.py (if exists):
```python
import pytest
from mastermind_cli.state.database import get_db

@pytest.fixture
async def test_db():
    # In-memory SQLite for testing
    pass

@pytest.fixture
def test_client():
    # FastAPI TestClient fixture
    pass
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create API test stubs for authentication, WebSocket, and task endpoints</name>
  <files>tests/api/test_app.py, tests/api/test_auth.py, tests/api/test_websocket.py, tests/api/test_executions.py, tests/api/test_audit.py, tests/api/test_sessions.py</files>
  <action>
    Create 6 API/integration test stub files:

    1. **tests/api/test_app.py** (UI-01):
       ```python
       import pytest
       from fastapi.testclient import TestClient

       def test_app_creates():
           """Test FastAPI application can be instantiated."""
           # TODO: Implement after Plan 01 Task 1
           assert False, "Test stub: FastAPI app creation"

       def test_routes_registered():
           """Test auth and task routes are registered."""
           # TODO: Implement after Plan 01 Task 1
           assert False, "Test stub: Route registration"
       ```

    2. **tests/api/test_auth.py** (UI-02, UI-03):
       ```python
       import pytest
       from fastapi.testclient import TestClient

       def test_login_success():
           """Test POST /api/auth/login with valid credentials returns 200."""
           # TODO: Implement after Plan 01 Task 1
           assert False, "Test stub: Login success"

       def test_login_invalid_credentials():
           """Test POST /api/auth/login with invalid credentials returns 401."""
           # TODO: Implement after Plan 01 Task 1
           assert False, "Test stub: Login invalid"

       def test_refresh_token_rotation():
           """Test POST /api/auth/refresh returns new access_token AND new refresh_token."""
           # TODO: Implement after Plan 01 Task 1
           assert False, "Test stub: Refresh token rotation"

       def test_old_refresh_token_rejected():
           """Test POST /api/auth/refresh with old/revoked token returns 401."""
           # TODO: Implement after Plan 01 Task 1
           assert False, "Test stub: Old refresh token rejected"

       def test_api_key_creation():
           """Test POST /api/auth/api-keys creates API key for authenticated user."""
           # TODO: Implement after Plan 01 Task 1
           assert False, "Test stub: API key creation"

       def test_api_key_authentication():
           """Test API key can be used via Authorization: Bearer {key} header."""
           # TODO: Implement after Plan 01 Task 1
           assert False, "Test stub: API key authentication"
       ```

    3. **tests/api/test_websocket.py** (UI-04, PAR-08):
       ```python
       import pytest
       import asyncio
       from fastapi.testclient import TestClient

       @pytest.mark.asyncio
       async def test_websocket_connects():
           """Test WebSocket connection accepts valid JWT token."""
           # TODO: Implement after Plan 01 Task 3
           assert False, "Test stub: WebSocket connection"

       @pytest.mark.asyncio
       async def test_progress_updates():
           """Test WebSocket client receives task_update_batch events."""
           # TODO: Implement after Plan 01 Task 3
           assert False, "Test stub: Progress updates"

       @pytest.mark.asyncio
       async def test_reconnection_resync():
           """Test client receives resync events after reconnection."""
           # TODO: Implement after Plan 01 Task 3
           assert False, "Test stub: Reconnection resync"

       @pytest.mark.asyncio
       async def test_broadcast_throttling():
           """Test throttling batches updates to max 1 per 300ms."""
           # TODO: Implement after Plan 01 Task 3
           assert False, "Test stub: Broadcast throttling"
       ```

    4. **tests/api/test_executions.py** (UI-06):
       ```python
       import pytest
       from fastapi.testclient import TestClient

       def test_create_task():
           """Test POST /api/tasks creates task and returns task_id."""
           # TODO: Implement after Plan 01 Task 2
           assert False, "Test stub: Create task"

       def test_list_tasks():
           """Test GET /api/tasks returns list of user's tasks."""
           # TODO: Implement after Plan 01 Task 2
           assert False, "Test stub: List tasks"

       def test_export_json():
           """Test export JSON downloads valid .json file."""
           # TODO: Implement after Plan 02 Task 3
           assert False, "Test stub: Export JSON"

       def test_export_yaml():
           """Test export YAML downloads valid .yaml file using js-yaml."""
           # TODO: Implement after Plan 02 Task 3
           assert False, "Test stub: Export YAML"

       def test_export_markdown():
           """Test export Markdown downloads formatted .md file."""
           # TODO: Implement after Plan 02 Task 3
           assert False, "Test stub: Export Markdown"

       def test_cancel_task():
           """Test DELETE /api/tasks/{id} cancels running task."""
           # TODO: Implement after Plan 01 Task 2
           assert False, "Test stub: Cancel task"
       ```

    5. **tests/api/test_audit.py** (UI-07):
       ```python
       import pytest
       from fastapi.testclient import TestClient

       def test_audit_log_created():
           """Test all POST/PUT/DELETE requests are logged to audit_log table."""
           # TODO: Implement after Plan 01 Task 1
           assert False, "Test stub: Audit log created"

       def test_audit_entries_include_user():
           """Test audit log entries include user_id, endpoint, timestamp, request_hash."""
           # TODO: Implement after Plan 01 Task 1
           assert False, "Test stub: Audit entry structure"

       def test_read_operations_not_logged():
           """Test GET requests do not create audit log entries."""
           # TODO: Implement after Plan 01 Task 1
           assert False, "Test stub: Read operations not logged"
       ```

    6. **tests/api/test_sessions.py** (UI-08):
       ```python
       import pytest
       import asyncio
       from fastapi.testclient import TestClient

       @pytest.mark.asyncio
       async def test_concurrent_requests_isolated():
           """Test multiple users have isolated sessions."""
           # TODO: Implement after Plan 01 Task 2
           assert False, "Test stub: Session isolation"

       @pytest.mark.asyncio
       async def test_user_cannot_access_other_tasks():
           """Test user A cannot access user B's tasks."""
           # TODO: Implement after Plan 01 Task 2
           assert False, "Test stub: Cross-user access blocked"

       def test_api_key_isolation():
           """Test API keys are scoped to user who created them."""
           # TODO: Implement after Plan 01 Task 1
           assert False, "Test stub: API key isolation"
       ```

    All test stubs follow pytest conventions:
    - Use `assert False, "Test stub: ..."` to indicate unimplemented
    - Include docstrings explaining what will be tested
    - Use `@pytest.mark.asyncio` for async tests
    - Reference downstream tasks (Plan 01, Plan 02) in TODOs
  </action>
  <verify>
    <automated>uv run pytest tests/api/ -v --collect-only</automated>
  </verify>
  <done>6 API test stub files exist with descriptive test functions, pytest discovers all tests</done>
</task>

<task type="auto">
  <name>Task 2: Create performance test stubs and Playwright E2E test stubs</name>
  <files>tests/perf/test_db_queries.py, tests/perf/test_websocket.py, tests/e2e/mobile.spec.ts, tests/e2e/graph.spec.ts, tests/e2e/execution.spec.ts, tests/e2e/perf.spec.ts, playwright.config.ts</files>
  <action>
    Create 2 performance test stubs and 4 E2E test stubs with Playwright config:

    1. **tests/perf/test_db_queries.py** (PERF-02):
       ```python
       import pytest
       from mastermind_cli.state.repositories import TaskRepository

       def test_query_latency(benchmark):
           """Test task state queries complete in <100ms."""
           # TODO: Implement after Plan 01 Task 2
           # benchmark.pedantic(TaskRepository.get_task, args=[task_id])
           assert False, "Test stub: Query latency <100ms"

       def test_index_performance():
           """Test indexes improve query performance."""
           # TODO: Implement after Plan 01 Task 2
           assert False, "Test stub: Index performance"

       def test_concurrent_query_performance():
           """Test multiple concurrent queries maintain <100ms latency."""
           # TODO: Implement after Plan 01 Task 2
           assert False, "Test stub: Concurrent queries"
       ```

    2. **tests/perf/test_websocket.py** (PAR-08, PERF-03):
       ```python
       import pytest
       import time
       from mastermind_cli.api.websocket import WebSocketManager

       @pytest.mark.asyncio
       async def test_broadcast_latency(benchmark):
           """Test WebSocket broadcast latency <500ms from state change to client receive."""
           # TODO: Implement after Plan 01 Task 3
           # Measure time from broadcast to client receive
           assert False, "Test stub: Broadcast latency <500ms"

       @pytest.mark.asyncio
       async def test_throttling_performance():
           """Test throttling limits broadcasts to 1 per 300ms."""
           # TODO: Implement after Plan 01 Task 3
           assert False, "Test stub: Throttling limits"

       @pytest.mark.asyncio
       async def test_multiple_clients_performance():
           """Test broadcast to 10 clients completes in <100ms."""
           # TODO: Implement after Plan 01 Task 3
           assert False, "Test stub: Multi-client broadcast"
       ```

    3. **tests/e2e/mobile.spec.ts** (UI-05):
       ```typescript
       import { test, expect } from '@playwright/test';

       test.describe('Mobile Responsiveness', () => {
         test('mobile layout stacks vertically', async ({ page }) => {
           // TODO: Implement after Plan 02 Task 1
           test.fail(true, 'Test stub: Mobile vertical stack');
         });

         test('touch targets are at least 44px', async ({ page }) => {
           // TODO: Implement after Plan 02 Task 1
           test.fail(true, 'Test stub: Touch target size');
         });

         test('navigation collapses to hamburger menu', async ({ page }) => {
           // TODO: Implement after Plan 02 Task 1
           test.fail(true, 'Test stub: Hamburger menu');
         });
       });
       ```

    4. **tests/e2e/graph.spec.ts** (UI-09):
       ```typescript
       import { test, expect } from '@playwright/test';

       test.describe('DAG Graph Visualization', () => {
         test('graph renders nodes and edges', async ({ page }) => {
           // TODO: Implement after Plan 03 Task 2
           test.fail(true, 'Test stub: Graph rendering');
         });

         test('node colors match states (gray/blue/green/red/yellow)', async ({ page }) => {
           // TODO: Implement after Plan 03 Task 2
           test.fail(true, 'Test stub: Node state colors');
         });

         test('hover highlights ancestors', async ({ page }) => {
           // TODO: Implement after Plan 03 Task 3
           test.fail(true, 'Test stub: Ancestor highlighting');
         });

         test('graph updates in real-time', async ({ page }) => {
           // TODO: Implement after Plan 03 Task 3
           test.fail(true, 'Test stub: Real-time updates');
         });
       });
       ```

    5. **tests/e2e/execution.spec.ts** (UI-10):
       ```typescript
       import { test, expect } from '@playwright/test';

       test.describe('End-to-End Execution Flow', () => {
         test('user can login and see dashboard', async ({ page }) => {
           // TODO: Implement after Plan 02 Task 2
           test.fail(true, 'Test stub: Login flow');
         });

         test('user can create task via form', async ({ page }) => {
           // TODO: Implement after Plan 02 Task 3
           test.fail(true, 'Test stub: Task creation');
         });

         test('real-time updates appear in UI', async ({ page }) => {
           // TODO: Implement after Plan 02 Task 4
           test.fail(true, 'Test stub: Real-time updates');
         });

         test('export downloads correct file format', async ({ page }) => {
           // TODO: Implement after Plan 02 Task 3
           test.fail(true, 'Test stub: Export functionality');
         });
       });
       ```

    6. **tests/e2e/perf.spec.ts** (PERF-04):
       ```typescript
       import { test, expect } from '@playwright/test';

       test.describe('Performance Tests', () => {
         test('page load time <2 seconds on 3G', async ({ page }) => {
           // TODO: Implement after Plan 02 Task 1
           test.fail(true, 'Test stub: Page load <2s');
         });

         test('time to interactive <3 seconds', async ({ page }) => {
           // TODO: Implement after Plan 02 Task 1
           test.fail(true, 'Test stub: TTI <3s');
         });

         test('no layout shifts (CLS <0.1)', async ({ page }) => {
           // TODO: Implement after Plan 02 Task 1
           test.fail(true, 'Test stub: Layout stability');
         });
       });
       ```

    7. **playwright.config.ts**:
       ```typescript
       import { defineConfig, devices } from '@playwright/test';

       export default defineConfig({
         testDir: './tests/e2e',
         fullyParallel: false,  // Run serially for smoke tests
         forbidOnly: !!process.env.CI,
         retries: process.env.CI ? 2 : 0,
         workers: 1,  // Single worker for stability
         reporter: 'html',
         use: {
           baseURL: 'http://localhost:8000',
           trace: 'on-first-retry',
           screenshot: 'only-on-failure',
         },
         projects: [
           {
             name: 'Desktop',
             use: { ...devices['Desktop Chrome'] },
           },
           {
             name: 'Mobile',
             use: { ...devices['iPhone 13'] },
           },
         ],
         webServer: {
           command: 'uv run uvicorn mastermind_cli.api.app:create_app --factory',
           url: 'http://localhost:8000',
           reuseExistingServer: !process.env.CI,
           timeout: 120000,
         },
       });
       ```

    Test stub conventions:
    - Use `test.fail(true, 'Test stub: ...')` for Playwright
    - Use `assert False, "Test stub: ..."` for pytest
    - Reference downstream plans in TODOs
    - Include descriptive docstrings
  </action>
  <verify>
    <automated>uv run pytest tests/perf/ -v --collect-only && uv run playwright test --list</automated>
  </verify>
  <done>2 performance test stubs, 4 E2E test stubs, and Playwright config exist, all tests discoverable</done>
</task>

<task type="auto">
  <name>Task 3: Update pyproject.toml with testing dependencies and pytest configuration</name>
  <files>pyproject.toml</files>
  <action>
    Update pyproject.toml with testing framework dependencies and pytest configuration:

    1. Add to `[project.dependencies]` (if not already present):
       ```toml
       fastapi = ">=0.115.0"
       uvicorn = {extras = ["standard"], version = ">=0.32.0"}
       python-jose = {extras = ["cryptography"], version = ">=3.3.0"}
       bcrypt = ">=4.2.0"
       python-multipart = ">=0.0.20"
       aiofiles = ">=24.0.0"
       websockets = ">=14.0"
       ```

    2. Add to `[project.dev-dependencies]`:
       ```toml
       pytest = ">=9.0.0"
       pytest-asyncio = ">=0.24.0"
       pytest-benchmark = ">=4.0.0"
       pytest-playwright = ">=0.5.0"
       httpx-ws = ">=0.6.0"  # For WebSocket testing
       ```

    3. Add `[tool.pytest.ini_options]` section:
       ```toml
       [tool.pytest.ini_options]
       asyncio_mode = "auto"
       asyncio_default_fixture_loop_scope = "function"
       testpaths = ["tests"]
       python_files = ["test_*.py", "*_test.py"]
       python_classes = ["Test*"]
       python_functions = ["test_*"]
       addopts = [
           "-v",
           "--strict-markers",
           "--tb=short",
           "--cov=mastermind_cli",
           "--cov-report=term-missing:skip-covered",
       ]
       markers = [
           "asyncio: mark test as async",
           "benchmark: mark test as performance benchmark",
           "integration: mark test as integration test",
           "e2e: mark test as end-to-end test",
       ]
       log_cli = true
       log_cli_level = "INFO"
       filterwarnings = [
           "ignore::DeprecationWarning",
           "ignore::PendingDeprecationWarning",
       ]
       ```

    4. Verify existing dependencies:
       - Check if pytest, pytest-asyncio already exist
       - Check if any conflicts with versions
       - Ensure Python 3.14+ compatibility

    5. Create `tests/conftest.py` if it doesn't exist:
       ```python
       import pytest
       import asyncio
       from typing import AsyncGenerator, Generator
       from fastapi.testclient import TestClient
       from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession
       from sqlalchemy.orm import sessionmaker

       from mastermind_cli.api.app import create_app
       from mastermind_cli.state.database import get_db

       @pytest.fixture
       def event_loop():
           """Create event loop for async tests."""
           loop = asyncio.get_event_loop_policy().new_event_loop()
           yield loop
           loop.close()

       @pytest.fixture
       async def test_db():
           """In-memory SQLite database for testing."""
           # TODO: Implement async DB fixture
           engine = create_async_engine("sqlite+aiosqlite:///:memory:")
           yield engine
           await engine.dispose()

       @pytest.fixture
       def test_client(test_db):
           """FastAPI TestClient with test database."""
           app = create_app()
           app.dependency_overrides[get_db] = lambda: test_db
           return TestClient(app)

       @pytest.fixture
       def mock_auth_headers():
           """Mock authentication headers for testing."""
           return {"Authorization": "Bearer test_token"}

       @pytest.fixture
       def mock_user():
           """Mock user object for testing."""
           return {"id": "test-user-1", "username": "testuser"}
       ```

    Configuration notes:
    - `asyncio_mode = "auto"` enables pytest-asyncio without explicit decorators
    - `pytest-benchmark` provides `benchmark` fixture for PERF tests
    - `pytest-playwright` provides `page` fixture for E2E tests
    - Coverage reporting enabled with `--cov` flag
    - Markers allow running specific test suites: `pytest -m "integration"`
  </action>
  <verify>
    <automated>uv run pytest --version && uv run pytest --collect-only</automated>
  </verify>
  <done>pyproject.toml updated with testing dependencies, pytest configured, conftest.py fixtures defined</done>
</task>

</tasks>

<verification>
## Overall Phase Checks

### Test Discovery
- [ ] pytest discovers all 8 API/integration tests (`pytest tests/api/ --collect-only`)
- [ ] pytest discovers all 2 performance tests (`pytest tests/perf/ --collect-only`)
- [ ] Playwright discovers all 4 E2E test suites (`playwright test --list`)
- [ ] All test stubs have descriptive names and docstrings

### Dependency Installation
- [ ] `uv sync` completes without errors
- [ ] pytest-asyncio installed and configured
- [ ] pytest-benchmark installed for performance tests
- [ ] pytest-playwright and Playwright browsers installed

### Configuration
- [ ] pytest.ini_options in pyproject.toml includes asyncio_mode
- [ ] Playwright config exists with correct baseURL and webServer
- [ ] conftest.py has fixtures for test_client, test_db, mock_auth_headers

### Wave 0 Completion
- [ ] All 14 test files from VALIDATION.md Wave 0 requirements exist
- [ ] No downstream task needs to create test files (all references exist)
- [ ] Test stubs are ready for implementation in respective plans
</verification>

<success_criteria>
1. All 14 test stub files exist (8 API, 2 perf, 4 E2E)
2. pytest discovers all tests without errors
3. Playwright discovers all E2E test suites
4. Testing dependencies installed in pyproject.toml
5. pytest configured with asyncio and benchmark support
6. Playwright configured with webServer and projects
7. conftest.py has basic fixtures for testing
8. Wave 0 completion enables Nyquist compliance for all downstream tasks
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui-platform/03-00-SUMMARY.md`
</output>
