---
phase: 03-web-ui-platform
task: 4
total_tasks: 12
status: in_progress
last_updated: "2026-03-17T23:06:12.537Z"
---

<current_state>
UAT Phase 3 en progreso via contenedor Docker. Tests 1-3 pasaron. Test 4 (refresh token rotation) tenía un bug que fue identificado y arreglado pero el commit fue RECHAZADO por el usuario antes de ejecutarse.

El fix está en el working tree pero NO commiteado:
- `mastermind_cli/api/routes/auth.py` — `jti` agregado a `create_access_token` y `create_refresh_token`

El contenedor en ejecución NO tiene este fix (fue rebuilt antes del rechazo del commit).
</current_state>

<completed_work>

| Test | Resultado |
|------|-----------|
| 1. Cold Start Smoke Test | ✅ pass |
| 2. Login API | ✅ pass (bug encontrado y arreglado: db_path no se propagaba a routes) |
| 3. Token protege rutas | ✅ pass |
| 4. Refresh token rotation | ❌ bug encontrado — commit pendiente |

### Fixes aplicados esta sesión (commiteados):
- `fix(infra)` d31917b — static files, admin seed, docker-compose
- `fix(api)` cc3db2d — db_path dependency_overrides en create_app
- `chore(tooling)` cd35d74 — pyrightconfig.json
- `fix(types)` eb6b121 — 4 errores Pyright (flow_config, Any, __all__, JSONEncoder)

### Fix pendiente de commit:
- `mastermind_cli/api/routes/auth.py` — `jti: str(uuid.uuid4())` en ambos tokens
- Verificado funcionando: segundo refresh devuelve HTTP 401 ✅

</completed_work>

<remaining_work>

1. Commitear fix de `jti` en auth.py
2. Rebuild container con el fix
3. Continuar UAT desde test 5:
   - Test 5: Task creation (POST /api/tasks con JWT)
   - Test 6: Session isolation (dos usuarios)
   - Test 7: WebSocket connection
   - Test 8: DAG graph endpoint
   - Test 9: Dashboard HTML accesible
   - Test 10: Export desde dashboard
   - Test 11: Responsive design
   - Test 12: Audit log

</remaining_work>

<decisions_made>

- Usar Docker para UAT en vez de servidor manual — más realista y reproducible
- `get_app()` lee `MM_DB_PATH` del env — db persistente en volumen Docker
- Admin seed en `create_auth_schema()` — primer run crea usuario admin/admin
- `jti` (UUID) en cada JWT — garantiza unicidad independiente del timing

</decisions_made>

<blockers>

Ninguno activo. El fix de jti está listo, solo falta commit + rebuild.

</blockers>

<next_action>
1. `git add mastermind_cli/api/routes/auth.py && git commit -m "fix(auth): add jti to JWT tokens — prevents same-second token collision breaking rotation"`
2. `docker compose build --no-cache && docker compose up -d`
3. Verificar rotación: login → refresh1 → refresh2 con mismo token → debe dar 401
4. Continuar con test 5 del UAT (POST /api/tasks)
5. Claude ejecuta los tests directamente (no pedir al usuario que corra curls)
</next_action>
