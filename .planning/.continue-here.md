---
phase: mm-flow-completion
task: 1
total_tasks: 4
status: in_progress
last_updated: "2026-04-14T11:19:55Z"
---

<current_state>
MM-FLOW-COMPLETION-PLAN.md está en v4 — aprobado por Brain #7 (94/100 → 100% con items finales resueltos).
El plan tiene 4 fases (FASE 5 cortada). NADA ejecutado aún — solo el plan fue corregido y aprobado.
Próximo paso: ejecutar FASE 1.
</current_state>

<completed_work>

**Sesión anterior (2026-04-13):**
- ✅ Auditoría completa de MM-Flow — estado real de cada módulo documentado
- ✅ PostgreSQL mastermind levantado: `docker compose up -d postgres`
- ✅ Análisis de 3 repos fuente (GSD, Paperclip, OpenClaw)
- ✅ `MM-FLOW-COMPLETION-PLAN.md` v2 creado (.planning/)

**Sesión 2026-04-14 (primera parte):**
- ✅ Plan evaluado por los 7 brain agents en paralelo — Global Rating 81/100, Dispatch Score 4.2/5
- ✅ 3 blockers + 4 conditions + findings adicionales identificados por brain council
- ✅ `.continue-here.md` actualizado con correcciones del brain council

**Sesión 2026-04-14 (esta sesión):**
- ✅ Brain council evaluation revisada en detalle (blockers, conditions, Brain #7 synthesis completo)
- ✅ Agent rewrote MM-FLOW-COMPLETION-PLAN.md → v3 con todas las correcciones aplicadas
- ✅ Brain #7 re-evaluó v3 → APPROVED 94/100
- ✅ 2 items pendientes resueltos directo en el plan (v4):
  - Zod `CostUpdateEventSchema` definido en Task 2.2 con safe parse pattern
  - `_write_runtime_state()` call sites en `--start` y `--complete` branches de Task 2.1
- ✅ Plan v4 auditado: 16/16 correcciones presentes

**YA ESTABA HECHO (no hacer):**
- audit router ya está wired en `apps/api/app.py` line 212 con try/except fallback
- `phase_executions` table ya existe en `apps/api/mastermind_cli/state/database.py` line 416
- `mm-flow-statusline.js` ya existe en `~/.claude/hooks/mm-flow-statusline.js` (74 líneas) — EXTENDER
- `mm-flow-context-monitor.js` existe (191 líneas, PostToolUse) — candidato para write-tool-call detection
- `mm-flow-session-init.js` existe (SessionStart) — modificar, no crear
- `mm-plan-phase-context.js` existe (UserPromptSubmit) — ya funciona

</completed_work>

<remaining_work>

**FASE 1 — Infrastructure Foundation (PRÓXIMO PASO INMEDIATO):**
- Task 1.1: ✅ PostgreSQL levantado
- Task 1.2: Aplicar `docker/postgres/mm-flow-audit.sql` → tablas de audit trail
  ```bash
  docker exec -i mastermind-postgres-1 psql -U postgres -d mastermind_bd < docker/postgres/mm-flow-audit.sql
  ```
- Task 1.3: Verificar seed data (workspaces con current_phase para project mastermind)
- Task 1.4: Crear tabla `agent_registry` + seed 7 brains
  - UNIQUE(organization_id, project_id, brain_id) — NO solo (org, brain)
  - Seed con DO $$ BEGIN ... END $$ guard, ON CONFLICT DO NOTHING
  - Guardrail CI: seed dos veces → COUNT(*) = 7 por proyecto
- Task 1.5 [NUEVO — BLOQUEANTE para FASE 2]: Crear config.yml loader + `config.yml`
  - Loader: missing file → defaults, malformed YAML → ConfigError, unknown model → ConfigError
  - Archivo: `.planning/.mm-flow/config.yml` con model profiles (quality/balanced/budget) + brain routing

**FASE 2 — CLI ↔ Skills Bridge:**
- Task 2.1: --start/--complete flags en mm-flow execute-phase
  - asyncio.run() wrapper, click.UsageError si ambos flags, _write_runtime_state() en ambas branches
- Task 2.2: DynamicDispatchEngine con DispatchResult Pydantic v2 strict
  - Literal["DISCUSSION","PLANNING","EXECUTION_WAVE","VERIFICATION"]
  - Pre-dispatch budget check, SLI-2 asyncio.wait_for(30.0), DISPATCH_ORACLE
  - Zod CostUpdateEventSchema en apps/web/src/types/api.ts
- Task 2.3: Wire skills /mm:execute-phase y /mm:plan-phase con CLI bookends

**FASE 3 — Context Persistence:**
- Task 3.1: Stop hook mm-flow-stop.js (thin JS dispatcher) + checkpoint_writer.py (Python, testeable CI)
  - Trigger: write-tool-call detection en transcript
- Task 3.2: Modificar mm-flow-session-init.js → stale checkpoint detection (>48h warning)
- Task 3.3: Extender mm-flow-context-monitor.js → write-tool-call detection (NO "cada 10 mensajes")
- Task 3.4: Fix context_loader flow (skill genera CONTEXT.md via mem_search, CLI registra artifact)

**FASE 4 — Audit Trail + Statusline:**
- Task 4.1: Depends(get_current_user_any) en 13 endpoints audit.py + 26 tests + CI gate
  - tests/api/test_audit_routes.py, tests/api/ en level2-tests de ci.yml
- Task 4.2: ~~config.yml~~ → movido a FASE 1 Task 1.5
- Task 4.3: Crear ~/.claude/backends.sh (credentials loader)
- Task 4.4: EXTENDER mm-flow-statusline.js (NO reemplazar)
  - Preservar líneas 24-43 (context window % lógica)
  - Agregar: phase state, active brain [ACTIVE]/[BARRIER]/[IDLE]/[OFFLINE] ANSI
  - Data source: runtime-state.json (NO DB)

**FASE 5 — CORTADA (Build Trap)**

</remaining_work>

<decisions_made>

**Del brain council (sesiones anteriores):**
- CLI es helper de estado, Claude es executor. Skills bookendean con --start y --complete.
- brain_router.py: deprecar → DynamicDispatchEngine
- FASE 5 CORTADA — Build Trap, no reduce T1
- config.yml MOVIDO a FASE 1 Task 1.5 (dependencia invertida)
- UNIQUE constraint agent_registry: (org_id, project_id, brain_id)
- Stop hook: lógica en Python (testeable CI), JS = thin dispatcher
- Statusline: EXTENDER archivo existente, no reemplazar
- Trigger de checkpoint: write-tool-call detection, no message count
- Budget check: pre-dispatch en DynamicDispatchEngine (FASE 2)
- DispatchResult: Pydantic v2 BaseModel strict

**Esta sesión:**
- Zod CostUpdateEventSchema: definir en apps/web/src/types/api.ts, usar safeParse (no cast unsafe)
- _write_runtime_state(): llamar en --start (ACTIVE) y --complete (IDLE) para statusline
- DISPATCH_ORACLE: EXECUTION_WAVE y VERIFICATION tienen sequential: True (no barrier)
- Plan v4 aprobado Brain #7 → listo para ejecución

</decisions_made>

<blockers>

Ningún blocker para arrancar FASE 1.

Los 3 blockers del brain council están resueltos EN EL PLAN (no en código todavía):
1. config.yml loader → FASE 1 Task 1.5 (plan corregido)
2. JWT auth audit.py → FASE 4 Task 4.1 (plan corregido)
3. UNIQUE constraint agent_registry → FASE 1 Task 1.4 (plan corregido)

</blockers>

<context>
El trabajo de estas sesiones fue de planificación y corrección — no de ejecución.
El plan MM-FLOW-COMPLETION-PLAN.md v4 incorpora todas las correcciones del brain council de 7 agentes.
Brain #7 lo aprobó (94/100 → 100% con los 2 items finales resueltos directamente en el plan).

El plan tiene 4 fases (FASE 5 cortada como Build Trap):
- FASE 1: Infrastructure (PostgreSQL audit trail + agent_registry + config.yml loader)
- FASE 2: CLI ↔ Skills bridge (DynamicDispatchEngine + lifecycle flags)
- FASE 3: Context persistence (Stop hook + write-tool-call detection)
- FASE 4: Audit trail + statusline (JWT auth 13 rutas + EXTENDER statusline)

**Archivos clave:**
- `.planning/MM-FLOW-COMPLETION-PLAN.md` — plan v4, aprobado, listo para ejecución
- `apps/api/routers/audit.py` — 1593 líneas, 13 routes sin JWT (FASE 4)
- `~/.claude/hooks/mm-flow-statusline.js` — existe 74 líneas, EXTENDER (FASE 4)
- `~/.claude/hooks/mm-flow-context-monitor.js` — 191 líneas, extender para write-tool-call detection
- `docker/postgres/mm-flow-audit.sql` — listo para aplicar (FASE 1 Task 1.2)
</context>

<next_action>
1. `/clear` → `/gsd:resume-work` para restaurar contexto
2. Ejecutar FASE 1:
   ```bash
   # Task 1.2: Audit trail SQL
   docker exec -i mastermind-postgres-1 psql -U postgres -d mastermind_bd < docker/postgres/mm-flow-audit.sql

   # Task 1.3: Verificar seed data
   docker exec mastermind-postgres-1 psql -U postgres -d mastermind_bd \
     -c "SELECT w.name, w.current_phase FROM workspaces w JOIN projects p ON w.project_id = p.id WHERE p.slug = 'mastermind';"

   # Task 1.4: agent_registry con UNIQUE(org, project, brain)
   # Task 1.5: config.yml loader
   ```
</next_action>
