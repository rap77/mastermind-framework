---
phase: post-v2.0.0-test-fixes
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-03-16T03:08:31.951Z
---

<current_state>
Post-v2.0.0 RELEASE test fixing session. Successfully committed 154 test fixes (commit 8c81fba). Working directory is clean, 4 commits ahead of origin/master.

Remaining: 4 tests failing with similar async/await patterns (missing `await` on coroutine objects).
</current_state>

<completed_work>
- Session 2026-03-15: Post-v2.0.0 bug fixes
  - Fixed test_create_api_key: Added @pytest.mark.asyncio decorator and await
  - Fixed test_executor_uses_error_formatter: Added task_repo.create() before execute_brain()
  - Fixed test_executor_hides_stack_trace: Added task_repo.create() before execute_brain()
  - Fixed test_speedup_factor: Corrected mock MCP client, added asyncio.to_thread()
  - Fixed test_concurrent_execution: Changed async mock to sync with time.sleep()
  - Fixed test_waves_execute_sequentially: Changed async mock to sync with time.sleep()
  - Fixed test_no_rate_limit_errors: Changed async mock to sync with time.sleep()

Commit: 8c81fba - "fix(tests): resolve 154 test failures with async/await and mock corrections"
- Test Results: 358 passing (98%), 4 failing
- Files modified: 5 files, 40 insertions(+), 21 deletions(-)
</completed_work>

<remaining_work>
4 tests failing with async/await issues:

1. test_web_auth_with_database (test_pure_function_arch.py:378)
   - Error: "TypeError: cannot unpack non-iterable coroutine object"
   - Warning: "coroutine 'create_api_key' was never awaited"
   - Line 378: `full_key, response = create_api_key(key_data)`
   - Fix: Add `@pytest.mark.asyncio` decorator and `await` keyword

2. test_error_recovery_flow (test_pure_function_arch.py)
   - Error: "TypeError: object of type 'coroutine' has no len()"
   - Root cause: Similar missing await pattern

3. test_exponential_backoff_with_jitter (test_task_executor.py)
   - Error: "assert 0 == 3" (call_times is empty)
   - Root cause: mock.call_mcp never being called (mock misconfiguration)

4. test_concurrent_executions_no_crosstalk (test_pure_function_arch.py)
   - Error: AssertionError about brief content
   - Root cause: Likely async mock vs sync code mismatch

Pattern: All 4 tests have async/await mismatches or mock configuration issues.
</remaining_work>

<decisions_made>
- Used systematic debugging process (4 phases) for each bug
- Pattern-based fixing: Found root cause once, applied to similar tests
- Changed async mocks to sync with time.sleep() to match real MCPClient interface
- Added asyncio.to_thread() in task_executor._call_brain() to avoid blocking event loop
- Fixed TaskGroup result collection: use task.result() instead of await task
</decisions_made>

<blockers>
- Context limit reached (80-90%): Recommended pausing to commit progress
- No technical blockers - all identified issues have clear fixes
</blockers>

<context>
Debugging approach was highly effective:
- Phase 1: Root cause investigation (read errors, reproduce, trace)
- Phase 2: Pattern analysis (find working examples, compare)
- Phase 3: Hypothesis (form theory, test minimally)
- Phase 4: Implementation (create test, fix single issue, verify)

Key insight: Many tests had async mocks for sync methods (query_brain vs call_mcp).
The real MCPClient has sync methods, so mocks must also be sync.
</context>

<next_action>
Resume with test_web_auth_with_database fix:
1. Open tests/integration/test_pure_function_arch.py
2. Find line ~378 with `create_api_key(key_data)` call
3. Add `@pytest.mark.asyncio` decorator to the test method
4. Add `await` before the `create_api_key()` call
5. Run pytest to verify
6. Repeat pattern for other 3 tests
</next_action>
