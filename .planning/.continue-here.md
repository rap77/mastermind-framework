---
phase: mm-flow-completion
task: 1
total_tasks: 4
status: in_progress
last_updated: "2026-04-14T02:22:27Z"
---

<current_state>
Plan MM-Flow Completion evaluado por los 7 brain agents (consejo completo). El plan original tenía errores importantes — fueron corregidos. El plan CORREGIDO está listo para ejecutarse. AÚN NO SE EJECUTÓ NADA.

**El plan original tenía:**
- FASE 5 completa (Build Trap — cortar)
- config.yml creado en FASE 4 pero consumido en FASE 2 (dependencia invertida — mover a FASE 1)
- "Wire audit router" como tarea (ya está wired en app.py line 212 — no hacer)
- "Crear statusline.js" (ya existe en ~/.claude/hooks/mm-flow-statusline.js — EXTENDER no reemplazar)
- "Cada 10 mensajes" para checkpoint reminder (trigger incorrecto — reemplazar con write-tool-call detection)

**Estado actual del plan CORREGIDO:**
- FASE 1: 0% — siguiente paso inmediato
- FASE 2: 0%
- FASE 3: 0%
- FASE 4: 0%
- FASE 5: CORTADA
</current_state>

<completed_work>

**Sesión anterior (2026-04-13):**
- ✅ Auditoría completa de MM-Flow — estado real de cada módulo documentado
- ✅ PostgreSQL mastermind levantado: `docker compose up -d postgres`
- ✅ Análisis de 3 repos fuente (GSD, Paperclip, OpenClaw)
- ✅ `MM-FLOW-COMPLETION-PLAN.md` v2 creado (.planning/)

**Sesión actual (2026-04-14):**
- ✅ Plan evaluado por los 7 brain agents en paralelo (Phase B) + Brain #7 síntesis (Phase C)
- ✅ Global Rating: 81/100 | Dispatch Score: 4.2/5 (Senior) | Brain #5: Principal (5/5)
- ✅ Errores críticos del plan identificados y documentados
- ✅ Orden de ejecución corregido aprobado por Brain #7
- ✅ Guardado en Engram (topic_key: mm-flow/completion-plan-evaluation)
- ✅ Todos los BRAIN-FEED-NN-domain.md actualizados por los respectivos brains

**YA ESTABA HECHO (no hacer):**
- audit router ya está wired en `apps/api/app.py` line 212 con try/except fallback
- `phase_executions` table ya existe en `apps/api/mastermind_cli/state/database.py` line 416
- `mm-flow-statusline.js` ya existe en `~/.claude/hooks/mm-flow-statusline.js` (74 líneas) — EXTENDER
- `mm-flow-context-monitor.js` existe (191 líneas, PostToolUse) — candidato para keyword detection
- `mm-flow-session-init.js` existe (SessionStart) — modificar, no crear
- `mm-plan-phase-context.js` existe (UserPromptSubmit) — ya funciona

</completed_work>

<remaining_work>

**FASE 1 — Infrastructure Foundation (PRÓXIMO PASO INMEDIATO):**
- Task 1.1: ✅ PostgreSQL levantado
- Task 1.2: Aplicar `docker/postgres/mm-flow-audit.sql` → tablas de audit trail
  ```bash
  docker exec -i mastermind-postgres-1 psql -U postgres -d mastermind_bd < docker/postgres/mm-flow-audit.sql
  ```
- Task 1.3: Verificar seed data (workspaces con current_phase para project mastermind)
- Task 1.4: Crear tabla `agent_registry` + seed 7 brains
  - **CORRECCIÓN Brain #5**: UNIQUE(organization_id, project_id, brain_id) — NO solo (org, brain)
  - Seed debe usar DO $$ BEGIN ... END $$ guard (no hardcode slug 'RAP-software' sin guard)
  - Seed debe ser idempotente: ON CONFLICT DO NOTHING
- Task 1.5 [NUEVO — movido desde FASE 4]: Crear config.yml loader + `config.yml` con model profiles
  - Sin esto, DynamicDispatchEngine en FASE 2 no puede cerrar su DoD
  - Loader necesita: missing file → defaults (no crash), malformed YAML → ConfigError, unknown model key → ConfigError

**FASE 2 — CLI ↔ Skills Bridge:**
- Task 2.1: --start/--complete flags en mm-flow execute-phase
  - asyncio.run() wrapper para llamadas async (CLI es síncrono, DB es async)
  - click.UsageError si --start y --complete se usan juntos
  - Output parseable: `execution_id:{uuid}` en stdout
- Task 2.2: DynamicDispatchEngine — reemplaza brain_router.py
  - **CORRECCIÓN Brain #5**: Return type DEBE ser DispatchResult (Pydantic v2 BaseModel strict), NO list[dict]
  - moment param DEBE ser Literal["DISCUSSION","PLANNING","EXECUTION_WAVE","VERIFICATION"]
  - Pre-dispatch budget check (absorbe Task 5.1 de la FASE 5 cortada)
  - SLI de latencia del barrier Brain #7 (Brain #7 recomendación — timeout definido)
- Task 2.3: Wire skills /mm:execute-phase y /mm:plan-phase con CLI bookends

**FASE 3 — Context Persistence:**
- Task 3.1: Stop hook (StopSession) — NO EXISTE, crear nuevo `~/.claude/hooks/mm-flow-stop.js`
  - Lógica de escritura de checkpoint EXTRAÍDA a Python (testeable en CI)
  - JavaScript hook = thin dispatcher que llama script Python
  - Trigger: write-tool-call detection (no message count)
- Task 3.2: Modificar mm-flow-session-init.js para:
  - Detectar SESSION-CHECKPOINT.md sin guardar
  - Calcular antigüedad: si >48h → warning de stale checkpoint en previewMessage
- Task 3.3: Reemplazar "cada 10 mensajes" por keyword/write-tool detection en mm-flow-context-monitor.js
  - Candidato: mm-flow-context-monitor.js (191 líneas, PostToolUse — ya existe)
- Task 3.4: Fix context_loader flow (skill genera CONTEXT.md via mem_search, CLI solo registra artifact)

**FASE 4 — Audit Trail + Config + Missing Pieces:**
- Task 4.1 [REEMPLAZA "wire audit router"]: Agregar JWT auth a los 13 endpoints de apps/api/routers/audit.py
  - **BLOQUEADOR Brain #5 + #6**: 13 routes, 0 con Depends(get_current_user_any)
  - HTTP-level tests: mínimo 26 tests en tests/api/test_audit_routes.py
  - Agregar tests/api/ al step level2-tests de ci.yml
- Task 4.2: ~~config.yml~~ → movido a FASE 1 Task 1.5
- Task 4.3: Crear ~/.claude/backends.sh (credentials loader)
- Task 4.4: EXTENDER mm-flow-statusline.js (NO reemplazar)
  - Preservar líneas 24-43 (context window % lógica)
  - Agregar: phase state, active brain, backend identity
  - Reemplazar emoji 🟢 por ANSI color + text: [ACTIVE]=\x1b[32m, [BARRIER]=\x1b[33m, [IDLE]=\x1b[2m, [OFFLINE]=\x1b[31m
  - Data source: runtime-state.json (NO consultar DB — latencia inaceptable en cada respuesta)

**FASE 5 — CORTADA (Build Trap):**
- Budget tracking: absorbido en FASE 2 Task 2.2 (pre-dispatch check en DynamicDispatchEngine)
- Heartbeat: presupone brains como procesos independientes — no existen así
- Sub-fases decimales: no reduce T1
- Plugin SDK: presupone canal externo no planificado

</remaining_work>

<decisions_made>

**De sesiones anteriores:**
- CLI es helper de estado, Claude es executor. Skills bookendean con --start y --complete.
- context_loader stub quedará en CLI. Skill genera CONTEXT.md nativamente con mem_search.
- brain_router.py: deprecar → DynamicDispatchEngine

**Nuevas decisiones de esta sesión (brain council):**
- FASE 5 CORTADA — Brain #1 ganó el conflicto vs Brains #2/#3/#5 que optimizaban features que no deberían existir
- config.yml MOVIDO a FASE 1 Task 1.5 (dependencia invertida detectada por Brain #1 + confirmada por Brain #5)
- UNIQUE constraint agent_registry: (org_id, project_id, brain_id) — no solo (org_id, brain_id)
- Stop hook: lógica en Python (testeable CI), JS = thin dispatcher
- Statusline: EXTENDER archivo existente, no reemplazar
- Trigger de checkpoint: write-tool-call detection, no message count
- Budget check: pre-dispatch en DynamicDispatchEngine (FASE 2), no FASE 5
- DispatchResult: Pydantic v2 BaseModel strict — no list[dict]

</decisions_made>

<blockers>

**3 blockers críticos identificados por brain council — deben resolverse en las Fases correspondientes:**

1. **FASE 4 blocker** — audit.py tiene 13 routes con 0 JWT auth → production-unsafe. La tarea real de FASE 4 es agregar auth, no wire the router (ya está wired).

2. **FASE 2 blocker** — DynamicDispatchEngine retorna list[dict] en el plan → debe ser DispatchResult (Pydantic v2 strict).

3. **FASE 1 blocker** — agent_registry UNIQUE(org_id, brain_id) es incorrecto → debe ser UNIQUE(org_id, project_id, brain_id). Hacerlo antes de seed o se necesita migración.

**Ningún blocker para arrancar FASE 1.**

</blockers>

<context>
Esta sesión fue de evaluación del plan. Los 7 brain agents evaluaron el MM-Flow Completion Plan y devolvieron feedback concreto basado en código verificado (no opiniones genéricas). Brain #5 llegó a nivel Principal (5/5) al encontrar que audit.py tiene 13 endpoints sin JWT — producción-inseguro, y el CI lo enmascara activamente.

El plan corregido tiene 4 fases (FASE 5 cortada), un Task 1.5 nuevo (config.yml), y correcciones específicas en cada fase. El plan NO se ejecutó esta sesión — solo se evaluó y corrigió.

**Archivos clave:**
- `.planning/MM-FLOW-COMPLETION-PLAN.md` — plan v2 (con las correcciones del brain council en mente, pero el archivo aún no fue actualizado para reflejar los cambios — hacerlo antes o durante FASE 1)
- `.planning/BRAIN-FEED-*.md` — todos actualizados por los brains con insights de esta evaluación
- `apps/api/routers/audit.py` — 1593 líneas, 13 routes sin JWT
- `~/.claude/hooks/mm-flow-statusline.js` — existe, extender (no reemplazar)
- `~/.claude/hooks/mm-flow-context-monitor.js` — 191 líneas, PostToolUse, candidato para keyword detection
- `docker/postgres/mm-flow-audit.sql` — SQL listo para aplicar (FASE 1 Task 1.2)
</context>

<next_action>
1. `/clear` → `/gsd:resume-work` para restaurar contexto
2. Actualizar MM-FLOW-COMPLETION-PLAN.md para reflejar las correcciones del brain council
3. Ejecutar FASE 1:
   ```bash
   # Task 1.2: Audit trail SQL
   docker exec -i mastermind-postgres-1 psql -U postgres -d mastermind_bd < docker/postgres/mm-flow-audit.sql

   # Task 1.3: Verificar seed data
   docker exec mastermind-postgres-1 psql -U postgres -d mastermind_bd \
     -c "SELECT w.name, w.current_phase FROM workspaces w JOIN projects p ON w.project_id = p.id WHERE p.slug = 'mastermind';"

   # Task 1.4: agent_registry (con UNIQUE correcto: org + project + brain)
   # Task 1.5: config.yml loader
   ```
</next_action>
