---
phase: mm-flow-completion
task: 1
total_tasks: 5
status: in_progress
last_updated: "2026-04-14T00:44:33Z"
---

<current_state>
Sesión de auditoría y replanteo de MM-Flow. Se auditaron los 3 repos fuente (GSD, Paperclip, OpenClaw), se identificaron todos los gaps, y se creó el plan de completación v2 en `.planning/MM-FLOW-COMPLETION-PLAN.md`. PostgreSQL de mastermind fue levantado (docker compose). El plan está aprobado — no arrancamos la ejecución todavía.
</current_state>

<completed_work>

**Esta sesión:**
- ✅ Auditoría completa de MM-Flow — estado real de cada módulo documentado
- ✅ Diagnóstico de PostgreSQL — schema principal YA existe (organizations, projects, workspaces, mm_flow_state, backend_sessions, etc.)
- ✅ PostgreSQL mastermind levantado: `docker compose up -d postgres`
- ✅ Análisis de 3 repos fuente:
  - GSD (`~/.claude/get-shit-done/`) — model profiles, sub-fases decimales, Nyquist auditing NO adoptados
  - Paperclip (`/proy/paperclip/`) — agentes como entidades ricas, budget/heartbeat NO adoptados
  - OpenClaw (`/proy/openclaw/`) — channel dispatch, plugin SDK, streaming NO adoptados
- ✅ `MM-FLOW-COMPLETION-PLAN.md` creado y guardado (v2 con análisis repos)
- ✅ Guardado en Engram (ID architecture: "MM-Flow Completion Plan v2")

**Estado de BD:**
- Schema principal: ✅ YA existe (fue aplicado en sesión anterior via init-db.sql)
- Audit trail tables (phase_executions, decisions, etc.): ❌ NUNCA ejecutadas
- agent_registry table: ❌ No existe (nueva, no estaba en diseño original)

</completed_work>

<remaining_work>

**FASE 1 — Infrastructure Foundation (PRÓXIMO PASO):**
- Task 1.2: Aplicar `docker/postgres/mm-flow-audit.sql` → tablas de audit trail
- Task 1.3: Verificar seed data (workspaces con current_phase para project mastermind)
- Task 1.4: Crear tabla `agent_registry` + seed 7 brain agents (SQL inline)

**FASE 2 — CLI ↔ Skills Bridge:**
- Task 2.1: Agregar `--start`/`--complete` flags a `mm-flow execute-phase` (reemplaza placeholder)
- Task 2.2: Crear `dispatch_engine.py` — reemplaza `brain_router.py` por keywords
- Task 2.3: Actualizar skills `/mm:execute-phase` y `/mm:plan-phase` con CLI bookends

**FASE 3 — Context Persistence (el problema original):**
- Task 3.1: Crear `~/.claude/hooks/mm-flow-checkpoint.js` (Stop hook → SESSION-CHECKPOINT.md)
- Task 3.2: Modificar `mm-flow-session-init.js` → detectar checkpoint sin guardar
- Task 3.3: Agregar UserPromptSubmit global recordatorio cada 10 mensajes
- Task 3.4: Fix context_loader → flujo Engram via skill, no CLI

**FASE 4 — Audit Trail + Config:**
- Task 4.1: Wire `apps/api/routers/audit.py` en `apps/api/main.py`
- Task 4.2: Crear `.planning/.mm-flow/config.yml` con model profiles + brain routing
- Task 4.3: Crear `~/.claude/backends.sh`
- Task 4.4: Crear `mm-flow-statusline.js` hook

**FASE 5 — Patrones pendientes de repos:**
- Task 5.1: Budget tracking por brain (Paperclip)
- Task 5.2: `mm-flow brain-status` CLI command (Paperclip heartbeat)
- Task 5.3: Sub-fases decimales en StateMachine (GSD)
- Task 5.4: Plugin SDK estructura base (OpenClaw)

</remaining_work>

<decisions_made>

- **Arquitectura CLI ↔ Skills**: CLI es helper de estado, Claude es executor. Skills bookendean con `mm-flow execute-phase --start` y `--complete`. El CLI NO ejecuta fases.
- **context_loader fix**: CLI no puede llamar Engram MCP. Fix correcto: skill genera CONTEXT.md nativamente con mem_search, CLI solo registra artifact en DB.
- **brain_router.py**: Deprecar. Reemplazar por `dispatch_engine.py` que lee `config.yml`.
- **agent_registry**: Nueva tabla DB (no estaba en diseño original). 7 brains como entidades ricas con capabilities, model_quality, token_budget.
- **Model profiles**: Adoptar patrón GSD — quality (opus) para Brain #7 y decisiones críticas, balanced (sonnet) para domain brains, budget (haiku) para context recovery.
- **Sub-fases decimales**: Adoptar patrón GSD — 19.1, 19.2 en lugar de wave_1, wave_2.
- **Repos analizados**: GSD, Paperclip, OpenClaw. Ver detalles en MM-FLOW-COMPLETION-PLAN.md.

</decisions_made>

<blockers>
Ninguno. Plan aprobado. PostgreSQL corriendo. Siguiente acción es ejecutar FASE 1.
</blockers>

<context>
Esta sesión fue de recuperación y planificación. El usuario había perdido el contexto de sesiones anteriores (Engram no guardó nada de las 3 sesiones de hoy). Reconstruimos todo desde los archivos del proyecto + análisis fresco de los 3 repos fuente. El plan MM-FLOW-COMPLETION-PLAN.md es el artefacto principal de esta sesión.

**Archivos clave:**
- `.planning/MM-FLOW-COMPLETION-PLAN.md` — plan completo v2
- `.planning/.mm-flow/` — módulos Python actuales
- `.planning/.mm-flow/cli/commands.py` — CLI con placeholders a reemplazar
- `docker/postgres/mm-flow-audit.sql` — SQL listo para aplicar
- `apps/api/routers/audit.py` — router listo, no wired
- `apps/api/services/engram_sync.py` — servicio listo, no wired
</context>

<next_action>
1. `/clear` → `/gsd:resume-work` para restaurar este contexto
2. Ejecutar FASE 1 del plan:
   ```bash
   docker exec -i mastermind-postgres-1 psql -U postgres -d mastermind_bd < docker/postgres/mm-flow-audit.sql
   ```
3. Verificar tablas creadas y continuar con Task 1.4 (agent_registry)
</next_action>
