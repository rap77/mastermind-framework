---
phase: agent-restructuring
task: 3
total_tasks: 6
status: in_progress
last_updated: "2026-03-31T22:47:06.258Z"
---

<current_state>
Fases 1+2 COMPLETAS y commiteadas (f3efd20). Listas para Fase 3.
Plan file: /home/rpadron/.claude/plans/sprightly-conjuring-hartmanis.md

7 cerebros evaluaron el plan esta sesión — 4 BLOCKERS identificados:
1. ✅ RESUELTO — create_experience_schema() en startup_event (Fase 2)
2. ❌ PENDIENTE — FlowDetector.get_flow_sequence() devuelve list[int], task_runner.py necesita mapping int→string
3. ❌ PENDIENTE — asyncio.create_task() → FastAPI BackgroundTasks en task_runner.py
4. ❌ PENDIENTE — wsStore.ts multi-taskId architecture (afecta Fase 4, no Fase 3)
</current_state>

<completed_work>

Fase 1 — Agent Contract (Brain #1 pilot) ✅
- CREADO: apps/api/mastermind_cli/tools/__init__.py
- CREADO: apps/api/mastermind_cli/tools/brain_memory.py — CLI query/log para brain agents via Bash
- MODIFICADO: .claude/agents/mm/brain-01-product/brain-01-product.md
  - Eliminado: mcpServers: notebooklm-mcp del frontmatter
  - Agregado: Protocolo de Memoria (Pasos 0-A query, 0-B nlm, Final log)
  - Agregado: Canonical structured output con routing fields

Fase 2 — ExperienceLogger activation ✅
- MODIFICADO: apps/api/mastermind_cli/api/app.py — create_experience_schema() en startup_event
- CREADO: apps/api/mastermind_cli/api/routes/experiences.py — GET /api/experiences/{brain_id}
  - JWT auth, limit+offset pagination, Option A IDOR (single-user)

Suite: 578 passed, 8 skipped, 3 RED stubs (Phase 12 — intencionales)
Commit: f3efd20 — todos los hooks pasaron (GGA, ruff, mypy)
</completed_work>

<remaining_work>

Fase 3 — Conectar API ↔ Ejecución Real:
- CREAR: apps/api/mastermind_cli/api/services/task_runner.py
  - CRÍTICO: FastAPI BackgroundTasks, NO asyncio.create_task()
  - CRÍTICO: lookup table validada FlowDetector int→string ANTES de escribir código
  - StatelessCoordinator.execute_flow(Brief(...), [brain_id]) — firma confirmada
  - Manejo de CancelledError: except (Exception, asyncio.CancelledError)
- MODIFICAR: apps/api/mastermind_cli/api/routes/tasks.py:98 — resolver TODO

Fase 4 — Brain-to-Brain Communication:
- CREAR: apps/api/mastermind_cli/orchestrator/brain_router.py
- PENDIENTE: wsStore.ts multi-taskId decision (Blocker #4) antes de Fase 4

Fases 5+6 — Auto-routing + replicate a Brains #2-#7

Tests requeridos (~23 nuevos, target ~612 backend):
- tests/api/test_app_startup.py (3 tests)
- tests/api/test_task_runner.py (6 tests — BackgroundTasks pattern)
- tests/api/test_experiences_route.py (4 tests)
- tests/unit/test_brain_router.py (5 tests)
- tests/cli/test_brain_memory_cli.py (5 tests)
</remaining_work>

<decisions_made>

- asyncio.create_task() → FastAPI BackgroundTasks — Brain #6 gana sobre Brain #5
- IDOR Option A — shared system telemetry (single-user tool). Sin user_id es intencional.
- Baseline corregido: 589 tests (no 575/579 — feeds estaban stale)
- Edge animation en Nexus RECHAZADA — ICE 4.8 < 15 (Brain #3)
- wsDispatcher.ts NO existe → archivo real es wsStore.ts (error crítico en el plan original)
- Evento WS 'status_change' NO existe → tipo real es 'task_update_batch' (Zod locked, api.ts:46)
- model: inherit en brain agents es CORRECTO — fix de Phase 11 ya aplicado
- FlowDetector int→string: lookup table validada, NO f-string interpolation
</decisions_made>

<blockers>

Blocker #2 (Fase 3): FlowDetector int→string mapping.
  Archivo: apps/api/mastermind_cli/orchestrator/flow_detector.py:115-139
  Mapping: {1: "brain-01-product", 2: "brain-02-ux", 3: "brain-03-ui",
            4: "brain-04-frontend", 5: "brain-05-backend", 6: "brain-06-qa", 7: "brain-07-growth"}

Blocker #4 (Fase 4): wsStore.ts single-taskId guard (line 38). brain_routing
  puede generar sub_task_id por brain — arquitectura multi-taskId sin confirmar.
  Resolver antes de Fase 4.

Brain #7 PROPOSALS for global BRAIN-FEED (pendiente decisión del usuario):
1. ExperienceLogger activation rate como invariante de Phase 12
2. BackgroundTasks como Architecture Pattern Invariant global
3. Baseline anchor actualizado a 589
</blockers>

<context>
Esta sesión evaluó el plan completo con los 7 cerebros en paralelo — primera vez que
se usa el dispatch completo mm:ask-all en producción. La evaluación encontró errores
críticos en el plan original (archivos inexistentes, tipos WS incorrectos) que
hubieran causado fallos silenciosos.

Los 7 domain BRAIN-FEED files se actualizaron con los findings de esta sesión.
Fases 1+2 se implementaron, testearon y commitearon en la misma sesión.
</context>

<next_action>
/clear → /sc:load → /gsd:resume-work

Para Fase 3:
1. Leer flow_detector.py:115-139 — verificar tipo de retorno de get_flow_sequence()
2. Definir BRAIN_ID_MAP lookup table en task_runner.py
3. Escribir tests/api/test_task_runner.py PRIMERO (TDD)
4. Crear apps/api/mastermind_cli/api/services/task_runner.py con BackgroundTasks
5. Modificar apps/api/mastermind_cli/api/routes/tasks.py:98 — resolver TODO
</next_action>
