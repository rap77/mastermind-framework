# Session: Post-v2.1 Debt Audit

**Date:** 2026-03-25
**Outcome:** 5 undocumented tech debt items found after milestone close. Pending documentation in PROJECT.md.

## Context
After archiving v2.1 milestone, ran a thorough codebase audit (TODOs, Pyright, tsc --noEmit, console.logs, `any` types). Found items NOT in documented tech debt.

## Findings

### ⚠️ Undocumented (needs action in v2.2)

1. **Pyright 156 errors in test files** — introduced in monorepo restructure commit f4d1315 (v2.1).
   ALL in /tests/, zero in production mastermind_cli/. Were incorrectly assumed pre-existing.
   Files: test_interfaces.py, test_legacy_wrapper.py, test_types.py, test_brain_protocol.py, etc.

2. **TypeScript fixture drift** — `apps/web/src/test/fixtures/brains.ts` uses `id: 1` (number)
   but BrainMetadataSchema now expects `id: string` (changed in commit 6dc0538, tech debt fix).
   `tsc --noEmit` fails with 5 errors. vitest still passes (no type-check at runtime).
   FIX: change `id: 1` → `id: '1'` in all MOCK_BRAINS entries (trivial, ~5 lines).

3. **uptime/last_called_at hardcoded** — brain_registry.py:170-171 always returns
   `uptime: 0.0` and `last_called_at: null`. Visible as "0 uptime" in Command Center UI.
   Requires tracking from execution history table.

4. **prefers-reduced-motion guard** — BrainTile.tsx:159 has TODO for accessibility guard.
   Was planned for "Task 4" but not implemented. Severity: low (accessibility only).

5. **websocket-metrics.ts monitoring stubs** — lines 91, 112, 132 have TODO for
   Prometheus/Datadog integration. Severity: low (observability, not functional).

### ✅ Intentional / Not debt

- tasks.py:98 TODO "Integrate with Coordinator.orchestrate()" — intentional design:
  CC-03 accepted in audit, web orchestration is CLI-only in v2.1. Not a bug.
- Pyright 0 errors in production code (mastermind_cli/)
- console.log: none in production web code
- `any` types: none in production web code
- SECRET_KEY hardcoded: already in PROJECT.md tech debt

## Current State
- v2.1 archived, tag v2.1 created LOCAL (not pushed to origin)
- master is 208 commits ahead of origin/master
- .continue-here.md created in phase 08 directory (commit a94a06e)
- PROJECT.md tech debt section NOT yet updated with these 5 items

## Next Session Action
1. Document 5 items in PROJECT.md "Technical debt carried into v2.2" section
2. Fix TypeScript fixture drift (trivial — change id: number to id: string in brains.ts)
3. Commit + git push origin master && git push origin v2.1
4. /gsd:new-milestone v2.2
