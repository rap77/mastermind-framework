# Session: v2.1 Gap Closure + Audit Passed

**Date:** 2026-03-25
**Outcome:** v2.1 milestone audit PASSED (tech_debt) — all 4 wiring gaps closed, suite clean, ready for complete-milestone

## Work Completed

### Gap Closure (branch fix/v2.1-wiring-gaps → master, commit 4ca71cf)
- **ER-02**: Created `/api/keys/route.ts` (GET, POST) + `/api/keys/[id]/route.ts` (DELETE) — Next.js proxy to FastAPI /api/keys
- **ER-03**: Created `/api/brains/[id]/yaml/route.ts` proxy + wired BrainYAMLViewer in NodeDetailPanel ("View YAML Config" button)
- **UX-01**: Wrapped NexusCanvas in `<NexusPage>` in `/nexus/page.tsx` — Focus Mode now activates
- **FND-03**: Moved `command-center/` → `(protected)/command-center/` — AuthGuardLayout now applies

### CORS Test Fix (commit 300d074)
- `test_cors_configuration` was sending `http://example.com` (not in ALLOWED_ORIGINS)
- Fixed: changed origin to `http://localhost:3000` (the default allowed origin)

### Audit (/gsd:audit-milestone)
- Integration checker confirmed all 4 gaps wired
- 22/22 requirements satisfied
- Status: tech_debt (no blockers)
- 12 tech debt items documented, all deferred to v2.2

## Final Suite
- Frontend: 407/407
- Backend: 575/575 (0 failures)

## Tech Debt Accepted for v2.2
1. BE-02: no Next.js proxy for /api/tasks/{id}/graph, NexusCanvas uses static star topology
2. WSBrainBridge disconnect() cleanup may race with CommandCenterWrapper-initiated connections
3. ×0 invocation counter hardcoded in BrainNode.tsx (sessionInvocationCounts write path works, read path stub)
4. BrainMetadataSchema dead code — REST brain responses not validated via Zod
5. useCallback deps missing startTask+router in CommandCenterWrapper
6. niche enum misaligned across BrainMetadataSchema / Brain interface / NexusCanvas runtime

## Next Session
`/gsd:complete-milestone` — archive v2.1, prepare v2.2 ROADMAP (Brain Agents)
