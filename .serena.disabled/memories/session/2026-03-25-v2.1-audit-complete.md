# Session: v2.1 Audit Complete — Wiring Gaps Found

**Date:** 2026-03-25
**Outcome:** v2.1 milestone audit ejecutado, gaps_found — 3 wiring gaps críticos detectados

## Work Completed

### Pre-audit fixes
- REQUIREMENTS.md actualizado: BE-01, CC-01, CC-02, CC-03 marcados [x] (checkboxes desactualizados de Phase 06)
- Phase 07 VERIFICATION.md generado (faltaba — nunca se corrió gsd-verifier al terminar Phase 07)
  - 4/4 requirements PASSED: BE-02, NEX-01, NEX-02, NEX-03

### Audit (v2.1-MILESTONE-AUDIT.md — commit 4992ac1)
- 4 fases verificadas: 05 ✅, 06 ✅, 07 ✅ (generado hoy), 08 ✅
- Integration checker ejecutado → 3 wiring gaps de producción confirmados

## Gaps Encontrados

### Críticos (features implementados pero no conectados)

1. **ER-02** — `/api/keys` Next.js proxy route NO existe
   - `APIKeyManager.tsx` llama `fetch('/api/keys')` → 404 en prod
   - Fix: crear `apps/web/src/app/api/keys/route.ts` (GET/POST/DELETE)

2. **ER-03** — `BrainYAMLViewer` es dead code + `/api/brains/[id]/yaml` proxy faltante
   - Componente existe pero nunca se monta en ninguna página de producción
   - Fix: wirear en NodeDetailPanel o Engine Room + crear proxy route

3. **UX-01** — `NexusPage` wrapper bypassed → Focus Mode nunca activa
   - `/nexus/page.tsx` renderiza `NexusCanvas` directo, bypassa `NexusPage`
   - Fix: 1 línea en `/nexus/page.tsx` — envolver canvas en `<NexusPage>`

### Parciales (no bloquean milestone)
4. **FND-03** — `/command-center` fuera del route group `(protected)` → sin auth
   - Fix: mover a `app/(protected)/command-center/`
5. **BE-02** — endpoint `/api/tasks/{id}/graph` sin consumer frontend (para v2.2)

## Current State

- Branch: master
- Audit: gaps_found — 3 unsatisfied (ER-02, ER-03, UX-01)
- Handoff: .planning/phases/08-strategy-vault-engine-room/.continue-here.md (commit d0552df)

## Next Session

1. `/gsd:plan-milestone-gaps` — crear fase de cierre con los 4 fixes (~60 min estimado)
2. Ejecutar fase de cierre
3. `/gsd:complete-milestone` — archivar v2.1, preparar v2.2
